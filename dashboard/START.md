# 🚀 Инструкция по запуску дашборда

## Шаг 1: Запуск серверов

### Migration API (порт 8080)
Migration API должен быть доступен на `http://localhost:8080`

Проверьте работоспособность:
```bash
curl http://localhost:8080/
```

Если API не работает, запустите сервер:
```bash
php -S localhost:8080 -t public
```

### Dashboard API (порт 8000)
Dashboard API должен быть доступен на `http://localhost:8000/dashboard/api`

Проверьте работоспособность:
```bash
curl http://localhost:8000/dashboard/api/health
```

Если API не работает, запустите сервер:
```bash
cd dashboard
php -S localhost:8000 -t .
```

## Шаг 2: Установка зависимостей фронтенда

```bash
cd dashboard/frontend
npm install
```

## Шаг 3: Сборка фронтенда

Для production:
```bash
cd dashboard/frontend
npm run build
```

Для разработки (с hot reload):
```bash
cd dashboard/frontend
npm run dev
```

Dev сервер будет доступен на `http://localhost:3000` (или другом порту, который укажет Vite)

## Шаг 4: Доступ к дашборду

После сборки фронтенд будет доступен по адресу:
- **Production**: http://localhost:8000/dashboard
- **Development**: http://localhost:3000 (если запущен `npm run dev`)

**Важно**: 
- Dashboard работает на порту **8000**
- Migration API работает на порту **8080**

## Проверка работы

1. Откройте браузер и перейдите на http://localhost:8000/dashboard
2. Должен открыться интерфейс дашборда
3. Проверьте вкладку "Анализ" в деталях миграции

## Возможные проблемы

### Фронтенд не собран
Если видите сообщение "Фронтенд не собран", выполните:
```bash
cd dashboard/frontend
npm install
npm run build
```

### Dashboard API не отвечает
Проверьте что PHP сервер запущен на порту 8000:
```bash
# Проверьте процессы
ps aux | grep "php.*8000"

# Или запустите сервер с router.php (обязательно!)
cd dashboard
php -S localhost:8000 router.php
```

**Важно**: Для правильной работы статических файлов необходимо использовать `router.php`:
```bash
cd dashboard
php -S localhost:8000 router.php
```

Без `router.php` статические файлы (JS, CSS) не будут загружаться.

### Migration API не отвечает
Проверьте что PHP сервер запущен на порту 8080:
```bash
# Проверьте процессы
ps aux | grep "php.*8080"

# Или запустите сервер
php -S localhost:8080 -t public
```

### Ошибки при сборке
Убедитесь что установлены все зависимости:
```bash
cd dashboard/frontend
rm -rf node_modules package-lock.json
npm install
npm run build
```

## Структура URL

- `/dashboard` - главная страница дашборда
- `/dashboard/api/*` - API endpoints
- `/dashboard/api/migrations/:id/quality-analysis` - анализ качества миграции
- `/dashboard/api/screenshots/:filename` - скриншоты страниц
