# 🎯 Быстрый старт: Анализ качества миграций

## ✅ Статус

Дашборд готов к использованию! Все компоненты установлены и собраны.

## 🚀 Запуск

### Вариант 1: Production (уже собран)
Просто откройте в браузере:
```
http://localhost:8080/dashboard
```

### Вариант 2: Development (с hot reload)
```bash
cd dashboard/frontend
npm run dev
```
Затем откройте: `http://localhost:3000`

## 📊 Использование анализа качества

### 1. Запуск миграции с анализом

Для включения анализа качества добавьте параметр `quality_analysis=true`:

```bash
curl -X POST http://localhost:8080/dashboard/api/migrations/run \
  -H "Content-Type: application/json" \
  -d '{
    "mb_project_uuid": "your-uuid",
    "brz_project_id": 12345,
    "mb_site_id": 31383,
    "mb_secret": "your-secret",
    "quality_analysis": true
  }'
```

Или через интерфейс дашборда - параметр будет добавлен автоматически.

### 2. Просмотр анализа в дашборде

1. Откройте дашборд: http://localhost:8080/dashboard
2. Выберите миграцию из списка
3. Перейдите на вкладку **"Анализ"**
4. Просмотрите:
   - Статистику по всем страницам
   - Рейтинги качества каждой страницы
   - Уровни критичности проблем

### 3. Детальный просмотр страницы

1. В списке страниц нажмите на карточку страницы
2. Откроется модальное окно с деталями:
   - **Обзор**: общая информация, рейтинг, ссылки
   - **Скриншоты**: сравнение исходной и мигрированной страниц
   - **Проблемы**: список проблем, рекомендации, детальный отчет

## 🔍 API Endpoints для анализа

### Получить список анализов миграции
```bash
GET /dashboard/api/migrations/:id/quality-analysis
```

### Получить статистику
```bash
GET /dashboard/api/migrations/:id/quality-analysis/statistics
```

### Получить детали анализа страницы
```bash
GET /dashboard/api/migrations/:id/quality-analysis/:pageSlug
```

### Получить скриншот
```bash
GET /dashboard/api/screenshots/:filename
```

## 📁 Где хранятся скриншоты

Скриншоты сохраняются в:
```
var/tmp/project_{project_id}/
```

Например:
```
var/tmp/project_12345/source_abc123.png
var/tmp/project_12345/migrated_abc123.png
```

## 🐛 Решение проблем

### Вкладка "Анализ" не отображается
- Убедитесь что фронтенд пересобран: `cd dashboard/frontend && npm run build`

### Нет данных анализа
- Проверьте что миграция запущена с параметром `quality_analysis=true`
- Проверьте логи на наличие ошибок анализа

### Скриншоты не загружаются
- Проверьте что файлы существуют в `var/tmp/project_*/`
- Проверьте права доступа к файлам
- Проверьте логи браузера на ошибки загрузки

## 📝 Примеры

### Проверка наличия анализа для миграции
```bash
curl http://localhost:8080/dashboard/api/migrations/22926177/quality-analysis
```

### Получение статистики
```bash
curl http://localhost:8080/dashboard/api/migrations/22926177/quality-analysis/statistics
```
