# Найденные проблемы

## Статус: ⏳ В процессе выявления

---

## Критические проблемы

### 1. ✅ ИСПРАВЛЕНО: Мобильные padding'ы устанавливались из margin-bottom

**Описание:**
В строках 139-148 файла `SectionStylesAble.php` все мобильные padding'ы устанавливались из `margin-bottom` вместо соответствующих `padding-*` значений.

**Было:**
```php
->set_mobilePaddingTop((int)($sectionStyles['margin-bottom'] ?? 0))
```

**Стало:**
```php
->set_mobilePaddingTop((int)($additionalOptions['mobilePaddingTop'] ?? $sectionStyles['padding-top'] ?? 0))
```

**Статус:** ✅ Исправлено
**Дата исправления:** Сегодня
**Файл:** `lib/MBMigration/Builder/Layout/Common/Concern/SectionStylesAble.php`

---

## Важные проблемы

### 2. ⏳ ПРОВЕРЯЕТСЯ: Мобильные margin'ы не извлекаются из DOM

**Описание:**
В текущей реализации мобильные margin'ы не извлекаются из DOM и не применяются к секциям.

**Текущее поведение:**
- Desktop margin'ы извлекаются и применяются
- Mobile margin'ы не извлекаются
- Mobile margin'ы могут быть установлены только через `additionalOptions`

**Вопрос:**
- Нужно ли извлекать мобильные margin'ы из DOM?
- Есть ли в исходных сайтах мобильные margin'ы?

**Статус:** ⏳ Требует проверки
**Приоритет:** Средний

---

### 3. ⏳ ПРОВЕРЯЕТСЯ: Tablet margin'ы не извлекаются из DOM

**Описание:**
В текущей реализации tablet margin'ы не извлекаются из DOM и не применяются к секциям.

**Текущее поведение:**
- Desktop margin'ы извлекаются и применяются
- Tablet margin'ы не извлекаются
- Tablet margin'ы могут быть установлены только через `additionalOptions`

**Вопрос:**
- Нужно ли извлекать tablet margin'ы из DOM?
- Есть ли в исходных сайтах tablet margin'ы?

**Статус:** ⏳ Требует проверки
**Приоритет:** Средний

---

## Потенциальные проблемы

### 4. ⏳ ПРОВЕРЯЕТСЯ: Margin collapse в CSS vs Brizy

**Описание:**
В CSS margin'ы между соседними элементами схлопываются (collapse). В Brizy margin'ы могут не схлопываться.

**Текущее поведение:**
- Мы применяем `marginTop` и `marginBottom` к каждой секции
- В исходных сайтах margin'ы равны 0, поэтому это не проблема
- Но если в будущем появятся ненулевые margin'ы, может возникнуть проблема

**Вопрос:**
- Как Brizy обрабатывает margin collapse?
- Нужно ли учитывать это при применении margin'ов?

**Статус:** ⏳ Требует проверки
**Приоритет:** Низкий (так как margin'ы сейчас равны 0)

---

### 5. ⏳ ПРОВЕРЯЕТСЯ: Отступы через другие механизмы

**Описание:**
В исходных сайтах отступы между секциями могут быть реализованы не только через margin'ы, но и через:
- Padding родительских элементов
- Gap в grid/flexbox
- Специальные обертки с отступами

**Текущее поведение:**
- Мы извлекаем только margin'ы и padding'ы самой секции
- Мы не проверяем родительские элементы
- Мы не проверяем gap в grid/flexbox

**Вопрос:**
- Есть ли в исходных сайтах отступы через другие механизмы?
- Нужно ли их учитывать?

**Статус:** ⏳ Требует проверки
**Приоритет:** Средний

---

## Желательные улучшения

### 6. 💡 ИДЕЯ: Оптимизация установки margin'ов

**Описание:**
Если margin равен 0, можно не устанавливать его явно, чтобы не засорять JSON.

**Текущее поведение:**
- Мы всегда устанавливаем `marginType: 'ungrouped'`
- Мы всегда устанавливаем все margin'ы, даже если они равны 0

**Предложение:**
- Устанавливать `marginType` только если есть ненулевые margin'ы
- Не устанавливать margin'ы, если они равны 0

**Статус:** 💡 Идея
**Приоритет:** Низкий

---

## Сводка

- **Критические проблемы:** 1 (исправлена) + 5 новых (найдены в анализе)
- **Важные проблемы:** 2 (требуют проверки) + 2 новых (найдены в анализе)
- **Потенциальные проблемы:** 2 (требуют проверки) + 1 новая (найдена в анализе)
- **Желательные улучшения:** 1 (идея)

**См. также:** `MISSING_ISSUES_ANALYSIS.md` - полный анализ упущенных проблем

---

## Следующие шаги

1. ✅ Проверить, нужно ли извлекать мобильные margin'ы из DOM (TASK-003)
2. ✅ Проверить, нужно ли извлекать tablet margin'ы из DOM (TASK-004, обновлена)
3. ⚠️ **КРИТИЧНО:** Исправить tablet padding'ы и margin'ы в `handleSectionStyles()` (TASK-004)
4. ⚠️ **КРИТИЧНО:** Исправить `setTopPaddingOfTheFirstElement` (TASK-007)
5. ⚠️ **КРИТИЧНО:** Исправить mobile margin'ы в `FullTextBlurBox.php` (TASK-008)
6. Проверить, как Brizy обрабатывает margin collapse (TASK-002)
7. Проверить, есть ли отступы через другие механизмы (TASK-005)

**См. также:** `MISSING_ISSUES_ANALYSIS.md` - полный анализ упущенных проблем
