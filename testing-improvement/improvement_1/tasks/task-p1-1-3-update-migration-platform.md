# Задача 1.1.3: Обновить MigrationPlatform для использования инжектированного Logger

## 📋 Информация о задаче

**Время выполнения**: 1-2 дня  
**Приоритет**: КРИТИЧЕСКИЙ (Приоритет 1)  
**Зависимости**: [Задача 1.1.2: Обновить ApplicationBootstrapper](./task-p1-1-2-update-application-bootstrapper.md)  
**Родительская задача**: [Задача 1.1: Убрать синглтоны](./task-p1-1-remove-singletons.md)

## 🎯 Принципы работы

⚠️ **ВАЖНО**: При выполнении этой задачи следуйте принципам из [PRINCIPLES.md](../../../../PRINCIPLES.md)

## ❌ Проблема

`MigrationPlatform` уже получает Logger через конструктор, но внутри класса все еще используется `Logger::instance()` (20 использований), что создает зависимость от глобального состояния.

**Почему это важно**:
- MigrationPlatform - основной класс миграции
- Невозможно полностью тестировать MigrationPlatform с моками
- Смешивание DI и глобального состояния создает путаницу

**Что будет, если не решить**:
- Невозможно создать полноценные unit тесты для MigrationPlatform
- Тесты будут зависеть от глобального состояния Logger

## 🎯 Ожидаемый результат

После выполнения задачи:

1. **Все Logger::instance() заменены** - на использование `$this->logger`
2. **Logger используется только через DI** - нет зависимостей от глобального состояния
3. **Тесты написаны** - тесты для MigrationPlatform с моками Logger

**Как проверить результат**:
- Нет вызовов `Logger::instance()` в MigrationPlatform
- Все логирование идет через `$this->logger`

## 📝 Что нужно сделать

### Шаг 1: Изучить MigrationPlatform

⚠️ **НЕ ПРИДУМЫВАТЬ!** Изучить реальный код:

**Файлы для изучения**:
- `lib/MBMigration/MigrationPlatform.php` - изучить все использования Logger

**Что изучить**:
- Где используется `Logger::instance()`
- Как Logger инжектируется через конструктор
- Какие методы используют Logger

### Шаг 2: Заменить все Logger::instance()

**Что сделать**:
1. Найти все вызовы `Logger::instance()` в MigrationPlatform
2. Заменить на использование `$this->logger`
3. Убедиться, что Logger доступен во всех методах

**Файлы для работы**:
- `lib/MBMigration/MigrationPlatform.php` - заменить все использования

**Важно**:
- Обновлять постепенно, по одному методу
- После каждого обновления запускать тесты

### Шаг 3: Написать тесты

⚠️ **ОБЯЗАТЕЛЬНО**: После реализации сразу написать тесты!

**Что тестировать**:
- MigrationPlatform использует инжектированный Logger
- Можно мокировать Logger в тестах
- Все методы логирования работают правильно

**Где создать тесты**:
- `tests/MBMigration/MigrationPlatformTest.php`

**Какие данные использовать**:
- ❌ НЕ придумывать тестовые данные
- ✅ Использовать реальные конфигурации из проекта

### Шаг 4: Добавить комментарии

⚠️ **ОБЯЗАТЕЛЬНО**: Добавить PHPDoc комментарии!

**Что комментировать**:
- Свойство `$logger` - описание назначения
- Методы, использующие Logger - описание использования

### Шаг 5: Запустить тесты

⚠️ **ОБЯЗАТЕЛЬНО**: Запустить тесты и убедиться, что все проходит!

**Команда**: `composer test`

**Если тесты не проходят**:
- ❌ НЕ продолжать дальше
- ✅ Исправить проблему сначала

## 📂 Исходные файлы

- `lib/MBMigration/MigrationPlatform.php` - класс MigrationPlatform

## ✅ Критерии приемки

- [ ] Все использования Logger::instance() заменены на $this->logger
- [ ] Нет вызовов Logger::instance() в MigrationPlatform
- [ ] Тесты написаны и проходят
- [ ] Все методы имеют PHPDoc комментарии
- [ ] Файлы добавлены в git

## 🔍 Как проверить

1. Проверить, что нет вызовов `Logger::instance()`: `grep -r "Logger::instance()" lib/MBMigration/MigrationPlatform.php`
2. Запустить тесты: `composer test`

## ⚠️ Важные замечания

- **НЕ придумывать** - если не знаешь, СПРОСИТЬ
- **Сразу тестировать** - после каждого изменения
- **Комментировать все** - PHPDoc обязателен

## 📚 Связанные задачи

- Предыдущая задача: [Задача 1.1.2: Обновить ApplicationBootstrapper](./task-p1-1-2-update-application-bootstrapper.md)
- Родительская задача: [Задача 1.1: Убрать синглтоны](./task-p1-1-remove-singletons.md)
- Следующая задача: [Задача 1.1.4: Обновить другие ключевые классы для использования LoggerFactory](./task-p1-1-4-update-other-classes-logger.md)

---

**ВАЖНО**: Если во время выполнения задачи возникли вопросы или что-то неизвестно - ОСТАНОВИТЬСЯ и ЗАПРОСИТЬ у пользователя, не придумывать!
