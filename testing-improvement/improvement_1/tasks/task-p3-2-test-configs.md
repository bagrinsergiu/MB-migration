# Задача 3.2: Добавить тестовые конфигурации

## 📋 Информация о задаче

**Время выполнения**: 3-5 дней  
**Приоритет**: СРЕДНИЙ (Приоритет 3)  
**Зависимости**: [Задача 3.1: Рефакторинг для снижения связанности](./task-p3-1-reduce-coupling.md)

## 🎯 Принципы работы

⚠️ **ВАЖНО**: При выполнении этой задачи следуйте принципам из [PRINCIPLES.md](../../../../PRINCIPLES.md)

## ❌ Проблема

Нет отдельных конфигураций для разных типов тестов:
- Unit тесты используют те же конфигурации, что и production
- Integration тесты требуют реальных сервисов
- E2E тесты требуют полной конфигурации
- Невозможно переключиться между реальными и моковыми сервисами

**Почему это важно**:
- Разные типы тестов требуют разных конфигураций
- Нужна возможность переключения между реальными и моковыми сервисами
- Упрощает настройку тестового окружения

**Что будет, если не решить**:
- Сложно настраивать тестовое окружение
- Невозможно легко переключаться между режимами

## 🎯 Ожидаемый результат

После выполнения задачи:

1. **Конфигурация для unit тестов** - отдельная конфигурация с моками
2. **Конфигурация для integration тестов** - конфигурация с тестовыми сервисами
3. **Конфигурация для E2E тестов** - конфигурация с реальными сервисами
4. **Возможность переключения** - легко переключаться между режимами
5. **Документация** - документация по использованию конфигураций

**Как проверить результат**:
- Можно легко переключаться между конфигурациями
- Каждый тип тестов использует свою конфигурацию
- Тесты работают с правильными конфигурациями

## 📝 Что нужно сделать

### Шаг 1: Создать структуру конфигураций

**Что сделать**:
1. Создать директорию для тестовых конфигураций
2. Определить структуру конфигураций
3. Создать базовую конфигурацию

**Где создать**:
- `tests/config/` - новая директория

### Шаг 2: Создать конфигурацию для unit тестов

**Что сделать**:
1. Создать конфигурацию с моками всех внешних зависимостей
2. Настроить DI контейнер для использования моков
3. Убедиться, что все зависимости мокируются

**Где создать**:
- `tests/config/unit.php` - конфигурация для unit тестов

### Шаг 3: Создать конфигурацию для integration тестов

**Что сделать**:
1. Создать конфигурацию с тестовыми сервисами
2. Настроить подключение к тестовым БД
3. Настроить тестовые API endpoints

**Где создать**:
- `tests/config/integration.php` - конфигурация для integration тестов

### Шаг 4: Создать конфигурацию для E2E тестов

**Что сделать**:
1. Создать конфигурацию с реальными сервисами
2. Настроить подключение к staging окружению
3. Убедиться, что все зависимости настроены

**Где создать**:
- `tests/config/e2e.php` - конфигурация для E2E тестов

### Шаг 5: Создать утилиту для загрузки конфигураций

**Что сделать**:
1. Создать класс для загрузки конфигураций
2. Предоставить методы для переключения между конфигурациями
3. Интегрировать с DI контейнером

**Где создать**:
- `tests/Helpers/TestConfigLoader.php`

### Шаг 6: Обновить базовые классы тестов

**Что сделать**:
1. Обновить базовые классы для использования конфигураций
2. Настроить автоматическую загрузку конфигураций
3. Убедиться, что каждый тип теста использует свою конфигурацию

**Где обновить**:
- `tests/TestCase/UnitTestCase.php`
- `tests/TestCase/IntegrationTestCase.php`
- `tests/TestCase/E2ETestCase.php`

### Шаг 7: Создать документацию

**Что сделать**:
1. Создать документацию по использованию конфигураций
2. Описать каждую конфигурацию
3. Привести примеры использования

**Где создать**:
- `tests/config/README.md`

### Шаг 8: Написать тесты

⚠️ **ОБЯЗАТЕЛЬНО**: После реализации сразу написать тесты!

**Что тестировать**:
- Конфигурации загружаются правильно
- Можно переключаться между конфигурациями
- Каждый тип теста использует свою конфигурацию

### Шаг 9: Добавить комментарии

⚠️ **ОБЯЗАТЕЛЬНО**: Добавить PHPDoc комментарии!

## 📂 Исходные файлы

- Существующие конфигурации в проекте
- Базовые классы тестов

## ✅ Критерии приемки

- [ ] Создана конфигурация для unit тестов
- [ ] Создана конфигурация для integration тестов
- [ ] Создана конфигурация для E2E тестов
- [ ] Создана утилита для загрузки конфигураций
- [ ] Базовые классы тестов обновлены
- [ ] Создана документация
- [ ] Тесты написаны и проходят
- [ ] Можно переключаться между конфигурациями
- [ ] Все классы и методы имеют PHPDoc комментарии

## 🔍 Как проверить

1. Попробовать запустить unit тесты с unit конфигурацией
2. Попробовать запустить integration тесты с integration конфигурацией
3. Проверить, что конфигурации загружаются правильно
4. Запустить все тесты: `composer test`

## 📚 Связанные задачи

- Предыдущая задача: [Задача 3.1: Рефакторинг для снижения связанности](./task-p3-1-reduce-coupling.md)
- Следующая задача: [Задача 3.3: Создать тестовые утилиты](./task-p3-3-test-utilities.md)

---

**ВАЖНО**: Если во время выполнения задачи возникли вопросы или что-то неизвестно - ОСТАНОВИТЬСЯ и ЗАПРОСИТЬ у пользователя, не придумывать!
