# Задача 1.1: Убрать синглтоны

## 📋 Информация о задаче

**Время выполнения**: 2-3 недели  
**Приоритет**: КРИТИЧЕСКИЙ (Приоритет 1)  
**Зависимости**: Нет (первая задача в плане)

## 🎯 Принципы работы

⚠️ **ВАЖНО**: При выполнении этой задачи следуйте принципам из [PRINCIPLES.md](../../../../PRINCIPLES.md):

1. ✅ **Сразу тестировать** - после каждого изменения написать тест
2. 🤖 **AI-friendly** - четкие имена, подробные комментарии, явные типы
3. 👤 **Human-friendly** - простота, один класс = одна цель
4. 💬 **Везде комментарии** - PHPDoc для всех классов и методов
5. 🧪 **Реальные данные** - если не знаешь, СПРОСИТЬ, не придумывать!

## ❌ Проблема

Проект использует паттерн Singleton для ключевых компонентов:
- `Logger::instance()` - синглтон для логирования
- `VariableCache::getInstance()` - синглтон для кэширования
- `DBConnector::getInstance()` - синглтон для подключения к БД

**Почему это критично**:
- Невозможно изолировать тесты друг от друга
- Состояние одного теста влияет на другие
- Невозможно запускать тесты параллельно
- Сложно мокировать зависимости
- Глобальное состояние делает код непредсказуемым

**Что будет, если не решить**:
- Невозможно создать надежные автоматические тесты
- Тесты будут нестабильными и зависимыми друг от друга
- Невозможно запускать тесты в CI/CD

## 🎯 Ожидаемый результат

После выполнения задачи:

1. **Logger** - инжектируется через конструктор, нет синглтона
2. **VariableCache** - инжектируется через конструктор, нет синглтона
3. **DBConnector** - инжектируется через конструктор, нет синглтона
4. **Все использования обновлены** - все места, где использовались синглтоны, обновлены
5. **Тесты проходят** - существующие тесты работают с новой архитектурой
6. **Созданы фабрики** - фабрики для создания объектов (если нужно)

**Как проверить результат**:
- Запустить все тесты: `composer test`
- Проверить, что нет вызовов `::instance()` или `::getInstance()`
- Проверить, что все зависимости инжектируются через конструктор

## 📝 Что нужно сделать

### Шаг 1: Изучить текущие синглтоны

⚠️ **НЕ ПРИДУМЫВАТЬ!** Изучить реальный код:

**Файлы для изучения**:
- `lib/MBMigration/Core/Logger.php` - изучить реализацию синглтона
- `lib/MBMigration/Builder/VariableCache.php` - изучить реализацию синглтона
- `lib/MBMigration/Layer/DataSource/DBConnector.php` - изучить реализацию синглтона

**Что изучить**:
- Как реализован синглтон
- Где используется `::instance()` или `::getInstance()`
- Какие зависимости есть у каждого класса

**Если что-то непонятно**:
- ❌ НЕ придумывать
- ✅ ЗАПРОСИТЬ у пользователя реальные данные/примеры

### Шаг 2: Найти все использования синглтонов

**Что сделать**:
1. Найти все вызовы `Logger::instance()` в проекте
2. Найти все вызовы `VariableCache::getInstance()` в проекте
3. Найти все вызовы `DBConnector::getInstance()` в проекте
4. Создать список всех мест использования

**Команды для поиска**:
```bash
grep -r "Logger::instance()" lib/
grep -r "VariableCache::getInstance()" lib/
grep -r "DBConnector::getInstance()" lib/
```

**Результат**: Список всех файлов и строк, где используются синглтоны

### Шаг 3: Рефакторинг Logger

**Что сделать**:
1. Убрать статический метод `instance()`
2. Убрать статическое свойство `$instance`
3. Сделать конструктор публичным
4. Обновить все места использования - инжектировать через конструктор
5. Создать фабрику `LoggerFactory` (если нужно)

**Файлы для работы**:
- `lib/MBMigration/Core/Logger.php` - рефакторинг класса
- Все файлы, где используется `Logger::instance()` - обновить

**Важно**:
- Сохранить обратную совместимость во время рефакторинга
- После каждого изменения запускать тесты
- Если тесты не проходят - исправить перед продолжением

### Шаг 4: Рефакторинг VariableCache

**Что сделать**:
1. Убрать статический метод `getInstance()`
2. Убрать статическое свойство `$instance`
3. Сделать конструктор публичным
4. Обновить все места использования - инжектировать через конструктор
5. Создать фабрику `VariableCacheFactory` (если нужно)

**Файлы для работы**:
- `lib/MBMigration/Builder/VariableCache.php` - рефакторинг класса
- Все файлы, где используется `VariableCache::getInstance()` - обновить

**Важно**:
- Сохранить обратную совместимость во время рефакторинга
- После каждого изменения запускать тесты

### Шаг 5: Рефакторинг DBConnector

**Что сделать**:
1. Убрать статический метод `getInstance()`
2. Убрать статическое свойство `$instance`
3. Сделать конструктор публичным
4. Обновить все места использования - инжектировать через конструктор
5. Создать фабрику `DBConnectorFactory` (если нужно)

**Файлы для работы**:
- `lib/MBMigration/Layer/DataSource/DBConnector.php` - рефакторинг класса
- Все файлы, где используется `DBConnector::getInstance()` - обновить

**Важно**:
- Сохранить обратную совместимость во время рефакторинга
- После каждого изменения запускать тесты

### Шаг 6: Создать фабрики (если нужно)

**Что сделать**:
1. Создать `LoggerFactory` для создания Logger
2. Создать `VariableCacheFactory` для создания VariableCache
3. Создать `DBConnectorFactory` для создания DBConnector

**Где создать**:
- `lib/MBMigration/Core/Factory/LoggerFactory.php`
- `lib/MBMigration/Builder/Factory/VariableCacheFactory.php`
- `lib/MBMigration/Layer/DataSource/Factory/DBConnectorFactory.php`

**Важно**:
- Фабрики должны быть простыми и понятными
- Фабрики должны принимать конфигурацию через конструктор

### Шаг 7: Написать тесты

⚠️ **ОБЯЗАТЕЛЬНО**: После реализации сразу написать тесты!

**Что тестировать**:
- Создание объектов через конструктор (не через синглтон)
- Инжекция зависимостей работает правильно
- Фабрики создают объекты правильно

**Где создать тесты**:
- `tests/MBMigration/Core/LoggerTest.php`
- `tests/MBMigration/Builder/VariableCacheTest.php`
- `tests/MBMigration/Layer/DataSource/DBConnectorTest.php`

**Какие данные использовать**:
- ❌ НЕ придумывать тестовые данные
- ✅ Использовать реальные конфигурации из проекта

### Шаг 8: Обновить все места использования

**Что сделать**:
1. Обновить `ApplicationBootstrapper` - инжектировать зависимости
2. Обновить `MigrationPlatform` - инжектировать зависимости
3. Обновить все другие классы, где используются синглтоны
4. Запустить все тесты и убедиться, что все работает

**Важно**:
- Обновлять постепенно, по одному классу
- После каждого обновления запускать тесты
- Если тесты не проходят - исправить перед продолжением

### Шаг 9: Добавить комментарии

⚠️ **ОБЯЗАТЕЛЬНО**: Добавить PHPDoc комментарии!

**Что комментировать**:
- Все публичные методы
- Конструкторы
- Фабрики

**Требования к комментариям**:
- Описание назначения
- Описание параметров
- Описание возвращаемого значения
- Описание исключений (если есть)

### Шаг 10: Финальная проверка

**Что сделать**:
1. Запустить все тесты: `composer test`
2. Проверить, что нет вызовов `::instance()` или `::getInstance()`
3. Проверить, что все зависимости инжектируются через конструктор
4. Проверить, что код компилируется без ошибок

**Если что-то не работает**:
- ❌ НЕ продолжать дальше
- ✅ Исправить проблему сначала

## 📂 Исходные файлы

- `lib/MBMigration/Core/Logger.php` - класс Logger с синглтоном
- `lib/MBMigration/Builder/VariableCache.php` - класс VariableCache с синглтоном
- `lib/MBMigration/Layer/DataSource/DBConnector.php` - класс DBConnector с синглтоном
- Все файлы, где используются эти синглтоны

## ✅ Критерии приемки

- [ ] Все синглтоны убраны (Logger, VariableCache, DBConnector)
- [ ] Все зависимости инжектируются через конструктор
- [ ] Все места использования обновлены
- [ ] Тесты написаны и проходят
- [ ] Все классы и методы имеют PHPDoc комментарии
- [ ] Код простой и понятный
- [ ] Использованы реальные данные (не придуманы)
- [ ] Все тесты запущены и проходят
- [ ] Нет вызовов `::instance()` или `::getInstance()`

## 🔍 Как проверить

1. Запустить поиск: `grep -r "::instance()" lib/` - должно быть пусто
2. Запустить поиск: `grep -r "::getInstance()" lib/` - должно быть пусто
3. Запустить тесты: `composer test` - все должны проходить
4. Проверить, что все конструкторы принимают зависимости

## ⚠️ Важные замечания

- **НЕ придумывать** - если не знаешь, СПРОСИТЬ
- **Сразу тестировать** - после каждого изменения
- **Комментировать все** - PHPDoc обязателен
- **Простота важна** - один класс = одна цель
- **Постепенный рефакторинг** - не ломать существующий функционал

## 🐛 Возможные проблемы

### Проблема: Много мест использования синглтонов
**Решение**: 
- Обновлять постепенно, по одному классу
- После каждого обновления запускать тесты
- Использовать поиск и замену с осторожностью

### Проблема: Тесты не проходят после рефакторинга
**Решение**: 
- Проверить, что все зависимости инжектируются правильно
- Проверить, что фабрики создают объекты правильно
- Исправить тесты, если они используют старый API

### Проблема: Не знаю, как создать фабрику
**Решение**: 
- ❌ НЕ придумывать
- ✅ ЗАПРОСИТЬ у пользователя пример или изучить существующие фабрики в проекте

## 📚 Связанные задачи

- Предыдущая задача: Нет (первая задача)
- Следующая задача: [Задача 1.2: Убрать статические свойства из Config](./task-p1-2-refactor-config.md)

## 📝 Заметки

[Место для заметок о выполнении задачи]

---

**ВАЖНО**: Если во время выполнения задачи возникли вопросы или что-то неизвестно - ОСТАНОВИТЬСЯ и ЗАПРОСИТЬ у пользователя, не придумывать!
