# Задача 1.1.1: Рефакторинг класса Logger

## 📋 Информация о задаче

**Время выполнения**: 1-2 дня  
**Приоритет**: КРИТИЧЕСКИЙ (Приоритет 1)  
**Зависимости**: Нет (первая подзадача задачи 1.1)  
**Родительская задача**: [Задача 1.1: Убрать синглтоны](./task-p1-1-remove-singletons.md)

## 🎯 Принципы работы

⚠️ **ВАЖНО**: При выполнении этой задачи следуйте принципам из [PRINCIPLES.md](../../../../PRINCIPLES.md):

1. ✅ **Сразу тестировать** - после каждого изменения написать тест
2. 🤖 **AI-friendly** - четкие имена, подробные комментарии, явные типы
3. 👤 **Human-friendly** - простота, один класс = одна цель
4. 💬 **Везде комментарии** - PHPDoc для всех классов и методов
5. 🧪 **Реальные данные** - если не знаешь, СПРОСИТЬ, не придумывать!

## ❌ Проблема

Класс `Logger` использует паттерн Singleton через статические методы `instance()` и `initialize()`, что делает невозможным:
- Создание нескольких экземпляров Logger с разными настройками
- Тестирование с моками Logger
- Изоляцию тестов друг от друга
- Параллельное выполнение тестов

**Почему это критично**:
- Невозможно изолировать тесты друг от друга
- Состояние одного теста влияет на другие
- Невозможно запускать тесты параллельно
- Сложно мокировать зависимости

**Что будет, если не решить**:
- Невозможно создать надежные автоматические тесты
- Тесты будут нестабильными и зависимыми друг от друга

## 🎯 Ожидаемый результат

После выполнения задачи:

1. **Logger имеет публичный конструктор** - можно создавать экземпляры через `new Logger()`
2. **Создан LoggerFactory** - фабрика для удобного создания Logger
3. **Статические методы помечены как deprecated** - сохранены для обратной совместимости
4. **Все методы имеют PHPDoc** - полная документация класса
5. **Написаны тесты** - тесты для нового API Logger

**Как проверить результат**:
- Можно создать Logger через конструктор: `new Logger('name', 'info', '/path/to/log')`
- Можно создать Logger через фабрику: `LoggerFactory::create('name', 'info', '/path/to/log')`
- Старые методы `instance()` и `initialize()` все еще работают (deprecated)

## 📝 Что нужно сделать

### Шаг 1: Изучить текущий Logger

⚠️ **НЕ ПРИДУМЫВАТЬ!** Изучить реальный код:

**Файлы для изучения**:
- `lib/MBMigration/Core/Logger.php` - изучить текущую реализацию

**Что изучить**:
- Как реализован синглтон (статические методы и свойства)
- Какие параметры принимает `initialize()`
- Как используется Logger в других классах

### Шаг 2: Рефакторинг Logger

**Что сделать**:
1. Добавить публичный конструктор, принимающий `$name`, `$logLevel`, `$path`
2. Сохранить статические методы `instance()` и `initialize()` для обратной совместимости, но пометить как `@deprecated`
3. Убрать статическое свойство `$instance` или оставить для обратной совместимости
4. Добавить PHPDoc комментарии для всех методов

**Файлы для работы**:
- `lib/MBMigration/Core/Logger.php` - рефакторинг класса

**Важно**:
- Сохранить обратную совместимость - старые методы должны работать
- После каждого изменения запускать тесты

### Шаг 3: Создать LoggerFactory

**Что сделать**:
1. Создать класс `LoggerFactory` в `lib/MBMigration/Core/Factory/LoggerFactory.php`
2. Добавить статический метод `create()` для создания Logger
3. Добавить статический метод `createDefault()` для создания Logger с настройками по умолчанию
4. Добавить PHPDoc комментарии

**Где создать**:
- `lib/MBMigration/Core/Factory/LoggerFactory.php`

**Важно**:
- Фабрика должна быть простой и понятной
- Фабрика должна принимать конфигурацию через параметры

### Шаг 4: Написать тесты

⚠️ **ОБЯЗАТЕЛЬНО**: После реализации сразу написать тесты!

**Что тестировать**:
- Создание Logger через конструктор
- Создание Logger через LoggerFactory
- Старые методы `instance()` и `initialize()` все еще работают
- Разные экземпляры Logger не влияют друг на друга

**Где создать тесты**:
- `tests/MBMigration/Core/LoggerTest.php`

**Какие данные использовать**:
- ❌ НЕ придумывать тестовые данные
- ✅ Использовать реальные пути к логам из проекта (например, `./test.log`)

### Шаг 5: Добавить комментарии

⚠️ **ОБЯЗАТЕЛЬНО**: Добавить PHPDoc комментарии!

**Что комментировать**:
- Класс Logger - описание назначения
- Конструктор - параметры и их назначение
- Статические методы - пометить как deprecated с объяснением альтернативы
- LoggerFactory - описание методов

**Требования к комментариям**:
- Описание назначения
- Описание параметров
- Описание возвращаемого значения
- Описание исключений (если есть)
- Для deprecated методов - указать альтернативу

### Шаг 6: Запустить тесты

⚠️ **ОБЯЗАТЕЛЬНО**: Запустить тесты и убедиться, что все проходит!

**Команда**: `composer test`

**Если тесты не проходят**:
- ❌ НЕ продолжать дальше
- ✅ Исправить проблему сначала

## 📂 Исходные файлы

- `lib/MBMigration/Core/Logger.php` - класс Logger с синглтоном

## ✅ Критерии приемки

- [ ] Logger имеет публичный конструктор
- [ ] Создан LoggerFactory
- [ ] Статические методы помечены как deprecated, но работают
- [ ] Все методы имеют PHPDoc комментарии
- [ ] Написаны тесты для нового API
- [ ] Все тесты проходят
- [ ] Код простой и понятный
- [ ] Использованы реальные данные (не придуманы)
- [ ] Файлы добавлены в git

## 🔍 Как проверить

1. Создать Logger через конструктор: `$logger = new Logger('test', 'info', './test.log')`
2. Создать Logger через фабрику: `$logger = LoggerFactory::create('test', 'info', './test.log')`
3. Проверить, что старые методы работают: `Logger::initialize('test', 'info', './test.log')`
4. Запустить тесты: `composer test`

## ⚠️ Важные замечания

- **НЕ придумывать** - если не знаешь, СПРОСИТЬ
- **Сразу тестировать** - после каждого изменения
- **Комментировать все** - PHPDoc обязателен
- **Простота важна** - один класс = одна цель
- **Обратная совместимость** - старые методы должны работать

## 🐛 Возможные проблемы

### Проблема: Не знаю, какие параметры нужны для конструктора
**Решение**: 
- ❌ НЕ придумывать
- ✅ Изучить метод `initialize()` и использовать те же параметры

### Проблема: Тесты не проходят после рефакторинга
**Решение**: 
- Проверить, что конструктор правильно создает handler
- Проверить, что статические методы все еще работают
- Исправить тесты, если они используют старый API

## 📚 Связанные задачи

- Родительская задача: [Задача 1.1: Убрать синглтоны](./task-p1-1-remove-singletons.md)
- Следующая задача: [Задача 1.1.2: Обновить ApplicationBootstrapper для использования LoggerFactory](./task-p1-1-2-update-application-bootstrapper.md)

## 📝 Заметки

[Место для заметок о выполнении задачи]

---

**ВАЖНО**: Если во время выполнения задачи возникли вопросы или что-то неизвестно - ОСТАНОВИТЬСЯ и ЗАПРОСИТЬ у пользователя, не придумывать!
