# Задача 1.2: Убрать статические свойства из Config

## 📋 Информация о задаче

**Время выполнения**: 1-2 недели  
**Приоритет**: КРИТИЧЕСКИЙ (Приоритет 1)  
**Зависимости**: [Задача 1.1: Убрать синглтоны](./task-p1-1-remove-singletons.md)

## 🎯 Принципы работы

⚠️ **ВАЖНО**: При выполнении этой задачи следуйте принципам из [PRINCIPLES.md](../../../../PRINCIPLES.md)

## ❌ Проблема

Класс `Config` использует 20+ статических свойств, которые устанавливаются один раз и используются везде:
- `Config::$mainToken`
- `Config::$urlAPI`
- `Config::$devMode`
- `Config::$cachePath`
- И еще 17+ статических свойств

**Почему это критично**:
- Невозможно изменить конфигурацию для тестов
- Состояние конфигурации сохраняется между тестами
- Невозможно тестировать с разными конфигурациями
- Глобальное состояние влияет на все тесты
- Невозможно мокировать Config

**Что будет, если не решить**:
- Невозможно создать изолированные тесты
- Тесты будут зависеть от глобального состояния
- Невозможно тестировать с разными конфигурациями

## 🎯 Ожидаемый результат

После выполнения задачи:

1. **Config** - обычный класс с инстансами, нет статических свойств
2. **ConfigInterface** - создан интерфейс для возможности мокирования
3. **Все использования обновлены** - все места, где использовались статические свойства, обновлены
4. **Тесты проходят** - существующие тесты работают с новой архитектурой
5. **Инжекция через конструктор** - Config инжектируется через конструктор везде

**Как проверить результат**:
- Запустить все тесты: `composer test`
- Проверить, что нет обращений к `Config::$property`
- Проверить, что Config инжектируется через конструктор

## 📝 Что нужно сделать

### Шаг 1: Изучить текущий Config

⚠️ **НЕ ПРИДУМЫВАТЬ!** Изучить реальный код:

**Файлы для изучения**:
- `lib/MBMigration/Core/Config.php` - изучить все статические свойства

**Что изучить**:
- Какие статические свойства есть
- Где они используются
- Как они устанавливаются
- Какие зависимости есть у Config

### Шаг 2: Найти все использования статических свойств

**Что сделать**:
1. Найти все обращения к `Config::$property` в проекте
2. Создать список всех мест использования
3. Сгруппировать по типам использования

**Команда для поиска**:
```bash
grep -r "Config::\$" lib/
```

### Шаг 3: Создать ConfigInterface

**Что сделать**:
1. Создать интерфейс `ConfigInterface`
2. Определить все методы для доступа к свойствам
3. Убедиться, что интерфейс покрывает все статические свойства

**Где создать**:
- `lib/MBMigration/Contracts/ConfigInterface.php`

### Шаг 4: Рефакторинг Config

**Что сделать**:
1. Преобразовать все статические свойства в обычные свойства
2. Создать геттеры для всех свойств
3. Реализовать `ConfigInterface`
4. Обновить конструктор для установки всех свойств

**Файлы для работы**:
- `lib/MBMigration/Core/Config.php` - рефакторинг класса

**Важно**:
- Сохранить обратную совместимость во время рефакторинга
- После каждого изменения запускать тесты

### Шаг 5: Обновить все места использования

**Что сделать**:
1. Заменить `Config::$property` на `$config->getProperty()`
2. Инжектировать Config через конструктор
3. Обновить все классы, которые используют Config

**Важно**:
- Обновлять постепенно, по одному классу
- После каждого обновления запускать тесты

### Шаг 6: Написать тесты

⚠️ **ОБЯЗАТЕЛЬНО**: После реализации сразу написать тесты!

**Что тестировать**:
- Создание Config с разными конфигурациями
- Геттеры возвращают правильные значения
- Можно создать мок ConfigInterface

**Где создать тесты**:
- `tests/MBMigration/Core/ConfigTest.php`

### Шаг 7: Добавить комментарии

⚠️ **ОБЯЗАТЕЛЬНО**: Добавить PHPDoc комментарии!

## 📂 Исходные файлы

- `lib/MBMigration/Core/Config.php` - класс Config со статическими свойствами
- Все файлы, где используются статические свойства Config

## ✅ Критерии приемки

- [ ] Все статические свойства преобразованы в обычные
- [ ] Создан ConfigInterface
- [ ] Все места использования обновлены
- [ ] Config инжектируется через конструктор
- [ ] Тесты написаны и проходят
- [ ] Все классы и методы имеют PHPDoc комментарии
- [ ] Нет обращений к `Config::$property`

## 🔍 Как проверить

1. Запустить поиск: `grep -r "Config::\$" lib/` - должно быть пусто
2. Запустить тесты: `composer test` - все должны проходить
3. Проверить, что Config инжектируется через конструктор

## 📚 Связанные задачи

- Предыдущая задача: [Задача 1.1: Убрать синглтоны](./task-p1-1-remove-singletons.md)
- Следующая задача: [Задача 1.3: Создать интерфейсы для ключевых компонентов](./task-p1-3-create-interfaces.md)

---

**ВАЖНО**: Если во время выполнения задачи возникли вопросы или что-то неизвестно - ОСТАНОВИТЬСЯ и ЗАПРОСИТЬ у пользователя, не придумывать!
