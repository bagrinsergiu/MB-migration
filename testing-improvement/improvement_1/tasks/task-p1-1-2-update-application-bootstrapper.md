# Задача 1.1.2: Обновить ApplicationBootstrapper для использования LoggerFactory

## 📋 Информация о задаче

**Время выполнения**: 1 день  
**Приоритет**: КРИТИЧЕСКИЙ (Приоритет 1)  
**Зависимости**: [Задача 1.1.1: Рефакторинг класса Logger](./task-p1-1-1-refactor-logger-class.md)  
**Родительская задача**: [Задача 1.1: Убрать синглтоны](./task-p1-1-remove-singletons.md)

## 🎯 Принципы работы

⚠️ **ВАЖНО**: При выполнении этой задачи следуйте принципам из [PRINCIPLES.md](../../../../PRINCIPLES.md)

## ❌ Проблема

`ApplicationBootstrapper` использует `Logger::initialize()` и `Logger::instance()` (16 использований), что создает глобальное состояние и делает невозможным тестирование.

**Почему это важно**:
- ApplicationBootstrapper - точка входа приложения
- Невозможно тестировать ApplicationBootstrapper изолированно
- Невозможно мокировать Logger в тестах

**Что будет, если не решить**:
- Невозможно создать unit тесты для ApplicationBootstrapper
- Тесты будут зависеть от глобального состояния Logger

## 🎯 Ожидаемый результат

После выполнения задачи:

1. **ApplicationBootstrapper использует LoggerFactory** - создает Logger через фабрику
2. **Logger инжектируется в MigrationPlatform** - передается через конструктор
3. **Все использования Logger::instance() заменены** - на использование инжектированного Logger
4. **Тесты написаны** - тесты для ApplicationBootstrapper с моками Logger

**Как проверить результат**:
- ApplicationBootstrapper создает Logger через LoggerFactory
- MigrationPlatform получает Logger через конструктор
- Нет вызовов `Logger::instance()` в ApplicationBootstrapper

## 📝 Что нужно сделать

### Шаг 1: Изучить ApplicationBootstrapper

⚠️ **НЕ ПРИДУМЫВАТЬ!** Изучить реальный код:

**Файлы для изучения**:
- `lib/MBMigration/ApplicationBootstrapper.php` - изучить все использования Logger

**Что изучить**:
- Где используется `Logger::initialize()`
- Где используется `Logger::instance()`
- Как создается MigrationPlatform
- Какие параметры передаются в Logger

### Шаг 2: Обновить создание Logger

**Что сделать**:
1. Заменить `Logger::initialize()` на `LoggerFactory::create()`
2. Сохранить созданный Logger в свойство класса
3. Передавать Logger в MigrationPlatform через конструктор

**Файлы для работы**:
- `lib/MBMigration/ApplicationBootstrapper.php` - обновить создание Logger

**Важно**:
- Сохранить все параметры, которые передавались в `Logger::initialize()`
- После каждого изменения запускать тесты

### Шаг 3: Заменить все Logger::instance()

**Что сделать**:
1. Найти все вызовы `Logger::instance()` в ApplicationBootstrapper
2. Заменить на использование свойства класса `$this->logger`
3. Убедиться, что Logger доступен во всех методах

**Файлы для работы**:
- `lib/MBMigration/ApplicationBootstrapper.php` - заменить все использования

**Важно**:
- Обновлять постепенно, по одному методу
- После каждого обновления запускать тесты

### Шаг 4: Обновить передачу Logger в MigrationPlatform

**Что сделать**:
1. Проверить, что MigrationPlatform уже принимает Logger через конструктор
2. Передать созданный Logger в конструктор MigrationPlatform
3. Убедиться, что все работает

**Файлы для работы**:
- `lib/MBMigration/ApplicationBootstrapper.php` - обновить создание MigrationPlatform

### Шаг 5: Написать тесты

⚠️ **ОБЯЗАТЕЛЬНО**: После реализации сразу написать тесты!

**Что тестировать**:
- ApplicationBootstrapper создает Logger через LoggerFactory
- ApplicationBootstrapper передает Logger в MigrationPlatform
- Можно мокировать Logger в тестах

**Где создать тесты**:
- `tests/MBMigration/ApplicationBootstrapperTest.php`

**Какие данные использовать**:
- ❌ НЕ придумывать тестовые данные
- ✅ Использовать реальные конфигурации из проекта

### Шаг 6: Добавить комментарии

⚠️ **ОБЯЗАТЕЛЬНО**: Добавить PHPDoc комментарии!

**Что комментировать**:
- Свойство `$logger` - описание назначения
- Методы, использующие Logger - описание использования

### Шаг 7: Запустить тесты

⚠️ **ОБЯЗАТЕЛЬНО**: Запустить тесты и убедиться, что все проходит!

**Команда**: `composer test`

**Если тесты не проходят**:
- ❌ НЕ продолжать дальше
- ✅ Исправить проблему сначала

## 📂 Исходные файлы

- `lib/MBMigration/ApplicationBootstrapper.php` - класс ApplicationBootstrapper

## ✅ Критерии приемки

- [ ] ApplicationBootstrapper использует LoggerFactory
- [ ] Все использования Logger::instance() заменены на $this->logger
- [ ] Logger передается в MigrationPlatform через конструктор
- [ ] Тесты написаны и проходят
- [ ] Все методы имеют PHPDoc комментарии
- [ ] Нет вызовов Logger::instance() в ApplicationBootstrapper
- [ ] Файлы добавлены в git

## 🔍 Как проверить

1. Проверить, что нет вызовов `Logger::instance()` в ApplicationBootstrapper: `grep -r "Logger::instance()" lib/MBMigration/ApplicationBootstrapper.php`
2. Проверить, что Logger создается через LoggerFactory
3. Запустить тесты: `composer test`

## ⚠️ Важные замечания

- **НЕ придумывать** - если не знаешь, СПРОСИТЬ
- **Сразу тестировать** - после каждого изменения
- **Комментировать все** - PHPDoc обязателен

## 🐛 Возможные проблемы

### Проблема: MigrationPlatform не принимает Logger через конструктор
**Решение**: 
- ❌ НЕ придумывать
- ✅ Изучить конструктор MigrationPlatform и обновить его, если нужно

## 📚 Связанные задачи

- Предыдущая задача: [Задача 1.1.1: Рефакторинг класса Logger](./task-p1-1-1-refactor-logger-class.md)
- Родительская задача: [Задача 1.1: Убрать синглтоны](./task-p1-1-remove-singletons.md)
- Следующая задача: [Задача 1.1.3: Обновить MigrationPlatform для использования инжектированного Logger](./task-p1-1-3-update-migration-platform.md)

---

**ВАЖНО**: Если во время выполнения задачи возникли вопросы или что-то неизвестно - ОСТАНОВИТЬСЯ и ЗАПРОСИТЬ у пользователя, не придумывать!
