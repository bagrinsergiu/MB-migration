# Задача 1.1.7: Финальная проверка и тесты

## 📋 Информация о задаче

**Время выполнения**: 2-3 дня  
**Приоритет**: КРИТИЧЕСКИЙ (Приоритет 1)  
**Зависимости**: [Задача 1.1.6: Рефакторинг DBConnector](./task-p1-1-6-refactor-dbconnector.md)  
**Родительская задача**: [Задача 1.1: Убрать синглтоны](./task-p1-1-remove-singletons.md)

## 🎯 Принципы работы

⚠️ **ВАЖНО**: При выполнении этой задачи следуйте принципам из [PRINCIPLES.md](../../../../PRINCIPLES.md)

## ❌ Проблема

После рефакторинга всех синглтонов необходимо:
- Убедиться, что все синглтоны убраны
- Написать комплексные тесты
- Проверить, что все тесты проходят
- Убедиться, что нет регрессий

**Почему это важно**:
- Нужно убедиться, что рефакторинг не сломал функциональность
- Нужны тесты для проверки новой архитектуры
- Нужна уверенность, что все работает правильно

**Что будет, если не решить**:
- Могут остаться скрытые проблемы
- Нет уверенности в правильности рефакторинга
- Невозможно гарантировать, что все работает

## 🎯 Ожидаемый результат

После выполнения задачи:

1. **Все синглтоны убраны** - нет вызовов `::instance()` или `::getInstance()`
2. **Все тесты написаны** - тесты для всех рефакторированных классов
3. **Все тесты проходят** - нет падающих тестов
4. **Нет регрессий** - существующий функционал работает
5. **Документация обновлена** - все изменения задокументированы

**Как проверить результат**:
- Запустить поиск: `grep -r "::instance()" lib/` - должно быть пусто
- Запустить поиск: `grep -r "::getInstance()" lib/` - должно быть пусто
- Запустить тесты: `composer test` - все должны проходить

## 📝 Что нужно сделать

### Шаг 1: Проверить отсутствие синглтонов

**Что сделать**:
1. Запустить поиск всех вызовов `Logger::instance()`
2. Запустить поиск всех вызовов `VariableCache::getInstance()`
3. Запустить поиск всех вызовов `DBConnector::getInstance()`
4. Убедиться, что все найдено и исправлено

**Команды для проверки**:
```bash
grep -r "Logger::instance()" lib/
grep -r "VariableCache::getInstance()" lib/
grep -r "DBConnector::getInstance()" lib/
```

**Если найдены использования**:
- Обновить их или пометить как deprecated с объяснением

### Шаг 2: Написать интеграционные тесты

**Что сделать**:
1. Создать тесты для проверки работы всей цепочки:
   - ApplicationBootstrapper → MigrationPlatform → другие классы
2. Проверить, что все зависимости инжектируются правильно
3. Проверить, что можно мокировать все зависимости

**Где создать тесты**:
- `tests/MBMigration/Integration/SingletonRemovalTest.php`

**Что тестировать**:
- Создание ApplicationBootstrapper с LoggerFactory
- Создание MigrationPlatform с инжектированными зависимостями
- Работа всех классов без синглтонов

### Шаг 3: Написать unit тесты для фабрик

**Что сделать**:
1. Создать тесты для LoggerFactory
2. Создать тесты для VariableCacheFactory
3. Создать тесты для DBConnectorFactory

**Где создать тесты**:
- `tests/MBMigration/Core/Factory/LoggerFactoryTest.php`
- `tests/MBMigration/Builder/Factory/VariableCacheFactoryTest.php`
- `tests/MBMigration/Layer/DataSource/Factory/DBConnectorFactoryTest.php`

**Что тестировать**:
- Фабрики создают правильные экземпляры
- Фабрики принимают правильные параметры
- Разные экземпляры не влияют друг на друга

### Шаг 4: Проверить все существующие тесты

**Что сделать**:
1. Запустить все существующие тесты
2. Убедиться, что они все проходят
3. Обновить тесты, если они используют старый API

**Команда**: `composer test`

**Если тесты не проходят**:
- Исправить тесты для использования нового API
- Убедиться, что функциональность не изменилась

### Шаг 5: Проверить обратную совместимость

**Что сделать**:
1. Проверить, что deprecated методы все еще работают
2. Убедиться, что старый код может работать (если нужно)
3. Документировать процесс миграции

**Важно**:
- Deprecated методы должны работать, но выдавать предупреждения
- Документировать, как мигрировать на новый API

### Шаг 6: Обновить документацию

**Что сделать**:
1. Обновить README или документацию с описанием изменений
2. Добавить примеры использования нового API
3. Добавить руководство по миграции

**Где обновить**:
- `testing-improvement/improvement_1/README.md` - добавить информацию о рефакторинге
- Создать `MIGRATION_GUIDE.md` - руководство по миграции

### Шаг 7: Финальная проверка

**Что сделать**:
1. Запустить все тесты: `composer test`
2. Проверить, что нет вызовов синглтонов
3. Проверить, что код компилируется без ошибок
4. Проверить линтер: `composer lint` (если есть)

**Команды**:
```bash
composer test
grep -r "::instance()" lib/
grep -r "::getInstance()" lib/
```

### Шаг 8: Обновить PROGRESS.md

**Что сделать**:
1. Отметить все шаги как выполненные
2. Обновить чендж лог
3. Добавить статистику выполненной работы

## 📂 Исходные файлы

- Все рефакторированные классы
- Все тесты
- Документация

## ✅ Критерии приемки

- [ ] Нет вызовов `::instance()` или `::getInstance()` в проекте
- [ ] Все тесты написаны и проходят
- [ ] Интеграционные тесты написаны
- [ ] Unit тесты для фабрик написаны
- [ ] Все существующие тесты проходят
- [ ] Обратная совместимость проверена
- [ ] Документация обновлена
- [ ] PROGRESS.md обновлен
- [ ] Файлы добавлены в git

## 🔍 Как проверить

1. Запустить поиск: `grep -r "::instance()" lib/` - должно быть пусто
2. Запустить поиск: `grep -r "::getInstance()" lib/` - должно быть пусто
3. Запустить тесты: `composer test` - все должны проходить
4. Проверить линтер (если есть)

## ⚠️ Важные замечания

- **НЕ придумывать** - если не знаешь, СПРОСИТЬ
- **Сразу тестировать** - после каждого изменения
- **Комментировать все** - PHPDoc обязателен
- **Проверять все** - не пропускать шаги проверки

## 🐛 Возможные проблемы

### Проблема: Некоторые тесты не проходят
**Решение**: 
- Изучить ошибки тестов
- Исправить тесты для использования нового API
- Убедиться, что функциональность не изменилась

### Проблема: Найдены вызовы синглтонов
**Решение**: 
- Обновить найденные места
- Или пометить как deprecated с объяснением, почему оставлено

## 📚 Связанные задачи

- Предыдущая задача: [Задача 1.1.6: Рефакторинг DBConnector](./task-p1-1-6-refactor-dbconnector.md)
- Родительская задача: [Задача 1.1: Убрать синглтоны](./task-p1-1-remove-singletons.md)
- Следующая задача: [Задача 1.2: Убрать статические свойства из Config](./task-p1-2-refactor-config.md)

---

**ВАЖНО**: Если во время выполнения задачи возникли вопросы или что-то неизвестно - ОСТАНОВИТЬСЯ и ЗАПРОСИТЬ у пользователя, не придумывать!
