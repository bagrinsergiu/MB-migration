# Задача 1.1.5: Рефакторинг VariableCache

## 📋 Информация о задаче

**Время выполнения**: 2-3 дня  
**Приоритет**: КРИТИЧЕСКИЙ (Приоритет 1)  
**Зависимости**: [Задача 1.1.4: Обновить другие ключевые классы](./task-p1-1-4-update-other-classes-logger.md)  
**Родительская задача**: [Задача 1.1: Убрать синглтоны](./task-p1-1-remove-singletons.md)

## 🎯 Принципы работы

⚠️ **ВАЖНО**: При выполнении этой задачи следуйте принципам из [PRINCIPLES.md](../../../../PRINCIPLES.md)

## ❌ Проблема

Класс `VariableCache` использует паттерн Singleton через статический метод `getInstance()`, что делает невозможным:
- Создание нескольких экземпляров VariableCache с разными путями
- Тестирование с моками VariableCache
- Изоляцию тестов друг от друга

**Почему это критично**:
- VariableCache используется в 22 местах
- Невозможно изолировать тесты друг от друга
- Состояние одного теста влияет на другие

**Что будет, если не решить**:
- Невозможно создать надежные автоматические тесты
- Тесты будут нестабильными

## 🎯 Ожидаемый результат

После выполнения задачи:

1. **VariableCache имеет публичный конструктор** - можно создавать экземпляры через `new VariableCache()`
2. **Создан VariableCacheFactory** - фабрика для удобного создания VariableCache
3. **Статический метод getInstance() помечен как deprecated** - сохранен для обратной совместимости
4. **Все использования обновлены** - все 22 места используют DI или фабрику
5. **Тесты написаны** - тесты для нового API VariableCache

**Как проверить результат**:
- Можно создать VariableCache через конструктор
- Можно создать VariableCache через фабрику
- Нет вызовов `VariableCache::getInstance()` в проекте

## 📝 Что нужно сделать

### Шаг 1: Изучить текущий VariableCache

⚠️ **НЕ ПРИДУМЫВАТЬ!** Изучить реальный код:

**Файлы для изучения**:
- `lib/MBMigration/Builder/VariableCache.php` - изучить текущую реализацию

**Что изучить**:
- Как реализован синглтон (статический метод и свойство)
- Какие параметры принимает конструктор
- Как используется VariableCache в других классах
- Особенность: используется массив для поддержки подклассов

### Шаг 2: Рефакторинг VariableCache

**Что сделать**:
1. Сделать конструктор публичным
2. Сохранить статический метод `getInstance()` для обратной совместимости, но пометить как `@deprecated`
3. Убрать статическое свойство `$instance` или оставить для обратной совместимости
4. Добавить PHPDoc комментарии для всех методов

**Файлы для работы**:
- `lib/MBMigration/Builder/VariableCache.php` - рефакторинг класса

**Важно**:
- Сохранить обратную совместимость - старый метод должен работать
- После каждого изменения запускать тесты

### Шаг 3: Создать VariableCacheFactory

**Что сделать**:
1. Создать класс `VariableCacheFactory` в `lib/MBMigration/Builder/Factory/VariableCacheFactory.php`
2. Добавить статический метод `create()` для создания VariableCache
3. Добавить PHPDoc комментарии

**Где создать**:
- `lib/MBMigration/Builder/Factory/VariableCacheFactory.php`

**Важно**:
- Фабрика должна быть простой и понятной
- Фабрика должна принимать cachePath через параметр

### Шаг 4: Найти все использования VariableCache::getInstance()

**Что сделать**:
1. Найти все 22 места использования `VariableCache::getInstance()`
2. Создать список всех файлов и строк
3. Определить приоритет обновления

**Команда для поиска**:
```bash
grep -r "VariableCache::getInstance()" lib/
```

### Шаг 5: Обновить все использования

**Что сделать**:
1. Обновить каждое место использования:
   - Если класс уже имеет DI - добавить VariableCache в конструктор
   - Если класс создается через new - использовать фабрику
2. Обновлять постепенно, по одному классу
3. После каждого обновления запускать тесты

**Основные классы для обновления**:
- `PageController` - 2 использования
- `BrizyAPI` - 1 использование
- `MBProjectDataCollector` - 1 использование
- `MenuHandler` - 2 использования
- `MediaController` - 3 использования
- `FontsController` - 5 использований
- `QueryBuilder` - 1 использование
- И другие

**Важно**:
- Обновлять постепенно, по одному классу
- После каждого обновления запускать тесты

### Шаг 6: Написать тесты

⚠️ **ОБЯЗАТЕЛЬНО**: После реализации сразу написать тесты!

**Что тестировать**:
- Создание VariableCache через конструктор
- Создание VariableCache через VariableCacheFactory
- Разные экземпляры VariableCache не влияют друг на друга
- Все методы VariableCache работают правильно

**Где создать тесты**:
- `tests/MBMigration/Builder/VariableCacheTest.php`

**Какие данные использовать**:
- ❌ НЕ придумывать тестовые данные
- ✅ Использовать реальные пути к кэшу из проекта

### Шаг 7: Добавить комментарии

⚠️ **ОБЯЗАТЕЛЬНО**: Добавить PHPDoc комментарии!

**Что комментировать**:
- Класс VariableCache - описание назначения
- Конструктор - параметры и их назначение
- Статический метод getInstance() - пометить как deprecated
- VariableCacheFactory - описание методов

### Шаг 8: Запустить тесты

⚠️ **ОБЯЗАТЕЛЬНО**: Запустить тесты и убедиться, что все проходит!

**Команда**: `composer test`

**Если тесты не проходят**:
- ❌ НЕ продолжать дальше
- ✅ Исправить проблему сначала

## 📂 Исходные файлы

- `lib/MBMigration/Builder/VariableCache.php` - класс VariableCache с синглтоном
- Все файлы, где используется `VariableCache::getInstance()` (22 места)

## ✅ Критерии приемки

- [ ] VariableCache имеет публичный конструктор
- [ ] Создан VariableCacheFactory
- [ ] Статический метод getInstance() помечен как deprecated, но работает
- [ ] Все 22 использования обновлены
- [ ] Нет вызовов VariableCache::getInstance() в проекте
- [ ] Тесты написаны и проходят
- [ ] Все методы имеют PHPDoc комментарии
- [ ] Файлы добавлены в git

## 🔍 Как проверить

1. Проверить, что нет вызовов `VariableCache::getInstance()`: `grep -r "VariableCache::getInstance()" lib/`
2. Запустить тесты: `composer test`
3. Проверить, что можно создать несколько экземпляров VariableCache

## ⚠️ Важные замечания

- **НЕ придумывать** - если не знаешь, СПРОСИТЬ
- **Сразу тестировать** - после каждого изменения
- **Комментировать все** - PHPDoc обязателен
- **Обновлять постепенно** - по одному классу

## 🐛 Возможные проблемы

### Проблема: VariableCache использует массив для поддержки подклассов
**Решение**: 
- Изучить реальную реализацию
- Сохранить эту логику, но сделать через фабрику или DI
- Если непонятно - ЗАПРОСИТЬ у пользователя

### Проблема: Много мест использования (22)
**Решение**: 
- Обновлять постепенно, по одному классу
- После каждого обновления запускать тесты
- Использовать поиск для отслеживания прогресса

## 📚 Связанные задачи

- Предыдущая задача: [Задача 1.1.4: Обновить другие ключевые классы](./task-p1-1-4-update-other-classes-logger.md)
- Родительская задача: [Задача 1.1: Убрать синглтоны](./task-p1-1-remove-singletons.md)
- Следующая задача: [Задача 1.1.6: Рефакторинг DBConnector](./task-p1-1-6-refactor-dbconnector.md)

---

**ВАЖНО**: Если во время выполнения задачи возникли вопросы или что-то неизвестно - ОСТАНОВИТЬСЯ и ЗАПРОСИТЬ у пользователя, не придумывать!
