# Задача 1.3: Создать интерфейсы для ключевых компонентов

## 📋 Информация о задаче

**Время выполнения**: 2-3 недели  
**Приоритет**: КРИТИЧЕСКИЙ (Приоритет 1)  
**Зависимости**: [Задача 1.2: Убрать статические свойства из Config](./task-p1-2-refactor-config.md)

## 🎯 Принципы работы

⚠️ **ВАЖНО**: При выполнении этой задачи следуйте принципам из [PRINCIPLES.md](../../../../PRINCIPLES.md)

## ❌ Проблема

Многие классы не имеют интерфейсов, что делает невозможным создание моков:

**Отсутствующие интерфейсы**:
- `VariableCache` - нет интерфейса
- `DBConnector` - нет интерфейса (частично есть DatabaseInterface, нужно расширить)
- `QueryBuilder` - нет интерфейса
- `FontsController` - нет интерфейса
- Множество классов в `Builder/Layout` - нет интерфейсов

**Почему это критично**:
- Невозможно создать моки для большинства компонентов
- Тесты вынуждены использовать реальные реализации
- Высокая связанность с внешними зависимостями
- Невозможно тестировать компоненты изолированно

**Что будет, если не решить**:
- Невозможно создать unit тесты
- Тесты будут медленными и нестабильными
- Невозможно тестировать сценарии ошибок

## 🎯 Ожидаемый результат

После выполнения задачи:

1. **CacheInterface** - создан для VariableCache
2. **DatabaseInterface** - расширен для DBConnector
3. **QueryBuilderInterface** - создан для QueryBuilder
4. **FontsControllerInterface** - создан для FontsController
5. **Интерфейсы для Builder/Layout** - созданы для ключевых классов
6. **Все реализации обновлены** - все классы реализуют интерфейсы
7. **Можно создавать моки** - можно мокировать все интерфейсы

**Как проверить результат**:
- Можно создать моки для всех интерфейсов
- Все классы реализуют соответствующие интерфейсы
- Тесты используют интерфейсы, а не конкретные классы

## 📝 Что нужно сделать

### Шаг 1: Определить ключевые компоненты

**Что сделать**:
1. Составить список всех классов, которые нужно абстрагировать
2. Определить приоритет (какие интерфейсы нужны в первую очередь)
3. Изучить каждый класс и определить его публичный API

**Ключевые компоненты**:
- `VariableCache` - кэширование
- `DBConnector` - подключение к БД
- `QueryBuilder` - построение GraphQL запросов
- `FontsController` - управление шрифтами
- Классы в `Builder/Layout` - построение элементов

### Шаг 2: Создать CacheInterface

**Что сделать**:
1. Изучить `VariableCache` и определить все публичные методы
2. Создать интерфейс `CacheInterface` с этими методами
3. Обновить `VariableCache` для реализации интерфейса

**Где создать**:
- `lib/MBMigration/Contracts/CacheInterface.php`

### Шаг 3: Расширить DatabaseInterface

**Что сделать**:
1. Изучить существующий `DatabaseInterface`
2. Изучить `DBConnector` и определить все методы
3. Расширить интерфейс, если нужно
4. Обновить `DBConnector` для реализации интерфейса

**Где создать/обновить**:
- `lib/MBMigration/Contracts/DatabaseInterface.php`

### Шаг 4: Создать QueryBuilderInterface

**Что сделать**:
1. Изучить `QueryBuilder` и определить все публичные методы
2. Создать интерфейс `QueryBuilderInterface`
3. Обновить `QueryBuilder` для реализации интерфейса

**Где создать**:
- `lib/MBMigration/Contracts/QueryBuilderInterface.php`

### Шаг 5: Создать FontsControllerInterface

**Что сделать**:
1. Изучить `FontsController` и определить все публичные методы
2. Создать интерфейс `FontsControllerInterface`
3. Обновить `FontsController` для реализации интерфейса

**Где создать**:
- `lib/MBMigration/Contracts/FontsControllerInterface.php`

### Шаг 6: Создать интерфейсы для Builder/Layout

**Что сделать**:
1. Определить ключевые классы в `Builder/Layout`
2. Создать интерфейсы для каждого класса
3. Обновить классы для реализации интерфейсов

**Приоритет**:
- Начать с наиболее используемых классов
- Создавать интерфейсы постепенно

### Шаг 7: Обновить все использования

**Что сделать**:
1. Заменить конкретные классы на интерфейсы в конструкторах
2. Обновить все места, где создаются объекты
3. Убедиться, что везде используются интерфейсы

**Важно**:
- Обновлять постепенно
- После каждого обновления запускать тесты

### Шаг 8: Написать тесты с моками

⚠️ **ОБЯЗАТЕЛЬНО**: После реализации сразу написать тесты с моками!

**Что тестировать**:
- Можно создать моки для всех интерфейсов
- Тесты используют моки, а не реальные реализации

**Где создать тесты**:
- `tests/MBMigration/Contracts/InterfacesTest.php` - проверка, что все интерфейсы можно мокировать

### Шаг 9: Добавить комментарии

⚠️ **ОБЯЗАТЕЛЬНО**: Добавить PHPDoc комментарии!

## 📂 Исходные файлы

- `lib/MBMigration/Builder/VariableCache.php`
- `lib/MBMigration/Layer/DataSource/DBConnector.php`
- `lib/MBMigration/Layer/Graph/QueryBuilder.php`
- `lib/MBMigration/Builder/Fonts/FontsController.php`
- Классы в `lib/MBMigration/Builder/Layout/`

## ✅ Критерии приемки

- [ ] Создан CacheInterface
- [ ] Расширен DatabaseInterface
- [ ] Создан QueryBuilderInterface
- [ ] Создан FontsControllerInterface
- [ ] Созданы интерфейсы для ключевых классов Builder/Layout
- [ ] Все классы реализуют интерфейсы
- [ ] Все использования обновлены на интерфейсы
- [ ] Тесты написаны и проходят
- [ ] Можно создавать моки для всех интерфейсов
- [ ] Все интерфейсы имеют PHPDoc комментарии

## 🔍 Как проверить

1. Проверить, что все интерфейсы существуют
2. Проверить, что все классы реализуют интерфейсы
3. Попробовать создать моки для всех интерфейсов
4. Запустить тесты: `composer test`

## 📚 Связанные задачи

- Предыдущая задача: [Задача 1.2: Убрать статические свойства из Config](./task-p1-2-refactor-config.md)
- Следующая задача: [Задача 1.4: Внедрить Dependency Injection](./task-p1-4-implement-di.md)

---

**ВАЖНО**: Если во время выполнения задачи возникли вопросы или что-то неизвестно - ОСТАНОВИТЬСЯ и ЗАПРОСИТЬ у пользователя, не придумывать!
