# Задача 1.4: Внедрить Dependency Injection

## 📋 Информация о задаче

**Время выполнения**: 2-3 недели  
**Приоритет**: КРИТИЧЕСКИЙ (Приоритет 1)  
**Зависимости**: [Задача 1.3: Создать интерфейсы для ключевых компонентов](./task-p1-3-create-interfaces.md)

## 🎯 Принципы работы

⚠️ **ВАЖНО**: При выполнении этой задачи следуйте принципам из [PRINCIPLES.md](../../../../PRINCIPLES.md)

## ❌ Проблема

Нет централизованного механизма управления зависимостями:
- Зависимости создаются в разных местах по-разному
- Невозможно централизованно управлять жизненным циклом объектов
- Сложно подменять зависимости для тестов
- Дублирование кода создания зависимостей

**Почему это критично**:
- Невозможно централизованно управлять зависимостями
- Сложно подменять зависимости для тестов
- Дублирование кода
- Сложно понять, какие зависимости нужны для класса

**Что будет, если не решить**:
- Сложно управлять зависимостями
- Невозможно легко подменять зависимости для тестов
- Дублирование кода создания объектов

## 🎯 Ожидаемый результат

После выполнения задачи:

1. **DI контейнер установлен** - выбран и установлен DI контейнер (PHP-DI или Symfony DI)
2. **Конфигурация контейнера** - создана конфигурация для всех зависимостей
3. **Все зависимости инжектируются** - все зависимости инжектируются через конструктор
4. **Фабрики созданы** - фабрики для сложных объектов
5. **Тесты используют контейнер** - тесты могут использовать контейнер для создания объектов

**Как проверить результат**:
- Можно получить любой объект из контейнера
- Все зависимости разрешаются автоматически
- Тесты могут использовать контейнер

## 📝 Что нужно сделать

### Шаг 1: Выбрать DI контейнер

**Варианты**:
- PHP-DI (рекомендуется для простоты)
- Symfony DI (если уже используется Symfony)

**Что сделать**:
1. Изучить оба варианта
2. Выбрать подходящий
3. Установить через composer

**Команда для установки PHP-DI**:
```bash
composer require php-di/php-di
```

### Шаг 2: Создать конфигурацию контейнера

**Что сделать**:
1. Создать файл конфигурации контейнера
2. Определить все зависимости
3. Настроить автовайринг (если поддерживается)

**Где создать**:
- `lib/MBMigration/Core/Container/ContainerConfig.php` или
- `config/di.php`

**Что настроить**:
- Все интерфейсы и их реализации
- Фабрики для сложных объектов
- Синглтоны (если нужно)

### Шаг 3: Создать Container класс

**Что сделать**:
1. Создать класс-обертку над DI контейнером
2. Инициализировать контейнер с конфигурацией
3. Предоставить методы для получения объектов

**Где создать**:
- `lib/MBMigration/Core/Container/Container.php`

### Шаг 4: Обновить ApplicationBootstrapper

**Что сделать**:
1. Использовать контейнер для создания зависимостей
2. Обновить создание MigrationPlatform через контейнер
3. Убедиться, что все зависимости разрешаются

**Файлы для работы**:
- `lib/MBMigration/ApplicationBootstrapper.php`

### Шаг 5: Создать фабрики для сложных объектов

**Что сделать**:
1. Определить объекты, которые требуют сложной инициализации
2. Создать фабрики для этих объектов
3. Зарегистрировать фабрики в контейнере

**Примеры фабрик**:
- `LoggerFactory` - создание Logger с конфигурацией
- `VariableCacheFactory` - создание VariableCache с путем
- `DBConnectorFactory` - создание DBConnector с конфигурацией

### Шаг 6: Обновить все создания объектов

**Что сделать**:
1. Найти все места, где создаются объекты через `new`
2. Заменить на получение из контейнера
3. Убедиться, что все зависимости инжектируются

**Важно**:
- Обновлять постепенно
- После каждого обновления запускать тесты

### Шаг 7: Написать тесты

⚠️ **ОБЯЗАТЕЛЬНО**: После реализации сразу написать тесты!

**Что тестировать**:
- Контейнер может создать все объекты
- Зависимости разрешаются правильно
- Можно подменять зависимости в тестах

**Где создать тесты**:
- `tests/MBMigration/Core/Container/ContainerTest.php`

### Шаг 8: Добавить комментарии

⚠️ **ОБЯЗАТЕЛЬНО**: Добавить PHPDoc комментарии!

## 📂 Исходные файлы

- Все файлы, где создаются объекты через `new`
- `lib/MBMigration/ApplicationBootstrapper.php`
- Все классы, которые имеют зависимости

## ✅ Критерии приемки

- [ ] DI контейнер установлен и настроен
- [ ] Конфигурация контейнера создана
- [ ] Container класс создан
- [ ] ApplicationBootstrapper использует контейнер
- [ ] Фабрики созданы для сложных объектов
- [ ] Все создания объектов обновлены
- [ ] Тесты написаны и проходят
- [ ] Можно получить любой объект из контейнера
- [ ] Все классы и методы имеют PHPDoc комментарии

## 🔍 Как проверить

1. Попробовать получить объект из контейнера
2. Проверить, что все зависимости разрешаются
3. Запустить тесты: `composer test`
4. Проверить, что тесты могут использовать контейнер

## 📚 Связанные задачи

- Предыдущая задача: [Задача 1.3: Создать интерфейсы для ключевых компонентов](./task-p1-3-create-interfaces.md)
- Следующая задача: [Задача 2.1: Абстрагировать внешние зависимости](./task-p2-1-abstract-external-deps.md)

---

**ВАЖНО**: Если во время выполнения задачи возникли вопросы или что-то неизвестно - ОСТАНОВИТЬСЯ и ЗАПРОСИТЬ у пользователя, не придумывать!
