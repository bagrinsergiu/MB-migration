# Этап 1: Рефакторинг для тестируемости

## 📋 Обзор этапа

**Длительность**: 2-3 недели  
**Приоритет**: КРИТИЧЕСКИЙ  
**Зависимости**: Нет (можно начинать сразу)

## 🎯 Цель этапа

Подготовить код к тестированию путем внедрения Dependency Injection и создания интерфейсов для всех внешних зависимостей.

## ❌ Проблема

Текущий код создает зависимости напрямую:

```php
// ❌ ПЛОХО - нельзя подменить моками
class MigrationPlatform {
    public function __construct(Config $config) {
        $this->brizyApi = new BrizyAPI($config);
        $this->parser = new MBProjectDataCollector($config);
    }
}
```

## ✅ Решение

Внедрить Dependency Injection:

```php
// ✅ ХОРОШО - можно подставить моки
class MigrationPlatform {
    public function __construct(
        Config $config,
        BrizyAPIInterface $brizyApi,
        MBProjectDataCollectorInterface $parser
    ) {
        $this->brizyApi = $brizyApi;
        $this->parser = $parser;
    }
}
```

---

## 📝 Задачи этапа

### Задача 1.1: Создать директорию для интерфейсов

**Время**: 15 минут

```bash
mkdir -p lib/MBMigration/Contracts
```

**Результат**: Директория создана

---

### Задача 1.2: Создать интерфейс для Brizy API

**Время**: 1-2 часа  
**Файл**: `lib/MBMigration/Contracts/BrizyAPIInterface.php`

**Что сделать**:
1. Изучить класс `BrizyAPI` (`lib/MBMigration/Layer/Brizy/BrizyAPI.php`)
2. Выделить публичные методы
3. Создать интерфейс с этими методами
4. Добавить PHPDoc комментарии

**Пример**:
```php
<?php

namespace MBMigration\Contracts;

interface BrizyAPIInterface
{
    /**
     * Создать проект в Brizy
     * 
     * @param array $data Данные проекта
     * @return array Ответ API с ID проекта
     */
    public function createProject(array $data): array;
    
    /**
     * Создать страницу в проекте
     */
    public function createPage(int $projectId, array $data): array;
    
    // ... остальные методы
}
```

**Чеклист**:
- [ ] Интерфейс создан
- [ ] Все публичные методы BrizyAPI включены
- [ ] PHPDoc комментарии добавлены
- [ ] Интерфейс протестирован (проверка синтаксиса)

---

### Задача 1.3: Создать интерфейс для MB Project Data Collector

**Время**: 1-2 часа  
**Файл**: `lib/MBMigration/Contracts/MBProjectDataCollectorInterface.php`

**Что сделать**:
1. Изучить класс `MBProjectDataCollector`
2. Выделить публичные методы
3. Создать интерфейс

**Чеклист**:
- [ ] Интерфейс создан
- [ ] Все публичные методы включены
- [ ] PHPDoc комментарии добавлены

---

### Задача 1.4: Создать интерфейс для Database

**Время**: 1 час  
**Файл**: `lib/MBMigration/Contracts/DatabaseInterface.php`

**Что сделать**:
1. Изучить классы `MySQL` и `PostgresSQL`
2. Выделить общие методы
3. Создать интерфейс

**Методы**:
- `query(string $sql, array $params = []): array`
- `insert(string $table, array $data): int`
- `update(string $table, array $data, array $where): int`
- `beginTransaction(): void`
- `commit(): void`
- `rollback(): void`

**Чеклист**:
- [ ] Интерфейс создан
- [ ] Все необходимые методы включены

---

### Задача 1.5: Создать интерфейс для S3 Uploader

**Время**: 30 минут  
**Файл**: `lib/MBMigration/Contracts/S3UploaderInterface.php`

**Что сделать**:
1. Изучить класс `S3Uploader`
2. Создать интерфейс

**Чеклист**:
- [ ] Интерфейс создан

---

### Задача 1.6: Расширить BrowserInterface

**Время**: 30 минут  
**Файл**: `lib/MBMigration/Browser/BrowserInterface.php` (уже существует)

**Что сделать**:
1. Проверить существующий интерфейс
2. Добавить недостающие методы, если нужно

**Чеклист**:
- [ ] Интерфейс проверен и дополнен

---

### Задача 1.7: Рефакторинг MigrationPlatform

**Время**: 4-6 часов  
**Файл**: `lib/MBMigration/MigrationPlatform.php`

**Что сделать**:
1. Изменить конструктор - принимать интерфейсы вместо создания объектов
2. Обновить все места, где создается MigrationPlatform
3. Убедиться, что код работает

**До**:
```php
public function __construct(
    Config $config,
    LoggerInterface $logger,
    $buildPage = '',
    $workspacesId = 0,
    // ...
) {
    $this->brizyApi = new BrizyAPI($config);
    $this->parser = new MBProjectDataCollector($config);
}
```

**После**:
```php
public function __construct(
    Config $config,
    LoggerInterface $logger,
    BrizyAPIInterface $brizyApi,
    MBProjectDataCollectorInterface $mbCollector,
    $buildPage = '',
    $workspacesId = 0,
    // ...
) {
    $this->brizyApi = $brizyApi;
    $this->parser = $mbCollector;
}
```

**Чеклист**:
- [ ] Конструктор изменен
- [ ] Все места создания MigrationPlatform обновлены
- [ ] Код компилируется без ошибок
- [ ] Протестировано вручную (запуск миграции)

---

### Задача 1.8: Рефакторинг PageController

**Время**: 2-3 часа  
**Файл**: `lib/MBMigration/Builder/PageController.php`

**Что сделать**:
1. Найти все прямые создания зависимостей
2. Изменить конструктор для принятия интерфейсов
3. Обновить места создания PageController

**Чеклист**:
- [ ] Конструктор изменен
- [ ] Все зависимости через интерфейсы
- [ ] Код работает

---

### Задача 1.9: Рефакторинг Bridge

**Время**: 2-3 часа  
**Файл**: `lib/MBMigration/Bridge/Bridge.php`

**Что сделать**:
1. Изменить конструктор
2. Убрать прямые создания зависимостей

**Чеклист**:
- [ ] Рефакторинг завершен
- [ ] Код работает

---

### Задача 1.10: Рефакторинг Dashboard API сервисов

**Время**: 3-4 часа  
**Файлы**: 
- `dashboard/api/services/DatabaseService.php`
- `dashboard/api/services/MigrationService.php`
- `dashboard/api/services/ApiProxyService.php`

**Что сделать**:
1. Для каждого сервиса изменить конструктор
2. Принимать интерфейсы вместо конкретных классов

**Пример для DatabaseService**:
```php
// До
public function __construct() {
    $this->db = new MySQL($config);
}

// После
public function __construct(DatabaseInterface $db) {
    $this->db = $db;
}
```

**Чеклист**:
- [ ] DatabaseService рефакторен
- [ ] MigrationService рефакторен
- [ ] ApiProxyService рефакторен
- [ ] Все контроллеры обновлены

---

### Задача 1.11: Создать DI контейнер (опционально)

**Время**: 4-6 часов  
**Приоритет**: НИЗКИЙ (можно отложить)

**Что сделать**:
1. Установить PHP-DI: `composer require php-di/php-di`
2. Создать конфигурацию DI
3. Настроить автоматическое создание объектов

**Файл**: `lib/MBMigration/Container.php`

**Чеклист**:
- [ ] PHP-DI установлен
- [ ] Конфигурация создана
- [ ] Контейнер работает

---

### Задача 1.12: Обновить ApplicationBootstrapper

**Время**: 2-3 часа  
**Файл**: `lib/MBMigration/ApplicationBootstrapper.php`

**Что сделать**:
1. Изменить создание MigrationPlatform
2. Передавать интерфейсы вместо создания объектов
3. Можно использовать DI контейнер (если создан)

**Чеклист**:
- [ ] ApplicationBootstrapper обновлен
- [ ] Все миграции работают

---

### Задача 1.13: Тестирование рефакторинга

**Время**: 2-3 часа

**Что сделать**:
1. Запустить существующие тесты: `composer test`
2. Запустить миграцию вручную
3. Проверить, что ничего не сломалось

**Чеклист**:
- [ ] Все существующие тесты проходят
- [ ] Миграция работает вручную
- [ ] Нет ошибок в логах

---

## 📊 Критерии завершения этапа

- [ ] Все интерфейсы созданы (BrizyAPI, MBProjectDataCollector, Database, S3Uploader)
- [ ] Все ключевые классы рефакторены (MigrationPlatform, PageController, Bridge)
- [ ] Все Dashboard API сервисы рефакторены
- [ ] Код компилируется без ошибок
- [ ] Существующие тесты проходят
- [ ] Миграция работает вручную

---

## 🧪 Проверка готовности к следующему этапу

Перед переходом к Этапу 2 убедитесь:

1. ✅ Можно создать мок для BrizyAPIInterface
2. ✅ Можно создать мок для MBProjectDataCollectorInterface
3. ✅ Можно передать моки в MigrationPlatform
4. ✅ Код работает с реальными зависимостями

**Тест**:
```php
// Это должно работать без ошибок
$brizyMock = new BrizyAPIMock();
$mbMock = new MBProjectDataCollectorMock();
$platform = new MigrationPlatform($config, $logger, $brizyMock, $mbMock);
```

---

## 📚 Примеры кода

См. [examples/stage-1/](../examples/stage-1/) для конкретных примеров:
- Создание интерфейсов
- Рефакторинг классов
- Использование DI

---

## ⚠️ Важные замечания

1. **Не ломать существующий функционал** - после каждого изменения проверять, что миграция работает
2. **Делать по одному классу** - не рефакторить все сразу
3. **Коммитить часто** - после каждой завершенной задачи
4. **Тестировать вручную** - после каждого изменения

---

## 🐛 Возможные проблемы

### Проблема: Циклические зависимости
**Решение**: Использовать lazy loading или пересмотреть архитектуру

### Проблема: Слишком много параметров в конструкторе
**Решение**: Создать конфигурационные объекты (DTO)

### Проблема: Старые места создания объектов
**Решение**: Использовать поиск по проекту (`grep`) для поиска всех мест

---

## 📅 Примерный план по дням

**Неделя 1**:
- День 1-2: Задачи 1.1-1.6 (создание интерфейсов)
- День 3-4: Задача 1.7 (рефакторинг MigrationPlatform)
- День 5: Задача 1.8 (рефакторинг PageController)

**Неделя 2**:
- День 1-2: Задачи 1.9-1.10 (Bridge и Dashboard сервисы)
- День 3: Задача 1.12 (ApplicationBootstrapper)
- День 4-5: Задача 1.13 (тестирование)

**Неделя 3** (если нужно):
- Дополнительное тестирование
- Исправление проблем
- Задача 1.11 (DI контейнер, опционально)

---

**Следующий этап**: [Этап 2: Создание тестовой инфраструктуры](./stage-2-test-infrastructure.md)
