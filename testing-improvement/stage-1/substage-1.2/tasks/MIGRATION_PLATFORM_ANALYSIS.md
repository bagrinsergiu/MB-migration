# Анализ класса MigrationPlatform

**Дата анализа**: 2025-01-27  
**Файл**: `lib/MBMigration/MigrationPlatform.php`  
**Версия**: Текущая версия из репозитория

## 📋 Конструктор

### Сигнатура конструктора

```php
public function __construct(
    Config          $config,
    LoggerInterface $logger,
                    $buildPage = '',
                    $workspacesId = 0,
    bool            $mMgrIgnore = true,
                    $mgr_manual = false,
    bool            $qualityAnalysis = false,
    string          $mb_element_name = '',
    bool            $skip_media_upload = false,
    bool            $skip_cache = false
)
```

### Параметры конструктора

1. `Config $config` - Конфигурация проекта
2. `LoggerInterface $logger` - Логгер для записи событий
3. `$buildPage = ''` - Имя страницы для сборки (по умолчанию пустая строка)
4. `$workspacesId = 0` - ID workspace (по умолчанию 0)
5. `bool $mMgrIgnore = true` - Игнорировать менеджер миграции (по умолчанию true)
6. `$mgr_manual = false` - Ручная миграция (по умолчанию false)
7. `bool $qualityAnalysis = false` - Включить анализ качества (по умолчанию false)
8. `string $mb_element_name = ''` - Имя элемента MB для тестирования (по умолчанию пустая строка)
9. `bool $skip_media_upload = false` - Пропустить загрузку медиа (по умолчанию false)
10. `bool $skip_cache = false` - Пропустить кэш (по умолчанию false)

### Создает ли зависимости в конструкторе?

**НЕТ** - Конструктор НЕ создает зависимости напрямую. Он только:
- Инициализирует кэш (`VariableCache`)
- Сохраняет переданные параметры в свойства класса
- Создает `ErrorDump` для обработки ошибок
- Регистрирует обработчики ошибок

### Что делает конструктор

1. Инициализирует кэш: `$this->cache = VariableCache::getInstance(Config::$cachePath)`
2. Сохраняет логгер: `$this->logger = $logger`
3. Сохраняет все параметры в свойства класса
4. Создает `ErrorDump` и регистрирует обработчики ошибок
5. Инициализирует `$this->finalSuccess['status'] = 'start'`
6. Инициализирует `$this->projectPagesList = []`

---

## 🔍 Места создания зависимостей

### В методе `init()` (строка 189)

**Строка 213**: `$this->parser = new MBProjectDataCollector();`

- **Класс**: `MBProjectDataCollector`
- **Параметры конструктора**: Нет параметров (конструктор принимает опциональный `$projectId = null`)
- **Сохраняется в**: `$this->parser` (тип: `MBProjectDataCollector`)
- **Используется в**: 
  - Метод `run()` - строки 260, 227, 228, 318, 344, 360
  - Метод `collector()` - не используется напрямую, но через `$this->pageController`

**Примечание**: `MBProjectDataCollector` реализует интерфейс `MBProjectDataCollectorInterface`

---

### В методе `run()` (строка 220)

#### 1. Создание BrizyAPI (строка 222)

**Строка 222**: `$this->brizyApi = new BrizyAPI();`

- **Класс**: `BrizyAPI`
- **Параметры конструктора**: Нет параметров (конструктор не принимает параметры)
- **Сохраняется в**: `$this->brizyApi` (тип: `BrizyApi` - опечатка в типе, должно быть `BrizyAPI`)
- **Используется в**: 
  - Метод `run()` - строки 224, 232, 235, 240, 241, 251, 254, 265, 280, 323, 382, 471, 487
  - Метод `collector()` - строка 607
  - Передается в `PageController` при создании (строка 291)

**Примечание**: `BrizyAPI` реализует интерфейс `BrizyAPIInterface`

#### 2. Создание PageController (строка 289)

**Строка 289-300**: 
```php
$this->pageController = new PageController(
    $this->parser,
    $this->brizyApi,
    $this->QueryBuilder,
    $this->logger,
    $this->projectID_Brizy,
    $designName,
    $this->qualityAnalysisEnabled,
    $this->mb_element_name,
    $this->skip_media_upload,
    $this->skip_cache
);
```

- **Класс**: `PageController`
- **Параметры конструктора**: 
  1. `$this->parser` (MBProjectDataCollector) - уже создан в `init()`
  2. `$this->brizyApi` (BrizyAPI) - только что создан в `run()`
  3. `$this->QueryBuilder` (QueryBuilder) - создается в `run()` на строке 284
  4. `$this->logger` (LoggerInterface) - передан в конструктор MigrationPlatform
  5. `$this->projectID_Brizy` (int) - вычисляется в `run()`
  6. `$designName` (string) - получается из `$this->parser->getDesignSite()`
  7. `$this->qualityAnalysisEnabled` (bool) - передан в конструктор MigrationPlatform
  8. `$this->mb_element_name` (string) - передан в конструктор MigrationPlatform
  9. `$this->skip_media_upload` (bool) - передан в конструктор MigrationPlatform
  10. `$this->skip_cache` (bool) - передан в конструктор MigrationPlatform
- **Сохраняется в**: `$this->pageController` (тип: `PageController`)
- **Используется в**: 
  - Метод `run()` - строки 314, 389, 398, 440, 630
  - Метод `collector()` - строки 593, 618, 630

**Примечание**: `PageController` не имеет интерфейса (пока)

#### 3. Создание QueryBuilder (строка 284)

**Строка 284-287**:
```php
$this->QueryBuilder = new QueryBuilder(
    $this->graphApiBrizy,
    $graphToken
);
```

- **Класс**: `QueryBuilder`
- **Параметры конструктора**: 
  1. `$this->graphApiBrizy` (string) - вычисляется в `init()`
  2. `$graphToken` (string) - получается из `$this->brizyApi->getGraphToken()`
- **Сохраняется в**: `$this->QueryBuilder` (тип: `QueryBuilder`)
- **Используется в**: 
  - Передается в `PageController` при создании (строка 292)
  - Сохраняется в кэш (строка 311)

**Примечание**: `QueryBuilder` не имеет интерфейса (пока)

#### 4. Создание MonkcmsAPI (строка 336)

**Строка 336**: `$mCms = new MonkcmsAPI($configM);`

- **Класс**: `MonkcmsAPI`
- **Параметры конструктора**: `$configM` (массив с `siteId` и `siteSecret`)
- **Сохраняется в**: Локальная переменная `$mCms` (НЕ в свойство класса)
- **Используется в**: 
  - Метод `run()` - строка 339 (получение серий)

**Примечание**: `MonkcmsAPI` не имеет интерфейса (пока) и используется только локально

---

## 📦 Свойства класса для зависимостей

### Основные зависимости

1. **`private MBProjectDataCollector $parser;`** (строка 33)
   - Создается в: `init()` (строка 213)
   - Используется в: `run()`, передается в `PageController`

2. **`private BrizyApi $brizyApi;`** (строка 35)
   - **Примечание**: Тип указан как `BrizyApi`, но класс называется `BrizyAPI` (возможна опечатка)
   - Создается в: `run()` (строка 222)
   - Используется в: `run()`, `collector()`, передается в `PageController`

3. **`private PageController $pageController;`** (строка 43)
   - Создается в: `run()` (строка 289)
   - Используется в: `run()`, `collector()`

4. **`private QueryBuilder $QueryBuilder;`** (строка 34)
   - Создается в: `run()` (строка 284)
   - Используется в: Передается в `PageController`

### Другие зависимости (не основные)

5. **`MonkcmsAPI`** - создается локально в `run()`, не сохраняется в свойство класса

---

## 🔄 Порядок инициализации

### Последовательность вызовов методов

1. **Конструктор `__construct()`** (строка 73)
   - Вызывается при создании объекта
   - Инициализирует базовые свойства
   - НЕ создает зависимости

2. **Метод `start()`** (строка 110)
   - Вызывается пользователем для начала миграции
   - Загружает кэш (если не `skip_cache`)
   - Вызывает `run()`
   - Сохраняет кэш (если не `skip_cache`)

3. **Метод `run()`** (строка 220)
   - Вызывается из `start()`
   - Создает `BrizyAPI` (строка 222)
   - Вызывает `init()` (строка 257)
   - Создает `QueryBuilder` (строка 284)
   - Создает `PageController` (строка 289)
   - Создает `MonkcmsAPI` локально (строка 336)
   - Выполняет миграцию

4. **Метод `init()`** (строка 189)
   - Вызывается из `run()` (строка 257)
   - Создает `MBProjectDataCollector` (строка 213)
   - Инициализирует другие параметры

### Порядок создания зависимостей

1. **В `run()`**: `BrizyAPI` создается первым (строка 222)
2. **В `init()`**: `MBProjectDataCollector` создается (строка 213) - вызывается из `run()`
3. **В `run()`**: `QueryBuilder` создается (строка 284)
4. **В `run()`**: `PageController` создается (строка 289) - использует `$this->parser` и `$this->brizyApi`
5. **В `run()`**: `MonkcmsAPI` создается локально (строка 336)

---

## 📝 Использование зависимостей

### `$this->parser` (MBProjectDataCollector)

**Используется в методах**:
- `run()` - строки 260 (`getDesignSite()`), 227-228 (статические методы), 318 (`getSite()`), 344 (`getPages()`), 360 (`getMainSection()`)
- Передается в `PageController` при создании (строка 290)

### `$this->brizyApi` (BrizyAPI)

**Используется в методах**:
- `run()` - множество вызовов методов API
- `collector()` - строка 607 (`uploadPicturesFromSections`)
- Передается в `PageController` при создании (строка 291)

### `$this->pageController` (PageController)

**Используется в методах**:
- `run()` - строки 314 (`getAllPage()`), 389 (`deleteAllPages()`), 398 (`createBlankPages()`), 440 (`getPageMapping()`)
- `collector()` - строки 593 (`getSectionsFromPage()`), 618 (`setCurrentPageOnWork()`), 630 (`run()`)

### `$this->QueryBuilder` (QueryBuilder)

**Используется в**:
- Передается в `PageController` при создании (строка 292)
- Сохраняется в кэш (строка 311)

---

## 🎯 Выводы для рефакторинга

### Зависимости, которые нужно инжектировать через конструктор

1. **`MBProjectDataCollectorInterface`** - создается в `init()`, используется в `run()` и передается в `PageController`
   - **Рекомендация**: Инжектировать через конструктор, так как используется в нескольких методах

2. **`BrizyAPIInterface`** - создается в `run()`, используется в `run()` и `collector()`, передается в `PageController`
   - **Рекомендация**: Инжектировать через конструктор, так как используется в нескольких методах

### Зависимости, которые можно оставить как есть

3. **`PageController`** - создается в `run()` с зависимостями, которые уже инжектированы
   - **Рекомендация**: Можно оставить создание в `run()`, но передавать зависимости через конструктор MigrationPlatform

4. **`QueryBuilder`** - создается в `run()` с параметрами, вычисляемыми в `run()`
   - **Рекомендация**: Можно оставить создание в `run()`, так как параметры зависят от выполнения `run()`

5. **`MonkcmsAPI`** - создается локально, используется только в одном месте
   - **Рекомендация**: Можно оставить как есть, так как используется локально

### План рефакторинга

1. Добавить в конструктор `MigrationPlatform` параметры:
   - `BrizyAPIInterface $brizyApi`
   - `MBProjectDataCollectorInterface $mbCollector`

2. Убрать из `init()` создание `MBProjectDataCollector` - использовать переданный через конструктор

3. Убрать из `run()` создание `BrizyAPI` - использовать переданный через конструктор

4. Обновить все места создания `MigrationPlatform` для передачи зависимостей

---

## ⚠️ Важные замечания

1. **Тип свойства `$brizyApi`**: Указан как `BrizyApi`, но класс называется `BrizyAPI` - возможно, нужно исправить тип

2. **Порядок вызовов**: `init()` вызывается из `run()`, поэтому зависимости, создаваемые в `init()`, доступны в `run()`

3. **PageController**: Создается с множеством параметров, включая уже созданные зависимости - это нормально, так как он создается в процессе выполнения `run()`

4. **Обратная совместимость**: При рефакторинге нужно убедиться, что все места создания `MigrationPlatform` обновлены

---

**Анализ выполнен**: 2025-01-27  
**Следующий шаг**: Рефакторинг конструктора MigrationPlatform (задача 2.2)
