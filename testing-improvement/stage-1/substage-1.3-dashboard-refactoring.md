# Подэтап 1.3: Рефакторинг Dashboard API

## 📋 Обзор подэтапа

**Длительность**: 1-2 дня  
**Приоритет**: ВЫСОКИЙ  
**Зависимости**: Подэтап 1.1 (интерфейсы должны быть созданы)

## 🎯 Цель подэтапа

Рефакторить сервисы Dashboard API, чтобы они использовали интерфейсы вместо прямых зависимостей.

## ❌ Проблема

Сервисы Dashboard API создают зависимости напрямую:
- `DatabaseService` создает `MySQL` напрямую
- `MigrationService` может создавать зависимости напрямую
- `ApiProxyService` может создавать зависимости напрямую

## ✅ Решение

Изменить конструкторы сервисов, чтобы они принимали интерфейсы.

---

## 📝 Задачи подэтапа

### Задача 3.1: Изучить DatabaseService

**Время**: 30-60 минут  
**Приоритет**: ВЫСОКИЙ

**Что сделать**:
1. Открыть файл `dashboard/api/services/DatabaseService.php`
2. Найти конструктор
3. Найти все прямые создания зависимостей
4. Определить, какие интерфейсы нужны

**Чеклист**:
- [ ] Конструктор изучен
- [ ] Зависимости найдены

---

### Задача 3.2: Рефакторинг DatabaseService

**Время**: 1-2 часа  
**Приоритет**: КРИТИЧЕСКИЙ  
**Файл**: `dashboard/api/services/DatabaseService.php`

**Что сделать**:
1. Изменить конструктор - принимать `DatabaseInterface`
2. Убрать прямое создание `MySQL`

**До**:
```php
public function __construct() {
    $this->db = new MySQL($config);
}
```

**После**:
```php
use MBMigration\Contracts\DatabaseInterface;

public function __construct(DatabaseInterface $db) {
    $this->db = $db;
}
```

**Важно**:
- Добавить use statement для DatabaseInterface
- Убрать создание MySQL
- Обновить все методы, которые используют $this->db

**Проверка**:
```bash
php -l dashboard/api/services/DatabaseService.php
```

**Чеклист**:
- [ ] Конструктор изменен
- [ ] DatabaseInterface добавлен в use
- [ ] Прямое создание MySQL убрано
- [ ] Синтаксис проверен

---

### Задача 3.3: Найти и обновить места создания DatabaseService

**Время**: 30-60 минут  
**Приоритет**: ВЫСОКИЙ

**Что сделать**:
1. Найти все места, где создается `new DatabaseService()`
2. Обновить их для передачи DatabaseInterface

**Команды**:
```bash
grep -r "new DatabaseService" dashboard/
```

**Основные места**:
- `dashboard/api/controllers/MigrationController.php`
- `dashboard/api/controllers/LogController.php`
- Другие контроллеры (если есть)

**Пример**:
```php
// До
$dbService = new DatabaseService();

// После
$db = new \MBMigration\Layer\DataSource\driver\MySQL($config);
$dbService = new DatabaseService($db);
```

**Чеклист**:
- [ ] Все места найдены
- [ ] Все места обновлены
- [ ] Код компилируется

---

### Задача 3.4: Изучить MigrationService

**Время**: 30 минут  
**Приоритет**: СРЕДНИЙ

**Что сделать**:
1. Открыть файл `dashboard/api/services/MigrationService.php`
2. Найти конструктор
3. Проверить, есть ли прямые создания зависимостей

**Чеклист**:
- [ ] Конструктор изучен
- [ ] Зависимости определены

---

### Задача 3.5: Рефакторинг MigrationService

**Время**: 1-2 часа  
**Приоритет**: СРЕДНИЙ  
**Файл**: `dashboard/api/services/MigrationService.php`

**Что сделать**:
1. Если есть прямые создания зависимостей - убрать их
2. Изменить конструктор для принятия интерфейсов
3. Обновить use statements

**Пример** (если нужно):
```php
// До
public function __construct(DatabaseService $dbService) {
    // Может создавать зависимости внутри
}

// После
public function __construct(DatabaseService $dbService) {
    // Принимает уже созданный сервис
    $this->dbService = $dbService;
}
```

**Чеклист**:
- [ ] Конструктор проверен/изменен
- [ ] Прямые создания убраны (если были)
- [ ] Синтаксис проверен

---

### Задача 3.6: Изучить ApiProxyService

**Время**: 30 минут  
**Приоритет**: СРЕДНИЙ

**Что сделать**:
1. Открыть файл `dashboard/api/services/ApiProxyService.php`
2. Найти конструктор
3. Проверить зависимости

**Чеклист**:
- [ ] Конструктор изучен
- [ ] Зависимости определены

---

### Задача 3.7: Рефакторинг ApiProxyService

**Время**: 1-2 часа  
**Приоритет**: СРЕДНИЙ  
**Файл**: `dashboard/api/services/ApiProxyService.php`

**Что сделать**:
1. Если есть прямые создания зависимостей - убрать их
2. Изменить конструктор для принятия интерфейсов
3. Обновить все места создания

**Чеклист**:
- [ ] Конструктор проверен/изменен
- [ ] Прямые создания убраны (если были)
- [ ] Все места создания обновлены

---

### Задача 3.8: Обновить контроллеры Dashboard API

**Время**: 1-2 часа  
**Приоритет**: ВЫСОКИЙ

**Что сделать**:
1. Открыть все контроллеры:
   - `dashboard/api/controllers/MigrationController.php`
   - `dashboard/api/controllers/LogController.php`
   - Другие контроллеры (если есть)
2. Обновить создание сервисов для передачи зависимостей

**Пример для MigrationController**:
```php
// До
public function __construct() {
    $this->dbService = new DatabaseService();
    $this->migrationService = new MigrationService($this->dbService);
}

// После
public function __construct(DatabaseService $dbService, MigrationService $migrationService) {
    $this->dbService = $dbService;
    $this->migrationService = $migrationService;
}
```

**Или создать в index.php**:
```php
// Создать зависимости
$db = new \MBMigration\Layer\DataSource\driver\MySQL($config);
$dbService = new DatabaseService($db);
$migrationService = new MigrationService($dbService);

// Передать в контроллер
$controller = new MigrationController($dbService, $migrationService);
```

**Чеклист**:
- [ ] MigrationController обновлен
- [ ] LogController обновлен
- [ ] Другие контроллеры обновлены (если есть)
- [ ] index.php обновлен для создания зависимостей

---

### Задача 3.9: Тестирование Dashboard API

**Время**: 1-2 часа  
**Приоритет**: ВЫСОКИЙ

**Что сделать**:
1. Проверить синтаксис всех файлов
2. Запустить существующие тесты (если есть)
3. Попробовать вызвать API endpoints вручную

**Команды**:
```bash
# Проверить синтаксис
find dashboard/api -name "*.php" -exec php -l {} \;

# Проверить основные файлы
php -l dashboard/api/services/DatabaseService.php
php -l dashboard/api/services/MigrationService.php
php -l dashboard/api/controllers/MigrationController.php
```

**Тестирование API**:
```bash
# Health check
curl http://localhost:8000/dashboard/api/health

# Список миграций
curl http://localhost:8000/dashboard/api/migrations
```

**Чеклист**:
- [ ] Все файлы синтаксически корректны
- [ ] API endpoints работают
- [ ] Нет ошибок в логах

---

### Задача 3.10: Финальная проверка Dashboard API

**Время**: 30 минут  
**Приоритет**: ВЫСОКИЙ

**Что сделать**:
1. Проверить все сервисы
2. Проверить все контроллеры
3. Убедиться, что все используют интерфейсы

**Чеклист**:
- [ ] Все сервисы рефакторены
- [ ] Все контроллеры обновлены
- [ ] Код компилируется
- [ ] API работает

---

## 📊 Критерии завершения подэтапа

- [ ] DatabaseService рефакторен и использует DatabaseInterface
- [ ] MigrationService проверен/рефакторен
- [ ] ApiProxyService проверен/рефакторен
- [ ] Все контроллеры обновлены
- [ ] index.php обновлен для создания зависимостей
- [ ] Код компилируется без ошибок
- [ ] API endpoints работают

---

## 🧪 Проверка готовности к следующему подэтапу

Перед переходом к Подэтапу 1.4 убедитесь:

1. ✅ Можно создать DatabaseService с моком:
   ```php
   $dbMock = new DatabaseMock();
   $dbService = new DatabaseService($dbMock);
   ```

2. ✅ API endpoints работают:
   ```bash
   curl http://localhost:8000/dashboard/api/health
   ```

3. ✅ Нет ошибок в логах

**Тест**:
```php
<?php
// test-dashboard.php
require 'vendor/autoload.php';

use Dashboard\Api\Services\DatabaseService;
use Tests\Helpers\Mocks\DatabaseMock;

$dbMock = new DatabaseMock();
$dbService = new DatabaseService($dbMock);

// Это должно работать
$migrations = $dbService->getMigrations();
echo "✅ DatabaseService работает с моком\n";
```

---

## ⚠️ Важные замечания

1. **Dashboard API - отдельная подсистема** - рефакторинг не должен влиять на основную миграцию
2. **Тестировать API endpoints** - после каждого изменения проверять работу API
3. **Сохранять обратную совместимость** - не ломать существующие endpoints

---

## 🐛 Возможные проблемы

### Проблема: API не работает после рефакторинга
**Решение**: 
- Проверить создание зависимостей в index.php
- Проверить логи на ошибки
- Убедиться, что все сервисы создаются правильно

### Проблема: Ошибки в контроллерах
**Решение**: 
- Проверить, что все зависимости передаются
- Проверить use statements
- Проверить типы параметров

---

## 📅 Примерный план по дням

**День 1**:
- Задачи 3.1-3.3 (DatabaseService)
- Задачи 3.4-3.5 (MigrationService)

**День 2**:
- Задачи 3.6-3.7 (ApiProxyService)
- Задачи 3.8-3.10 (контроллеры и тестирование)

---

**Следующий подэтап**: [Подэтап 1.4: Интеграция и тестирование](./substage-1.4-integration-testing.md)
