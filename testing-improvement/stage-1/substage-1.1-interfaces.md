# Подэтап 1.1: Создание интерфейсов

## 📋 Обзор подэтапа

**Длительность**: 2-3 дня  
**Приоритет**: КРИТИЧЕСКИЙ  
**Зависимости**: Нет

## 🎯 Цель подэтапа

Создать все необходимые интерфейсы для внешних зависимостей, которые будут использоваться для Dependency Injection.

## ❌ Проблема

Сейчас классы напрямую зависят от конкретных реализаций:
- `BrizyAPI` - конкретный класс
- `MBProjectDataCollector` - конкретный класс
- `MySQL` / `PostgresSQL` - конкретные классы
- `S3Uploader` - конкретный класс

Невозможно подставить моки для тестирования.

## ✅ Решение

Создать интерфейсы для всех внешних зависимостей:
- `BrizyAPIInterface`
- `MBProjectDataCollectorInterface`
- `DatabaseInterface`
- `S3UploaderInterface`
- Расширить `BrowserInterface` (уже существует)

---

## 📝 Задачи подэтапа

### Задача 1.1: Создать директорию для интерфейсов

**Время**: 15 минут  
**Приоритет**: ВЫСОКИЙ

**Что сделать**:
```bash
mkdir -p lib/MBMigration/Contracts
```

**Проверка**:
```bash
ls -la lib/MBMigration/Contracts
```

**Чеклист**:
- [ ] Директория `lib/MBMigration/Contracts` создана
- [ ] Директория пустая и готова к использованию

---

### Задача 1.2: Изучить класс BrizyAPI

**Время**: 30-60 минут  
**Приоритет**: ВЫСОКИЙ

**Что сделать**:
1. Открыть файл `lib/MBMigration/Layer/Brizy/BrizyAPI.php`
2. Изучить все публичные методы
3. Записать список методов с их сигнатурами
4. Определить, какие методы критичны для миграции

**Методы для изучения**:
- `__construct()`
- `getProjectMetadata()`
- `createProject()`
- `createPage()`
- `updatePage()`
- `uploadMedia()`
- И другие публичные методы

**Результат**: Список всех публичных методов с их параметрами и возвращаемыми типами

**Чеклист**:
- [ ] Класс BrizyAPI изучен
- [ ] Список публичных методов составлен
- [ ] Понимание назначения каждого метода

---

### Задача 1.3: Создать интерфейс BrizyAPIInterface

**Время**: 1-2 часа  
**Приоритет**: КРИТИЧЕСКИЙ  
**Файл**: `lib/MBMigration/Contracts/BrizyAPIInterface.php`

**Что сделать**:
1. Создать файл интерфейса
2. Добавить namespace: `namespace MBMigration\Contracts;`
3. Для каждого публичного метода из BrizyAPI создать метод в интерфейсе
4. Добавить PHPDoc комментарии с описанием
5. Указать типы параметров и возвращаемых значений

**Пример структуры**:
```php
<?php

namespace MBMigration\Contracts;

interface BrizyAPIInterface
{
    /**
     * Получить метаданные проекта
     * 
     * @param int $projectId ID проекта в Brizy
     * @return array Метаданные проекта
     * @throws \Exception При ошибке API
     */
    public function getProjectMetadata($projectId): array;
    
    /**
     * Создать проект в Brizy
     * 
     * @param array $data Данные проекта (name, workspace_id, и т.д.)
     * @return array Ответ API с ID созданного проекта
     * @throws \Exception При ошибке создания
     */
    public function createProject(array $data): array;
    
    /**
     * Создать страницу в проекте
     * 
     * @param int $projectId ID проекта
     * @param array $data Данные страницы
     * @return array Ответ API с ID созданной страницы
     * @throws \Exception При ошибке создания
     */
    public function createPage(int $projectId, array $data): array;
    
    /**
     * Обновить страницу
     * 
     * @param int $projectId ID проекта
     * @param int $pageId ID страницы
     * @param array $data Данные для обновления
     * @return array Ответ API
     * @throws \Exception При ошибке обновления
     */
    public function updatePage(int $projectId, int $pageId, array $data): array;
    
    /**
     * Загрузить медиа файл в проект
     * 
     * @param int $projectId ID проекта
     * @param string $filePath Путь к файлу
     * @return array Ответ API с URL загруженного файла
     * @throws \Exception При ошибке загрузки
     */
    public function uploadMedia(int $projectId, string $filePath): array;
    
    // ... остальные методы из BrizyAPI
}
```

**Важно**:
- Все методы должны точно соответствовать сигнатурам в BrizyAPI
- Типы параметров и возвращаемых значений должны совпадать
- PHPDoc комментарии должны быть информативными

**Проверка**:
```bash
# Проверка синтаксиса
php -l lib/MBMigration/Contracts/BrizyAPIInterface.php
```

**Чеклист**:
- [ ] Файл интерфейса создан
- [ ] Все публичные методы BrizyAPI включены
- [ ] Сигнатуры методов совпадают
- [ ] PHPDoc комментарии добавлены
- [ ] Синтаксис проверен (нет ошибок)

---

### Задача 1.4: Изучить класс MBProjectDataCollector

**Время**: 30-60 минут  
**Приоритет**: ВЫСОКИЙ

**Что сделать**:
1. Открыть файл `lib/MBMigration/Layer/MB/MBProjectDataCollector.php`
2. Изучить все публичные методы
3. Записать список методов с их сигнатурами

**Методы для изучения**:
- `getProjectData()`
- `getPages()`
- `getPageData()`
- `getDesignSite()`
- И другие публичные методы

**Результат**: Список всех публичных методов

**Чеклист**:
- [ ] Класс изучен
- [ ] Список методов составлен

---

### Задача 1.5: Создать интерфейс MBProjectDataCollectorInterface

**Время**: 1-2 часа  
**Приоритет**: КРИТИЧЕСКИЙ  
**Файл**: `lib/MBMigration/Contracts/MBProjectDataCollectorInterface.php`

**Что сделать**:
1. Создать файл интерфейса
2. Добавить все публичные методы из MBProjectDataCollector
3. Добавить PHPDoc комментарии

**Пример структуры**:
```php
<?php

namespace MBMigration\Contracts;

interface MBProjectDataCollectorInterface
{
    /**
     * Получить данные проекта из Ministry Brands
     * 
     * @param string $projectUuid UUID проекта в MB
     * @return array Данные проекта
     * @throws \Exception При ошибке получения данных
     */
    public function getProjectData(string $projectUuid): array;
    
    /**
     * Получить список страниц проекта
     * 
     * @param string $projectUuid UUID проекта
     * @return array Массив страниц
     */
    public function getPages(string $projectUuid): array;
    
    /**
     * Получить данные конкретной страницы
     * 
     * @param string $projectUuid UUID проекта
     * @param string $pageSlug Slug страницы
     * @return array Данные страницы
     */
    public function getPageData(string $projectUuid, string $pageSlug): array;
    
    /**
     * Получить тему/дизайн проекта
     * 
     * @param string $projectUuid UUID проекта
     * @return string Название темы
     */
    public function getDesignSite(string $projectUuid): string;
    
    // ... остальные методы
}
```

**Проверка**:
```bash
php -l lib/MBMigration/Contracts/MBProjectDataCollectorInterface.php
```

**Чеклист**:
- [ ] Интерфейс создан
- [ ] Все публичные методы включены
- [ ] PHPDoc комментарии добавлены
- [ ] Синтаксис проверен

---

### Задача 1.6: Изучить классы Database (MySQL и PostgresSQL)

**Время**: 30-60 минут  
**Приоритет**: ВЫСОКИЙ

**Что сделать**:
1. Открыть файлы:
   - `lib/MBMigration/Layer/DataSource/driver/MySQL.php`
   - `lib/MBMigration/Layer/DataSource/driver/PostgresSQL.php`
2. Найти общие публичные методы
3. Определить, какие методы нужны для интерфейса

**Методы для изучения**:
- `query()`
- `insert()`
- `update()`
- `delete()`
- `beginTransaction()`
- `commit()`
- `rollback()`

**Результат**: Список общих методов с их сигнатурами

**Чеклист**:
- [ ] Оба класса изучены
- [ ] Общие методы определены

---

### Задача 1.7: Создать интерфейс DatabaseInterface

**Время**: 1 час  
**Приоритет**: ВЫСОКИЙ  
**Файл**: `lib/MBMigration/Contracts/DatabaseInterface.php`

**Что сделать**:
1. Создать интерфейс с общими методами
2. Убедиться, что методы подходят для обоих драйверов

**Пример**:
```php
<?php

namespace MBMigration\Contracts;

interface DatabaseInterface
{
    /**
     * Выполнить SQL запрос
     * 
     * @param string $sql SQL запрос
     * @param array $params Параметры для prepared statement
     * @return array Результат запроса
     * @throws \Exception При ошибке выполнения
     */
    public function query(string $sql, array $params = []): array;
    
    /**
     * Вставить запись в таблицу
     * 
     * @param string $table Название таблицы
     * @param array $data Данные для вставки
     * @return int ID вставленной записи
     * @throws \Exception При ошибке вставки
     */
    public function insert(string $table, array $data): int;
    
    /**
     * Обновить записи в таблице
     * 
     * @param string $table Название таблицы
     * @param array $data Данные для обновления
     * @param array $where Условия WHERE (ключ => значение)
     * @return int Количество обновленных записей
     * @throws \Exception При ошибке обновления
     */
    public function update(string $table, array $data, array $where): int;
    
    /**
     * Удалить записи из таблицы
     * 
     * @param string $table Название таблицы
     * @param array $where Условия WHERE
     * @return int Количество удаленных записей
     * @throws \Exception При ошибке удаления
     */
    public function delete(string $table, array $where): int;
    
    /**
     * Начать транзакцию
     * 
     * @return void
     * @throws \Exception При ошибке начала транзакции
     */
    public function beginTransaction(): void;
    
    /**
     * Закоммитить транзакцию
     * 
     * @return void
     * @throws \Exception При ошибке коммита
     */
    public function commit(): void;
    
    /**
     * Откатить транзакцию
     * 
     * @return void
     * @throws \Exception При ошибке отката
     */
    public function rollback(): void;
}
```

**Проверка**:
```bash
php -l lib/MBMigration/Contracts/DatabaseInterface.php
```

**Чеклист**:
- [ ] Интерфейс создан
- [ ] Все необходимые методы включены
- [ ] Методы подходят для MySQL и PostgreSQL
- [ ] Синтаксис проверен

---

### Задача 1.8: Изучить класс S3Uploader

**Время**: 30 минут  
**Приоритет**: СРЕДНИЙ

**Что сделать**:
1. Открыть файл `lib/MBMigration/Core/S3Uploader.php`
2. Изучить публичные методы
3. Записать список методов

**Чеклист**:
- [ ] Класс изучен
- [ ] Методы определены

---

### Задача 1.9: Создать интерфейс S3UploaderInterface

**Время**: 30 минут - 1 час  
**Приоритет**: СРЕДНИЙ  
**Файл**: `lib/MBMigration/Contracts/S3UploaderInterface.php`

**Что сделать**:
1. Создать интерфейс
2. Добавить все публичные методы из S3Uploader
3. Добавить PHPDoc комментарии

**Пример**:
```php
<?php

namespace MBMigration\Contracts;

interface S3UploaderInterface
{
    /**
     * Загрузить файл в S3
     * 
     * @param string $filePath Локальный путь к файлу
     * @param string $s3Key Ключ в S3 (путь)
     * @return string URL загруженного файла
     * @throws \Exception При ошибке загрузки
     */
    public function uploadFile(string $filePath, string $s3Key): string;
    
    // ... остальные методы
}
```

**Проверка**:
```bash
php -l lib/MBMigration/Contracts/S3UploaderInterface.php
```

**Чеклист**:
- [ ] Интерфейс создан
- [ ] Все методы включены
- [ ] Синтаксис проверен

---

### Задача 1.10: Проверить и расширить BrowserInterface

**Время**: 30 минут - 1 час  
**Приоритет**: СРЕДНИЙ  
**Файл**: `lib/MBMigration/Browser/BrowserInterface.php` (уже существует)

**Что сделать**:
1. Открыть существующий интерфейс
2. Проверить, все ли методы есть
3. Сравнить с реализацией `BrowserPHP`
4. Добавить недостающие методы, если нужно

**Проверка**:
```bash
# Проверить существующий интерфейс
cat lib/MBMigration/Browser/BrowserInterface.php

# Проверить реализацию
cat lib/MBMigration/Browser/BrowserPHP.php
```

**Чеклист**:
- [ ] Интерфейс проверен
- [ ] Все необходимые методы присутствуют
- [ ] Недостающие методы добавлены (если нужно)

---

### Задача 1.11: Проверить, что классы реализуют интерфейсы

**Время**: 30 минут  
**Приоритет**: ВЫСОКИЙ

**Что сделать**:
1. Обновить классы, чтобы они реализовывали интерфейсы:
   - `BrizyAPI implements BrizyAPIInterface`
   - `MBProjectDataCollector implements MBProjectDataCollectorInterface`
   - `MySQL implements DatabaseInterface`
   - `PostgresSQL implements DatabaseInterface`
   - `S3Uploader implements S3UploaderInterface`
   - `BrowserPHP implements BrowserInterface`

**Пример**:
```php
// lib/MBMigration/Layer/Brizy/BrizyAPI.php
namespace MBMigration\Layer\Brizy;

use MBMigration\Contracts\BrizyAPIInterface;

class BrizyAPI extends Utils implements BrizyAPIInterface
{
    // ... существующий код
}
```

**Проверка**:
```bash
# Проверить, что классы компилируются
php -l lib/MBMigration/Layer/Brizy/BrizyAPI.php
php -l lib/MBMigration/Layer/MB/MBProjectDataCollector.php
# и т.д.
```

**Чеклист**:
- [ ] BrizyAPI implements BrizyAPIInterface
- [ ] MBProjectDataCollector implements MBProjectDataCollectorInterface
- [ ] MySQL implements DatabaseInterface
- [ ] PostgresSQL implements DatabaseInterface
- [ ] S3Uploader implements S3UploaderInterface
- [ ] BrowserPHP implements BrowserInterface
- [ ] Все классы компилируются без ошибок

---

### Задача 1.12: Финальная проверка интерфейсов

**Время**: 30 минут  
**Приоритет**: ВЫСОКИЙ

**Что сделать**:
1. Проверить синтаксис всех интерфейсов
2. Убедиться, что все интерфейсы в правильном namespace
3. Проверить, что все классы реализуют интерфейсы
4. Запустить существующие тесты (если есть)

**Команды**:
```bash
# Проверить все интерфейсы
find lib/MBMigration/Contracts -name "*.php" -exec php -l {} \;

# Проверить, что классы реализуют интерфейсы
grep -r "implements" lib/MBMigration/Layer/
grep -r "implements" lib/MBMigration/Core/
grep -r "implements" lib/MBMigration/Browser/
```

**Чеклист**:
- [ ] Все интерфейсы синтаксически корректны
- [ ] Все классы реализуют интерфейсы
- [ ] Существующие тесты проходят (если есть)
- [ ] Нет ошибок компиляции

---

## 📊 Критерии завершения подэтапа

- [ ] Директория `lib/MBMigration/Contracts` создана
- [ ] Все интерфейсы созданы:
  - [ ] BrizyAPIInterface
  - [ ] MBProjectDataCollectorInterface
  - [ ] DatabaseInterface
  - [ ] S3UploaderInterface
  - [ ] BrowserInterface проверен/расширен
- [ ] Все классы реализуют соответствующие интерфейсы
- [ ] Все интерфейсы синтаксически корректны
- [ ] PHPDoc комментарии добавлены ко всем методам

---

## 🧪 Проверка готовности к следующему подэтапу

Перед переходом к Подэтапу 1.2 убедитесь:

1. ✅ Можно использовать интерфейсы в type hints:
   ```php
   function test(BrizyAPIInterface $api) {
       // Это должно работать
   }
   ```

2. ✅ Классы реализуют интерфейсы:
   ```php
   $api = new BrizyAPI();
   // $api должен быть instanceof BrizyAPIInterface
   ```

3. ✅ Нет ошибок компиляции

**Тест**:
```php
<?php
// test-interfaces.php
require 'vendor/autoload.php';

use MBMigration\Contracts\BrizyAPIInterface;
use MBMigration\Layer\Brizy\BrizyAPI;

$api = new BrizyAPI();
if ($api instanceof BrizyAPIInterface) {
    echo "✅ BrizyAPI implements BrizyAPIInterface\n";
} else {
    echo "❌ Error: BrizyAPI does not implement interface\n";
    exit(1);
}
```

Запустить: `php test-interfaces.php`

---

## ⚠️ Важные замечания

1. **Точность сигнатур** - методы в интерфейсах должны точно совпадать с методами в классах
2. **Типы данных** - использовать строгую типизацию (type hints)
3. **PHPDoc** - добавлять подробные комментарии для будущих разработчиков
4. **Обратная совместимость** - интерфейсы не должны ломать существующий код

---

## 🐛 Возможные проблемы

### Проблема: Методы в интерфейсе не совпадают с классом
**Решение**: 
- Проверить сигнатуры методов
- Убедиться, что типы параметров совпадают
- Проверить возвращаемые типы

### Проблема: Класс не компилируется после implements
**Решение**: 
- Проверить, что все методы интерфейса реализованы
- Проверить совпадение сигнатур
- Использовать `php -l` для проверки синтаксиса

---

## 📅 Примерный план по дням

**День 1**:
- Задачи 1.1-1.3 (директория, изучение, BrizyAPIInterface)

**День 2**:
- Задачи 1.4-1.7 (MBProjectDataCollectorInterface, DatabaseInterface)

**День 3**:
- Задачи 1.8-1.12 (S3UploaderInterface, BrowserInterface, проверки)

---

**Следующий подэтап**: [Подэтап 1.2: Рефакторинг Core классов](./substage-1.2-core-refactoring.md)
