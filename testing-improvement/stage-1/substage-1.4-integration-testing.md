# Подэтап 1.4: Интеграция и тестирование

## 📋 Обзор подэтапа

**Длительность**: 2-3 дня  
**Приоритет**: КРИТИЧЕСКИЙ  
**Зависимости**: Подэтапы 1.1, 1.2, 1.3 (все рефакторинги должны быть завершены)

## 🎯 Цель подэтапа

Завершить рефакторинг, интегрировать все изменения и протестировать, что ничего не сломалось.

## ❌ Проблема

После рефакторинга нужно:
- Обновить ApplicationBootstrapper для создания зависимостей
- Убедиться, что все работает вместе
- Протестировать полный цикл миграции

## ✅ Решение

1. Обновить ApplicationBootstrapper
2. Опционально создать DI контейнер
3. Протестировать все изменения
4. Убедиться, что миграция работает

---

## 📝 Задачи подэтапа

### Задача 4.1: Изучить ApplicationBootstrapper

**Время**: 1-2 часа  
**Приоритет**: КРИТИЧЕСКИЙ

**Что сделать**:
1. Открыть файл `lib/MBMigration/ApplicationBootstrapper.php`
2. Найти метод `migrationFlow()` или место создания MigrationPlatform
3. Изучить, как создается MigrationPlatform
4. Определить, какие зависимости нужно создать

**Чеклист**:
- [ ] ApplicationBootstrapper изучен
- [ ] Место создания MigrationPlatform найдено
- [ ] Зависимости определены

---

### Задача 4.2: Обновить ApplicationBootstrapper

**Время**: 2-3 часа  
**Приоритет**: КРИТИЧЕСКИЙ  
**Файл**: `lib/MBMigration/ApplicationBootstrapper.php`

**Что сделать**:
1. Найти место создания MigrationPlatform
2. Создать все необходимые зависимости перед созданием MigrationPlatform
3. Передать зависимости в конструктор

**Пример**:
```php
// До
$platform = new MigrationPlatform(
    $config,
    $logger,
    $buildPage,
    $workspacesId,
    // ...
);

// После
// Создать зависимости
$brizyApi = new BrizyAPI($config);
$mbCollector = new MBProjectDataCollector($config);

$platform = new MigrationPlatform(
    $config,
    $logger,
    $brizyApi,
    $mbCollector,
    $buildPage,
    $workspacesId,
    // ...
);
```

**Важно**:
- Сохранить всю логику инициализации
- Не ломать существующий функционал
- Проверить все параметры

**Проверка**:
```bash
php -l lib/MBMigration/ApplicationBootstrapper.php
```

**Чеклист**:
- [ ] ApplicationBootstrapper обновлен
- [ ] Все зависимости создаются перед MigrationPlatform
- [ ] Синтаксис проверен

---

### Задача 4.3: Обновить другие места создания MigrationPlatform

**Время**: 1-2 часа  
**Приоритет**: ВЫСОКИЙ

**Что сделать**:
1. Найти все оставшиеся места создания MigrationPlatform
2. Обновить их аналогично ApplicationBootstrapper

**Команды**:
```bash
# Найти все создания
grep -r "new MigrationPlatform" lib/ public/ dashboard/
```

**Основные места**:
- `lib/MBMigration/Bridge/Bridge.php`
- Другие места (если есть)

**Чеклист**:
- [ ] Все места найдены
- [ ] Все места обновлены
- [ ] Код компилируется

---

### Задача 4.4: Создать DI контейнер (опционально)

**Время**: 4-6 часов  
**Приоритет**: НИЗКИЙ (можно отложить)

**Что сделать**:
1. Установить PHP-DI: `composer require php-di/php-di`
2. Создать конфигурацию DI
3. Настроить автоматическое создание объектов

**Установка**:
```bash
composer require php-di/php-di
```

**Создать файл**: `lib/MBMigration/Container.php`

**Пример**:
```php
<?php

namespace MBMigration;

use DI\Container;
use DI\ContainerBuilder;
use MBMigration\Contracts\BrizyAPIInterface;
use MBMigration\Contracts\MBProjectDataCollectorInterface;
use MBMigration\Contracts\DatabaseInterface;
use MBMigration\Layer\Brizy\BrizyAPI;
use MBMigration\Layer\MB\MBProjectDataCollector;
use MBMigration\Layer\DataSource\driver\MySQL;
use MBMigration\Core\Config;

class ContainerFactory
{
    public static function create(Config $config): Container
    {
        $builder = new ContainerBuilder();
        
        // Настройка контейнера
        $builder->addDefinitions([
            // Интерфейсы -> Реализации
            BrizyAPIInterface::class => function() use ($config) {
                return new BrizyAPI($config);
            },
            
            MBProjectDataCollectorInterface::class => function() use ($config) {
                return new MBProjectDataCollector($config);
            },
            
            DatabaseInterface::class => function() use ($config) {
                return new MySQL($config);
            },
            
            // Другие зависимости
            Config::class => $config,
        ]);
        
        return $builder->build();
    }
}
```

**Использование**:
```php
$container = ContainerFactory::create($config);
$platform = $container->get(MigrationPlatform::class);
```

**Чеклист**:
- [ ] PHP-DI установлен
- [ ] ContainerFactory создан
- [ ] Конфигурация настроена
- [ ] Контейнер работает

---

### Задача 4.5: Проверка синтаксиса всех файлов

**Время**: 30 минут  
**Приоритет**: ВЫСОКИЙ

**Что сделать**:
1. Проверить синтаксис всех измененных файлов
2. Исправить найденные ошибки

**Команды**:
```bash
# Проверить все PHP файлы
find lib/MBMigration -name "*.php" -exec php -l {} \;
find dashboard/api -name "*.php" -exec php -l {} \;

# Или использовать более детальную проверку
php -l lib/MBMigration/ApplicationBootstrapper.php
php -l lib/MBMigration/MigrationPlatform.php
php -l lib/MBMigration/Builder/PageController.php
php -l lib/MBMigration/Bridge/Bridge.php
php -l dashboard/api/services/DatabaseService.php
```

**Чеклист**:
- [ ] Все файлы проверены
- [ ] Нет ошибок синтаксиса
- [ ] Все ошибки исправлены

---

### Задача 4.6: Запустить существующие тесты

**Время**: 30 минут - 1 час  
**Приоритет**: ВЫСОКИЙ

**Что сделать**:
1. Запустить все существующие тесты
2. Убедиться, что они проходят
3. Исправить сломанные тесты (если есть)

**Команды**:
```bash
# Запустить все тесты
composer test

# Или напрямую
vendor/bin/phpunit -c phpunit.xml.dist
```

**Чеклист**:
- [ ] Все тесты запущены
- [ ] Все тесты проходят
- [ ] Сломанные тесты исправлены (если были)

---

### Задача 4.7: Тестирование миграции вручную

**Время**: 1-2 часа  
**Приоритет**: КРИТИЧЕСКИЙ

**Что сделать**:
1. Попробовать запустить миграцию вручную (если возможно)
2. Проверить, что все этапы работают
3. Проверить логи на ошибки

**Важно**: 
- Использовать тестовые данные
- Не запускать на production данных
- Проверить все этапы миграции

**Пример запроса**:
```bash
# Если есть тестовый endpoint
curl "http://localhost/?mb_project_uuid=test-uuid&brz_project_id=12345"
```

**Проверка логов**:
```bash
# Проверить логи на ошибки
tail -f var/log/migration.log
```

**Чеклист**:
- [ ] Миграция запускается
- [ ] Все этапы работают
- [ ] Нет критических ошибок в логах

---

### Задача 4.8: Тестирование Dashboard API

**Время**: 30 минут - 1 час  
**Приоритет**: ВЫСОКИЙ

**Что сделать**:
1. Проверить все API endpoints
2. Убедиться, что они работают
3. Проверить логи на ошибки

**Команды**:
```bash
# Health check
curl http://localhost:8000/dashboard/api/health

# Список миграций
curl http://localhost:8000/dashboard/api/migrations

# Детали миграции
curl http://localhost:8000/dashboard/api/migrations/12345
```

**Чеклист**:
- [ ] Все endpoints работают
- [ ] Нет ошибок в ответах
- [ ] Логи чистые

---

### Задача 4.9: Создать тест с моками

**Время**: 1-2 часа  
**Приоритет**: ВЫСОКИЙ

**Что сделать**:
1. Создать простой тест, который использует моки
2. Убедиться, что рефакторинг позволяет использовать моки

**Создать файл**: `tests/Unit/MBMigration/RefactoringTest.php`

**Пример**:
```php
<?php

namespace Tests\Unit\MBMigration;

use MBMigration\MigrationPlatform;
use MBMigration\Core\Config;
use MBMigration\Contracts\BrizyAPIInterface;
use MBMigration\Contracts\MBProjectDataCollectorInterface;
use Monolog\Logger;
use Monolog\Handler\NullHandler;
use PHPUnit\Framework\TestCase;

class RefactoringTest extends TestCase
{
    public function testMigrationPlatformAcceptsInterfaces(): void
    {
        // Arrange
        $config = new Config();
        $logger = new Logger('test');
        $logger->pushHandler(new NullHandler());
        
        // Создать моки (пока простые заглушки)
        $brizyMock = $this->createMock(BrizyAPIInterface::class);
        $mbMock = $this->createMock(MBProjectDataCollectorInterface::class);
        
        // Act
        $platform = new MigrationPlatform(
            $config,
            $logger,
            $brizyMock,
            $mbMock
        );
        
        // Assert
        $this->assertInstanceOf(MigrationPlatform::class, $platform);
    }
}
```

**Запустить тест**:
```bash
vendor/bin/phpunit tests/Unit/MBMigration/RefactoringTest.php
```

**Чеклист**:
- [ ] Тест создан
- [ ] Тест проходит
- [ ] Моки работают

---

### Задача 4.10: Финальная проверка всего рефакторинга

**Время**: 1-2 часа  
**Приоритет**: КРИТИЧЕСКИЙ

**Что сделать**:
1. Проверить все критерии завершения этапа
2. Убедиться, что все задачи выполнены
3. Создать отчет о проделанной работе

**Чеклист проверки**:
- [ ] Все интерфейсы созданы
- [ ] Все классы реализуют интерфейсы
- [ ] MigrationPlatform рефакторен
- [ ] PageController рефакторен
- [ ] Bridge рефакторен
- [ ] Dashboard API сервисы рефакторены
- [ ] ApplicationBootstrapper обновлен
- [ ] Код компилируется без ошибок
- [ ] Все тесты проходят
- [ ] Миграция работает вручную
- [ ] Dashboard API работает
- [ ] Можно создать моки и использовать их

**Создать отчет**:
```markdown
# Отчет о рефакторинге Этапа 1

## Выполненные задачи:
- [x] Все интерфейсы созданы
- [x] MigrationPlatform рефакторен
- [x] PageController рефакторен
- [x] Bridge рефакторен
- [x] Dashboard API рефакторен
- [x] ApplicationBootstrapper обновлен

## Проблемы:
- Нет критических проблем

## Готовность к Этапу 2:
✅ Да, можно переходить к созданию тестовой инфраструктуры
```

**Чеклист**:
- [ ] Все критерии проверены
- [ ] Отчет создан
- [ ] Готовность к следующему этапу подтверждена

---

## 📊 Критерии завершения подэтапа

- [ ] ApplicationBootstrapper обновлен
- [ ] Все места создания MigrationPlatform обновлены
- [ ] Код компилируется без ошибок
- [ ] Все существующие тесты проходят
- [ ] Миграция работает вручную
- [ ] Dashboard API работает
- [ ] Можно создать моки и использовать их
- [ ] Создан тест с моками (доказательство работы)

---

## 🧪 Проверка готовности к следующему этапу

Перед переходом к Этапу 2 убедитесь:

1. ✅ Можно создать мок для любого интерфейса:
   ```php
   $brizyMock = $this->createMock(BrizyAPIInterface::class);
   ```

2. ✅ Можно передать моки в классы:
   ```php
   $platform = new MigrationPlatform($config, $logger, $brizyMock, $mbMock);
   ```

3. ✅ Код работает с реальными зависимостями:
   ```php
   $brizyApi = new BrizyAPI($config);
   $platform = new MigrationPlatform($config, $logger, $brizyApi, $mbCollector);
   ```

4. ✅ Все тесты проходят

**Финальный тест**:
```php
<?php
// test-final-refactoring.php
require 'vendor/autoload.php';

use MBMigration\MigrationPlatform;
use MBMigration\Core\Config;
use MBMigration\Contracts\BrizyAPIInterface;
use MBMigration\Contracts\MBProjectDataCollectorInterface;
use Monolog\Logger;
use Monolog\Handler\NullHandler;

$config = new Config();
$logger = new Logger('test');
$logger->pushHandler(new NullHandler());

// Тест 1: С реальными зависимостями
echo "Тест 1: Реальные зависимости\n";
try {
    $brizyApi = new \MBMigration\Layer\Brizy\BrizyAPI();
    $mbCollector = new \MBMigration\Layer\MB\MBProjectDataCollector($config);
    $platform = new MigrationPlatform($config, $logger, $brizyApi, $mbCollector);
    echo "✅ Успешно создан с реальными зависимостями\n";
} catch (Exception $e) {
    echo "❌ Ошибка: " . $e->getMessage() . "\n";
    exit(1);
}

// Тест 2: С моками (используя PHPUnit моки)
echo "\nТест 2: Моки\n";
try {
    $brizyMock = $this->createMock(BrizyAPIInterface::class);
    $mbMock = $this->createMock(MBProjectDataCollectorInterface::class);
    $platform = new MigrationPlatform($config, $logger, $brizyMock, $mbMock);
    echo "✅ Успешно создан с моками\n";
} catch (Exception $e) {
    echo "❌ Ошибка: " . $e->getMessage() . "\n";
    exit(1);
}

echo "\n🎉 Все тесты пройдены! Рефакторинг завершен успешно!\n";
```

---

## ⚠️ Важные замечания

1. **Не торопиться** - лучше потратить больше времени на тестирование, чем потом исправлять баги
2. **Тестировать все сценарии** - не только happy path
3. **Проверять логи** - могут быть скрытые проблемы
4. **Документировать изменения** - для будущих разработчиков

---

## 🐛 Возможные проблемы

### Проблема: Миграция не работает после рефакторинга
**Решение**: 
- Проверить создание всех зависимостей
- Проверить логи на ошибки
- Убедиться, что все параметры передаются правильно

### Проблема: Тесты не проходят
**Решение**: 
- Проверить, что тесты обновлены под новый API
- Убедиться, что все зависимости создаются правильно
- Проверить use statements в тестах

### Проблема: API не работает
**Решение**: 
- Проверить создание зависимостей в index.php
- Проверить логи
- Убедиться, что все сервисы создаются правильно

---

## 📅 Примерный план по дням

**День 1**:
- Задачи 4.1-4.3 (ApplicationBootstrapper)
- Задача 4.5 (проверка синтаксиса)

**День 2**:
- Задачи 4.6-4.8 (тестирование)
- Задача 4.9 (тест с моками)

**День 3**:
- Задача 4.10 (финальная проверка)
- Задача 4.4 (DI контейнер, опционально)
- Исправление проблем

---

## 🎉 Завершение Этапа 1

После завершения всех подэтапов:

✅ **Код готов к тестированию**:
- Все интерфейсы созданы
- Все классы используют интерфейсы
- Можно подставлять моки

✅ **Ничего не сломано**:
- Все тесты проходят
- Миграция работает
- API работает

✅ **Готовность к Этапу 2**:
- Можно создавать тестовую инфраструктуру
- Можно писать тесты с моками

---

**Следующий этап**: [Этап 2: Создание тестовой инфраструктуры](../stage-2-test-infrastructure.md)
