# Запуск тестов

## 🐳 Запуск тестов в Docker контейнере (рекомендуется)

Проект использует Docker контейнер `mg_migration` для запуска тестов. Это гарантирует одинаковое окружение для всех разработчиков.

### Предварительные требования

1. Docker и Docker Compose должны быть установлены
2. Контейнер `mg_migration` должен быть запущен:
   ```bash
   docker ps | grep mg_migration
   ```

### Команды для запуска тестов

#### Запустить все тесты
```bash
docker exec mg_migration composer test
```

#### Запустить конкретный тест
```bash
docker exec mg_migration vendor/bin/phpunit tests/MBMigration/Contracts/ContractsDirectoryTest.php
docker exec mg_migration vendor/bin/phpunit tests/MBMigration/Layer/Brizy/BrizyAPIAnalysisTest.php
```

#### Запустить тесты с покрытием кода
```bash
docker exec mg_migration composer test:coverage
```

#### Запустить тесты из конкретной директории
```bash
docker exec mg_migration vendor/bin/phpunit tests/MBMigration/Contracts/
```

#### Запустить тесты с подробным выводом
```bash
docker exec mg_migration vendor/bin/phpunit --verbose tests/
```

### Примеры запуска тестов для задач

#### Задача 1.1: Проверка директории Contracts
```bash
docker exec mg_migration vendor/bin/phpunit tests/MBMigration/Contracts/ContractsDirectoryTest.php
```

**Ожидаемый результат**:
```
OK (5 tests, 7 assertions)
```

#### Задача 1.2: Проверка анализа BrizyAPI
```bash
docker exec mg_migration vendor/bin/phpunit tests/MBMigration/Layer/Brizy/BrizyAPIAnalysisTest.php
```

**Ожидаемый результат**:
```
OK (7 tests, 56 assertions)
```

---

## 💻 Запуск тестов локально (без Docker)

Если у вас установлен PHP 7.4+ и все зависимости, можно запускать тесты локально:

```bash
# Установить зависимости
composer install

# Запустить все тесты
composer test

# Запустить конкретный тест
vendor/bin/phpunit tests/MBMigration/Contracts/ContractsDirectoryTest.php
```

**⚠️ Внимание**: Локальное окружение может отличаться от контейнера, поэтому рекомендуется использовать Docker.

---

## 🔧 Устранение проблем

### Проблема: Контейнер не запущен
```bash
# Проверить статус
docker ps -a | grep mg_migration

# Запустить контейнер
docker compose up -d migration
```

### Проблема: Тесты не находят файлы
Убедитесь, что пути в тестах правильные. В контейнере рабочая директория - `/project`.

### Проблема: Ошибки с Logger
Некоторые тесты требуют инициализации Logger. Проверьте `tests/bootstrap.php`.

### Проблема: Xdebug предупреждения
Предупреждения Xdebug о подключении к отладчику можно игнорировать - они не влияют на выполнение тестов.

---

## 📊 Структура тестов

```
tests/
├── bootstrap.php                          # Инициализация окружения
├── MBMigration/
│   ├── Contracts/
│   │   └── ContractsDirectoryTest.php    # Тест для задачи 1.1
│   ├── Layer/
│   │   └── Brizy/
│   │       └── BrizyAPIAnalysisTest.php  # Тест для задачи 1.2
│   └── Builder/
│       └── ...
```

---

## ✅ Чеклист перед коммитом

- [ ] Все тесты проходят: `docker exec mg_migration composer test`
- [ ] Новые тесты добавлены и проходят
- [ ] Тесты имеют понятные имена и комментарии
- [ ] Тесты следуют принципам из `PRINCIPLES.md`

---

**Последнее обновление**: 2025-01-27
