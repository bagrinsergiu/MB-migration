# Подэтап 1.2: Рефакторинг Core классов

## 📋 Обзор подэтапа

**Длительность**: 4-5 дней  
**Приоритет**: КРИТИЧЕСКИЙ  
**Зависимости**: Подэтап 1.1 (интерфейсы должны быть созданы)

## 🎯 Цель подэтапа

Рефакторить основные классы системы миграции, чтобы они использовали интерфейсы вместо прямых зависимостей.

## ❌ Проблема

Основные классы создают зависимости напрямую:
- `MigrationPlatform` создает `BrizyAPI` и `MBProjectDataCollector`
- `PageController` создает зависимости напрямую
- `Bridge` создает зависимости напрямую

Невозможно подставить моки для тестирования.

## ✅ Решение

Изменить конструкторы классов, чтобы они принимали интерфейсы вместо создания объектов.

---

## 📝 Задачи подэтапа

### Задача 2.1: Изучить MigrationPlatform

**Время**: 1-2 часа  
**Приоритет**: ВЫСОКИЙ

**Что сделать**:
1. Открыть файл `lib/MBMigration/MigrationPlatform.php`
2. Найти конструктор
3. Найти все места, где создаются зависимости:
   - `new BrizyAPI()`
   - `new MBProjectDataCollector()`
   - Другие прямые создания объектов
4. Записать список всех зависимостей

**Результат**: Список всех зависимостей, которые нужно инжектировать

**Чеклист**:
- [ ] Конструктор изучен
- [ ] Все зависимости найдены
- [ ] Список зависимостей составлен

---

### Задача 2.2: Рефакторинг конструктора MigrationPlatform

**Время**: 2-3 часа  
**Приоритет**: КРИТИЧЕСКИЙ  
**Файл**: `lib/MBMigration/MigrationPlatform.php`

**Что сделать**:
1. Изменить конструктор - добавить параметры для интерфейсов
2. Убрать прямые создания объектов
3. Сохранить обратную совместимость (если возможно)

**До**:
```php
public function __construct(
    Config $config,
    LoggerInterface $logger,
    $buildPage = '',
    $workspacesId = 0,
    bool $mMgrIgnore = true,
    $mgr_manual = false,
    bool $qualityAnalysis = false,
    string $mb_element_name = '',
    bool $skip_media_upload = false,
    bool $skip_cache = false
) {
    $this->brizyApi = new BrizyAPI($config);
    $this->parser = new MBProjectDataCollector($config);
    // ...
}
```

**После**:
```php
public function __construct(
    Config $config,
    LoggerInterface $logger,
    BrizyAPIInterface $brizyApi,
    MBProjectDataCollectorInterface $mbCollector,
    $buildPage = '',
    $workspacesId = 0,
    bool $mMgrIgnore = true,
    $mgr_manual = false,
    bool $qualityAnalysis = false,
    string $mb_element_name = '',
    bool $skip_media_upload = false,
    bool $skip_cache = false
) {
    $this->brizyApi = $brizyApi;
    $this->parser = $mbCollector;
    // ...
}
```

**Важно**:
- Добавить use statements для интерфейсов
- Убрать все `new` создания зависимостей
- Сохранить логику инициализации

**Проверка**:
```bash
# Проверить синтаксис
php -l lib/MBMigration/MigrationPlatform.php
```

**Чеклист**:
- [ ] Конструктор изменен
- [ ] Интерфейсы добавлены в use statements
- [ ] Прямые создания объектов убраны
- [ ] Синтаксис проверен

---

### Задача 2.3: Найти все места создания MigrationPlatform

**Время**: 30-60 минут  
**Приоритет**: ВЫСОКИЙ

**Что сделать**:
1. Найти все места, где создается `new MigrationPlatform()`
2. Записать список файлов и строк

**Команды**:
```bash
# Найти все создания MigrationPlatform
grep -r "new MigrationPlatform" lib/ dashboard/ public/
```

**Результат**: Список всех мест создания MigrationPlatform

**Чеклист**:
- [ ] Все места найдены
- [ ] Список составлен

---

### Задача 2.4: Обновить места создания MigrationPlatform

**Время**: 2-3 часа  
**Приоритет**: КРИТИЧЕСКИЙ

**Что сделать**:
1. Для каждого места создания MigrationPlatform:
   - Создать объекты зависимостей перед созданием MigrationPlatform
   - Передать их в конструктор

**Основные места**:
- `lib/MBMigration/ApplicationBootstrapper.php`
- `lib/MBMigration/Bridge/Bridge.php`
- Другие места (если есть)

**Пример**:
```php
// До
$platform = new MigrationPlatform($config, $logger, ...);

// После
$brizyApi = new BrizyAPI($config);
$mbCollector = new MBProjectDataCollector($config);
$platform = new MigrationPlatform(
    $config,
    $logger,
    $brizyApi,
    $mbCollector,
    ...
);
```

**Важно**:
- Не ломать существующую логику
- Сохранить все параметры
- Проверить после каждого изменения

**Чеклист**:
- [ ] ApplicationBootstrapper обновлен
- [ ] Bridge обновлен
- [ ] Все другие места обновлены
- [ ] Код компилируется

---

### Задача 2.5: Тестирование MigrationPlatform

**Время**: 1-2 часа  
**Приоритет**: ВЫСОКИЙ

**Что сделать**:
1. Запустить существующие тесты: `composer test`
2. Попробовать запустить миграцию вручную (если возможно)
3. Проверить логи на ошибки

**Команды**:
```bash
# Запустить тесты
composer test

# Проверить синтаксис всех файлов
find lib/MBMigration -name "*.php" -exec php -l {} \;
```

**Чеклист**:
- [ ] Все тесты проходят
- [ ] Нет ошибок компиляции
- [ ] Миграция работает (если тестируется вручную)

---

### Задача 2.6: Изучить PageController

**Время**: 1-2 часа  
**Приоритет**: ВЫСОКИЙ

**Что сделать**:
1. Открыть файл `lib/MBMigration/Builder/PageController.php`
2. Найти конструктор
3. Найти все прямые создания зависимостей
4. Определить, какие интерфейсы нужны

**Зависимости для поиска**:
- BrowserInterface
- BrizyAPIInterface
- Другие зависимости

**Чеклист**:
- [ ] Конструктор изучен
- [ ] Все зависимости найдены

---

### Задача 2.7: Рефакторинг PageController

**Время**: 2-3 часа  
**Приоритет**: ВЫСОКИЙ  
**Файл**: `lib/MBMigration/Builder/PageController.php`

**Что сделать**:
1. Изменить конструктор - добавить параметры для интерфейсов
2. Убрать прямые создания объектов
3. Обновить use statements

**Пример**:
```php
// До
public function __construct(...) {
    $this->browser = BrowserPHP::instance(...);
    $this->brizyApi = new BrizyAPI(...);
}

// После
public function __construct(
    ...,
    BrowserInterface $browser,
    BrizyAPIInterface $brizyApi
) {
    $this->browser = $browser;
    $this->brizyApi = $brizyApi;
}
```

**Проверка**:
```bash
php -l lib/MBMigration/Builder/PageController.php
```

**Чеклист**:
- [ ] Конструктор изменен
- [ ] Интерфейсы добавлены
- [ ] Прямые создания убраны
- [ ] Синтаксис проверен

---

### Задача 2.8: Найти и обновить места создания PageController

**Время**: 1-2 часа  
**Приоритет**: ВЫСОКИЙ

**Что сделать**:
1. Найти все места создания PageController
2. Обновить их для передачи зависимостей

**Команды**:
```bash
grep -r "new PageController" lib/
```

**Чеклист**:
- [ ] Все места найдены
- [ ] Все места обновлены
- [ ] Код компилируется

---

### Задача 2.9: Изучить Bridge

**Время**: 1-2 часа  
**Приоритет**: СРЕДНИЙ

**Что сделать**:
1. Открыть файл `lib/MBMigration/Bridge/Bridge.php`
2. Найти конструктор
3. Найти все прямые создания зависимостей
4. Определить, какие интерфейсы нужны

**Чеклист**:
- [ ] Конструктор изучен
- [ ] Зависимости найдены

---

### Задача 2.10: Рефакторинг Bridge

**Время**: 2-3 часа  
**Приоритет**: СРЕДНИЙ  
**Файл**: `lib/MBMigration/Bridge/Bridge.php`

**Что сделать**:
1. Изменить конструктор
2. Убрать прямые создания зависимостей
3. Обновить все места создания Bridge

**Важно**:
- Bridge - большой класс (1079 строк)
- Делать изменения аккуратно
- Тестировать после каждого изменения

**Чеклист**:
- [ ] Конструктор изменен
- [ ] Зависимости инжектируются
- [ ] Все места создания обновлены
- [ ] Код компилируется

---

### Задача 2.11: Финальная проверка Core классов

**Время**: 1-2 часа  
**Приоритет**: ВЫСОКИЙ

**Что сделать**:
1. Проверить синтаксис всех измененных файлов
2. Запустить существующие тесты
3. Проверить, что ничего не сломалось

**Команды**:
```bash
# Проверить синтаксис
find lib/MBMigration -name "*.php" -exec php -l {} \;

# Запустить тесты
composer test

# Проверить основные файлы
php -l lib/MBMigration/MigrationPlatform.php
php -l lib/MBMigration/Builder/PageController.php
php -l lib/MBMigration/Bridge/Bridge.php
```

**Чеклист**:
- [ ] Все файлы синтаксически корректны
- [ ] Все тесты проходят
- [ ] Нет ошибок компиляции
- [ ] Основная функциональность работает

---

## 📊 Критерии завершения подэтапа

- [ ] MigrationPlatform рефакторен и использует интерфейсы
- [ ] PageController рефакторен и использует интерфейсы
- [ ] Bridge рефакторен и использует интерфейсы
- [ ] Все места создания классов обновлены
- [ ] Код компилируется без ошибок
- [ ] Существующие тесты проходят
- [ ] Можно создать моки и передать их в классы

---

## 🧪 Проверка готовности к следующему подэтапу

Перед переходом к Подэтапу 1.3 убедитесь:

1. ✅ Можно создать MigrationPlatform с моками:
   ```php
   $brizyMock = new BrizyAPIMock();
   $mbMock = new MBProjectDataCollectorMock();
   $platform = new MigrationPlatform($config, $logger, $brizyMock, $mbMock);
   ```

2. ✅ Код работает с реальными зависимостями:
   ```php
   $brizyApi = new BrizyAPI($config);
   $mbCollector = new MBProjectDataCollector($config);
   $platform = new MigrationPlatform($config, $logger, $brizyApi, $mbCollector);
   ```

3. ✅ Все тесты проходят

**Тест**:
```php
<?php
// test-refactoring.php
require 'vendor/autoload.php';

use MBMigration\MigrationPlatform;
use MBMigration\Core\Config;
use Monolog\Logger;
use Monolog\Handler\NullHandler;

// Создать реальные зависимости
$config = new Config();
$logger = new Logger('test');
$logger->pushHandler(new NullHandler());

// Это должно работать после рефакторинга
$brizyApi = new \MBMigration\Layer\Brizy\BrizyAPI();
$mbCollector = new \MBMigration\Layer\MB\MBProjectDataCollector($config);

try {
    $platform = new MigrationPlatform(
        $config,
        $logger,
        $brizyApi,
        $mbCollector
    );
    echo "✅ MigrationPlatform создан успешно\n";
} catch (Exception $e) {
    echo "❌ Ошибка: " . $e->getMessage() . "\n";
    exit(1);
}
```

---

## ⚠️ Важные замечания

1. **Делать по одному классу** - не рефакторить все сразу
2. **Коммитить часто** - после каждого успешного изменения
3. **Тестировать после каждого изменения** - не накапливать проблемы
4. **Сохранять обратную совместимость** - если возможно
5. **Не ломать существующий функционал** - проверять после каждого изменения

---

## 🐛 Возможные проблемы

### Проблема: Слишком много параметров в конструкторе
**Решение**: 
- Создать конфигурационные объекты (DTO)
- Использовать builder pattern
- Или использовать DI контейнер (в следующем подэтапе)

### Проблема: Циклические зависимости
**Решение**: 
- Использовать lazy loading
- Пересмотреть архитектуру
- Разделить на более мелкие классы

### Проблема: Старые места создания объектов
**Решение**: 
- Использовать поиск по проекту (`grep`)
- Проверить все файлы, которые используют класс
- Обновить все места

### Проблема: Код не компилируется
**Решение**: 
- Проверить use statements
- Проверить типы параметров
- Проверить, что все интерфейсы подключены

---

## 📅 Примерный план по дням

**День 1**:
- Задачи 2.1-2.5 (MigrationPlatform)

**День 2**:
- Задачи 2.6-2.8 (PageController)

**День 3**:
- Задачи 2.9-2.10 (Bridge)

**День 4-5**:
- Задача 2.11 (финальная проверка)
- Исправление проблем
- Дополнительное тестирование

---

**Следующий подэтап**: [Подэтап 1.3: Рефакторинг Dashboard API](./substage-1.3-dashboard-refactoring.md)
