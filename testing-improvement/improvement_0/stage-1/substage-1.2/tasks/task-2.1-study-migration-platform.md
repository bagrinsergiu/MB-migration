# Задача 2.1: Изучить MigrationPlatform

## 📋 Информация о задаче

**Время выполнения**: 1-2 часа  
**Приоритет**: ВЫСОКИЙ  
**Зависимости**: Подэтап 1.1 (интерфейсы должны быть созданы)

## 🎯 Принципы работы

⚠️ **ВАЖНО**: При выполнении этой задачи следуйте принципам из [PRINCIPLES.md](../../../../PRINCIPLES.md):

1. ✅ **Сразу тестировать** - после изучения написать тест понимания
2. 🤖 **AI-friendly** - четко документировать найденное
3. 👤 **Human-friendly** - структурировать информацию понятно
4. 💬 **Везде комментарии** - в документе с результатами добавить комментарии
5. 🧪 **Реальные данные** - изучить РЕАЛЬНЫЙ код, НЕ придумывать!

## ❌ Проблема

Перед рефакторингом класса `MigrationPlatform` необходимо тщательно изучить его реальную структуру, чтобы:

1. **Понять текущий конструктор** - какие параметры он принимает, что делает
2. **Найти все места создания зависимостей** - где в классе создаются объекты через `new`
3. **Определить все зависимости** - какие классы создаются напрямую
4. **Понять порядок инициализации** - когда и где создаются зависимости
5. **Выявить связи между методами** - какие методы используют созданные зависимости

Без детального изучения РЕАЛЬНОГО кода невозможно правильно провести рефакторинг. Неправильный рефакторинг может сломать существующую функциональность.

⚠️ **КРИТИЧЕСКИ ВАЖНО**: Изучить РЕАЛЬНЫЙ код, не предполагать и не придумывать!

## 🎯 Ожидаемый результат

После выполнения задачи должен быть создан документ `MIGRATION_PLATFORM_ANALYSIS.md`, содержащий:

1. **Описание конструктора** - точная сигнатура из реального кода, все параметры с их типами
2. **Список всех мест создания зависимостей** - где в классе используется `new` для создания объектов:
   - В конструкторе (если есть)
   - В методах init(), run(), и других методах
   - Точные строки кода и названия методов
3. **Список всех зависимостей** - какие классы создаются:
   - BrizyAPI
   - MBProjectDataCollector
   - PageController
   - Другие классы (если есть)
4. **Порядок создания зависимостей** - когда и в каком порядке они создаются
5. **Использование зависимостей** - где и как используются созданные объекты
6. **Свойства класса** - какие свойства хранят зависимости

Этот документ будет использоваться в следующих задачах для правильного рефакторинга.

## 📝 Что нужно сделать

### Шаг 1: Открыть и изучить реальный файл

⚠️ **НЕ ПРИДУМЫВАТЬ!** Открыть реальный файл: `lib/MBMigration/MigrationPlatform.php`

**Что сделать**:
1. Открыть файл в редакторе
2. Прочитать весь файл от начала до конца
3. Понять структуру класса - свойства, методы, traits

### Шаг 2: Изучить конструктор

**Что сделать**:
1. Найти метод `__construct()` в файле
2. Записать точную сигнатуру конструктора:
   - Все параметры с их типами (если указаны)
   - Значения по умолчанию (если есть)
   - Порядок параметров
3. Изучить тело конструктора:
   - Что делается в конструкторе
   - Создаются ли там зависимости через `new`
   - Инициализируются ли свойства

⚠️ **ВАЖНО**: Записывать ТОЧНО то, что есть в коде, не предполагать!

### Шаг 3: Найти все места создания зависимостей

**Что сделать**:
1. Использовать поиск по файлу: `new ` (с пробелом)
2. Для каждого найденного `new`:
   - Записать название класса
   - Записать метод, в котором это происходит
   - Записать строку кода (или номер строки)
   - Записать параметры конструктора (если есть)
3. Проверить все методы класса:
   - `init()`
   - `run()`
   - Другие методы (если есть)

⚠️ **ВАЖНО**: Не пропустить ни одного места создания объекта!

### Шаг 4: Определить свойства для зависимостей

**Что сделать**:
1. Найти все свойства класса (в начале класса)
2. Определить, какие свойства хранят зависимости:
   - `$brizyApi`
   - `$parser` (MBProjectDataCollector)
   - `$pageController`
   - Другие (если есть)
3. Записать типы свойств (если указаны)

### Шаг 5: Понять порядок инициализации

**Что сделать**:
1. Изучить методы `start()`, `init()`, `run()`
2. Понять порядок вызова методов
3. Определить, когда создаются зависимости:
   - В конструкторе?
   - В методе init()?
   - В методе run()?
   - В других методах?

### Шаг 6: Создать документ с результатами

**Создать файл**: `testing-improvement/stage-1/substage-1.2/tasks/MIGRATION_PLATFORM_ANALYSIS.md`

**Структура документа**:
```markdown
# Анализ класса MigrationPlatform

## Конструктор
- Сигнатура: [точная сигнатура из кода]
- Параметры: [список всех параметров]
- Создает ли зависимости: [да/нет, какие]

## Места создания зависимостей

### В методе init()
- Строка [номер]: `new MBProjectDataCollector()`
- Параметры: [если есть]

### В методе run()
- Строка [номер]: `new BrizyAPI()`
- Параметры: [если есть]
- Строка [номер]: `new PageController(...)`
- Параметры: [точные параметры из кода]

[и так далее для всех мест]

## Свойства класса
- `$brizyApi` - тип: [если указан]
- `$parser` - тип: [если указан]
- `$pageController` - тип: [если указан]

## Порядок инициализации
1. [какой метод вызывается первым]
2. [какой метод вызывается вторым]
...

## Использование зависимостей
- `$this->brizyApi` используется в методах: [список]
- `$this->parser` используется в методах: [список]
- `$this->pageController` используется в методах: [список]
```

**Требования к документу**:
- Четкая структура
- Точные данные из реального кода
- Подробные комментарии
- Номера строк для каждого места создания

### Шаг 7: Написать тест понимания

⚠️ **ОБЯЗАТЕЛЬНО**: После изучения написать простой тест, чтобы проверить понимание!

**Что сделать**:
1. Создать файл: `tests/Unit/MBMigration/MigrationPlatformStudyTest.php`
2. Написать тест, который проверяет понимание структуры класса
3. Например: проверить, что конструктор действительно принимает определенные параметры
4. Запустить тест: `vendor/bin/phpunit tests/Unit/MBMigration/MigrationPlatformStudyTest.php`

**Если тест не проходит** - значит понимание неправильное, нужно пересмотреть!

### Шаг 8: Если что-то непонятно - ЗАПРОСИТЬ

⚠️ **КРИТИЧЕСКИ ВАЖНО**: Если что-то непонятно в коде:

- ❌ НЕ придумывать
- ❌ НЕ предполагать
- ✅ ЗАПРОСИТЬ у пользователя:
  - Объяснение непонятной логики
  - Пример реального использования
  - Реальные данные, которые передаются

## 📂 Исходные файлы

- `lib/MBMigration/MigrationPlatform.php` - реальный файл класса для изучения

## ✅ Критерии приемки

- [ ] Файл `MIGRATION_PLATFORM_ANALYSIS.md` создан с полным анализом
- [ ] Конструктор изучен и задокументирован
- [ ] Все места создания зависимостей найдены и задокументированы
- [ ] Все свойства класса задокументированы
- [ ] Порядок инициализации понятен и задокументирован
- [ ] Использование зависимостей задокументировано
- [ ] Написан тест для проверки понимания
- [ ] Тест проходит
- [ ] Если что-то было непонятно - запрошено у пользователя

## 🔍 Как проверить

1. **Проверить полноту анализа**:
   - Использовать `grep -n "new " lib/MBMigration/MigrationPlatform.php`
   - Сравнить найденные места с документом
   - Убедиться, что ничего не пропущено

2. **Проверить точность**:
   - Для каждого места в документе проверить реальный код
   - Убедиться, что сигнатуры совпадают
   - Убедиться, что параметры записаны правильно

3. **Проверить тест**:
   - Запустить тест
   - Убедиться, что он проходит

## ⚠️ Важные замечания

- **Изучать РЕАЛЬНЫЙ код** - не предполагать, не придумывать
- **Записывать ТОЧНО** - сигнатуры должны точно совпадать с кодом
- **Не пропускать места** - все `new` должны быть найдены
- **Понять порядок** - важно понять, когда создаются зависимости
- **Если непонятно - СПРОСИТЬ** - лучше остановиться и спросить, чем придумать неправильно

## 🐛 Возможные проблемы

### Проблема: Не понимаю, зачем создается зависимость в этом месте
**Решение**: 
- ❌ НЕ придумывать назначение
- ✅ ЗАПРОСИТЬ у пользователя объяснение логики

### Проблема: Не знаю, какие параметры передаются в конструктор
**Решение**: 
- Изучить реальный код - параметры должны быть видны
- Если непонятно - записать как есть в коде
- ❌ НЕ придумывать параметры
- ✅ ЗАПРОСИТЬ у пользователя, если нужно уточнить

### Проблема: Не понимаю порядок инициализации
**Решение**: 
- Изучить методы start(), init(), run()
- Проследить порядок вызовов
- ❌ НЕ придумывать порядок
- ✅ ЗАПРОСИТЬ у пользователя объяснение, если непонятно

## 📚 Связанные задачи

- Предыдущая задача: [Задача 1.12: Финальная проверка интерфейсов](../substage-1.1/tasks/task-1.12-final-check.md)
- Следующая задача: [Задача 2.2: Рефакторинг конструктора MigrationPlatform](./task-2.2-refactor-migration-platform-constructor.md)

## 📝 Заметки

[Место для заметок о выполнении задачи]

---

**ВАЖНО**: Если во время изучения кода возникли вопросы или что-то непонятно - ОСТАНОВИТЬСЯ и ЗАПРОСИТЬ у пользователя, не придумывать!
