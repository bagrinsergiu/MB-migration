# Задача 2.2: Рефакторинг конструктора MigrationPlatform

## 📋 Информация о задаче

**Время выполнения**: 2-3 часа  
**Приоритет**: КРИТИЧЕСКИЙ  
**Зависимости**: Задача 2.1 (класс MigrationPlatform должен быть изучен)

## 🎯 Принципы работы

⚠️ **ВАЖНО**: При выполнении этой задачи следуйте принципам из [PRINCIPLES.md](../../../../PRINCIPLES.md):

1. ✅ **Сразу тестировать** - после каждого изменения написать тест
2. 🤖 **AI-friendly** - четкие имена, подробные комментарии, явные типы
3. 👤 **Human-friendly** - простота, один класс = одна цель
4. 💬 **Везде комментарии** - PHPDoc для всех методов
5. 🧪 **Реальные данные** - использовать данные из задачи 2.1, НЕ придумывать!

## ❌ Проблема

В классе `MigrationPlatform` зависимости создаются напрямую в методах `init()` и `run()`:
- В методе `init()` создается `new MBProjectDataCollector()`
- В методе `run()` создается `new BrizyAPI()`
- В методе `run()` создается `new PageController(...)`

Это создает проблемы:
1. **Невозможность тестирования с моками** - нельзя подставить фейковые реализации
2. **Жесткая связанность** - класс зависит от конкретных реализаций
3. **Сложность замены реализации** - если понадобится другая реализация, придется менять код
4. **Нарушение принципа инверсии зависимостей** - класс зависит от конкретных реализаций, а не от абстракций

Без рефакторинга невозможно использовать Dependency Injection и создавать тесты с моками.

⚠️ **КРИТИЧЕСКИ ВАЖНО**: Использовать данные из анализа задачи 2.1, не придумывать!

## 🎯 Ожидаемый результат

После выполнения задачи класс `MigrationPlatform` должен:

1. **Принимать зависимости через конструктор** - конструктор должен принимать интерфейсы вместо создания объектов
2. **Не создавать зависимости напрямую** - все `new` для зависимостей должны быть убраны
3. **Использовать переданные зависимости** - использовать объекты, переданные в конструктор
4. **Сохранять функциональность** - класс должен работать так же, как до рефакторинга
5. **Иметь PHPDoc комментарии** - все изменения должны быть задокументированы

Конкретные изменения зависят от реальной структуры класса, найденной в задаче 2.1.

## 📝 Что нужно сделать

### Шаг 1: Изучить результаты анализа

⚠️ **НЕ ПРИДУМЫВАТЬ!** Использовать документ из задачи 2.1: `MIGRATION_PLATFORM_ANALYSIS.md`

**Что сделать**:
1. Открыть документ `MIGRATION_PLATFORM_ANALYSIS.md`
2. Изучить все найденные места создания зависимостей
3. Понять текущую структуру конструктора
4. Определить, какие интерфейсы нужно добавить в конструктор

### Шаг 2: Определить, какие зависимости инжектировать

**На основе анализа определить**:
1. Какие зависимости создаются в методе `init()` - нужно ли их инжектировать через конструктор или можно создать в init()
2. Какие зависимости создаются в методе `run()` - нужно ли их инжектировать через конструктор или можно создать в run()
3. Какие зависимости используются в нескольких методах - их точно нужно инжектировать

⚠️ **ВАЖНО**: Если не понятно, как лучше - ЗАПРОСИТЬ у пользователя рекомендацию!

### Шаг 3: Изменить конструктор

**Что сделать**:
1. Открыть файл `lib/MBMigration/MigrationPlatform.php`
2. Найти конструктор `__construct()`
3. Добавить параметры для интерфейсов:
   - Если `MBProjectDataCollector` создается в `init()` и используется дальше - добавить `MBProjectDataCollectorInterface $mbCollector`
   - Если `BrizyAPI` создается в `run()` и используется дальше - добавить `BrizyAPIInterface $brizyApi`
   - Если `PageController` создается в `run()` - возможно, его нужно создавать там, но принимать зависимости для него
4. Добавить use statements для интерфейсов в начале файла
5. Сохранить зависимостей в свойства класса

⚠️ **ВАЖНО**: 
- Использовать ТОЧНЫЕ сигнатуры из анализа задачи 2.1
- Не менять порядок существующих параметров (если возможно)
- Сохранить все существующие параметры конструктора

### Шаг 4: Убрать прямые создания зависимостей

**Что сделать**:
1. Найти все места `new MBProjectDataCollector()` в методе `init()`
2. Заменить на использование переданной зависимости
3. Найти все места `new BrizyAPI()` в методе `run()`
4. Заменить на использование переданной зависимости
5. Для `PageController` - если он создается в `run()`, возможно нужно передавать его зависимости в конструктор MigrationPlatform

⚠️ **ВАЖНО**: 
- Убрать ВСЕ прямые создания зависимостей
- Использовать переданные через конструктор объекты
- Сохранить всю логику инициализации

### Шаг 5: Добавить PHPDoc комментарии

⚠️ **ОБЯЗАТЕЛЬНО**: Добавить PHPDoc комментарии!

**Что сделать**:
1. Обновить PHPDoc конструктора:
   - Описать новые параметры (интерфейсы)
   - Объяснить, что зависимости теперь инжектируются
2. Добавить комментарии в местах изменений:
   - Объяснить, почему убрали `new`
   - Объяснить, откуда берется зависимость

### Шаг 6: Написать тест

⚠️ **ОБЯЗАТЕЛЬНО**: После изменений сразу написать тест!

**Что сделать**:
1. Создать файл: `tests/Unit/MBMigration/MigrationPlatformRefactoringTest.php`
2. Написать тест, который проверяет:
   - Можно ли создать MigrationPlatform с моками интерфейсов
   - Правильно ли сохраняются зависимости
   - Работает ли класс с переданными зависимостями

**Какие данные использовать**:
- ❌ НЕ придумывать структуру моков
- ✅ ЗАПРОСИТЬ у пользователя пример реальных данных, если нужно
- Использовать простые моки для проверки

3. Запустить тест: `vendor/bin/phpunit tests/Unit/MBMigration/MigrationPlatformRefactoringTest.php`

**Если тест не проходит** - исправить проблему, не продолжать дальше!

### Шаг 7: Проверить синтаксис

**Что сделать**:
1. Проверить синтаксис файла: `php -l lib/MBMigration/MigrationPlatform.php`
2. Убедиться, что нет ошибок компиляции
3. Если есть ошибки - исправить

### Шаг 8: Если что-то непонятно - ЗАПРОСИТЬ

⚠️ **КРИТИЧЕСКИ ВАЖНО**: Если что-то непонятно:

- ❌ НЕ придумывать решение
- ❌ НЕ предполагать
- ✅ ЗАПРОСИТЬ у пользователя:
  - Рекомендацию по структуре конструктора
  - Пример правильного рефакторинга
  - Объяснение непонятной логики

## 📂 Исходные файлы

- `lib/MBMigration/MigrationPlatform.php` - файл для рефакторинга
- `testing-improvement/stage-1/substage-1.2/tasks/MIGRATION_PLATFORM_ANALYSIS.md` - результаты анализа из задачи 2.1

## ✅ Критерии приемки

- [ ] Конструктор изменен - добавлены параметры для интерфейсов
- [ ] Интерфейсы добавлены в use statements
- [ ] Все прямые создания зависимостей убраны из методов init() и run()
- [ ] Зависимости используются из свойств класса (переданные через конструктор)
- [ ] PHPDoc комментарии обновлены
- [ ] Написан тест для проверки рефакторинга
- [ ] Тест проходит
- [ ] Синтаксис проверен - нет ошибок компиляции
- [ ] Если что-то было непонятно - запрошено у пользователя

## 🔍 Как проверить

1. **Проверить синтаксис**: `php -l lib/MBMigration/MigrationPlatform.php`
2. **Проверить, что нет `new` для зависимостей**: `grep -n "new BrizyAPI\|new MBProjectDataCollector" lib/MBMigration/MigrationPlatform.php`
3. **Проверить конструктор**: убедиться, что он принимает интерфейсы
4. **Запустить тест**: убедиться, что тест проходит

## ⚠️ Важные замечания

- **Использовать данные из анализа** - не придумывать структуру
- **Сохранять функциональность** - класс должен работать так же
- **Не ломать существующий код** - проверять после каждого изменения
- **Тестировать сразу** - после каждого изменения писать тест
- **Если непонятно - СПРОСИТЬ** - лучше остановиться и спросить

## 🐛 Возможные проблемы

### Проблема: Не знаю, какие зависимости инжектировать в конструктор, а какие создавать в методах
**Решение**: 
- ❌ НЕ придумывать решение
- ✅ ЗАПРОСИТЬ у пользователя рекомендацию:
  - Какие зависимости должны быть в конструкторе?
  - Какие можно создавать в методах?

### Проблема: Не понимаю, как передать зависимости для PageController
**Решение**: 
- Изучить конструктор PageController из анализа
- Понять, какие зависимости ему нужны
- ❌ НЕ придумывать решение
- ✅ ЗАПРОСИТЬ у пользователя рекомендацию, если непонятно

### Проблема: Тест не проходит
**Решение**: 
- Изучить ошибку теста
- Исправить проблему
- ❌ НЕ продолжать дальше, пока тест не проходит
- ✅ ЗАПРОСИТЬ у пользователя помощь, если не получается исправить

## 📚 Связанные задачи

- Предыдущая задача: [Задача 2.1: Изучить MigrationPlatform](./task-2.1-study-migration-platform.md)
- Следующая задача: [Задача 2.3: Найти все места создания MigrationPlatform](./task-2.3-find-migration-platform-creations.md)

## 📝 Заметки

[Место для заметок о выполнении задачи]

---

**ВАЖНО**: Если во время рефакторинга возникли вопросы или что-то непонятно - ОСТАНОВИТЬСЯ и ЗАПРОСИТЬ у пользователя, не придумывать!
