# Задача 1.10: Проверить и расширить BrowserInterface

## 📋 Информация о задаче

**Время выполнения**: 30 минут - 1 час  
**Приоритет**: СРЕДНИЙ  
**Зависимости**: Задача 1.9 (S3UploaderInterface создан)

## ❌ Проблема

В проекте уже существует интерфейс `BrowserInterface` в файле `lib/MBMigration/Browser/BrowserInterface.php`. Однако необходимо проверить:

1. **Полнота интерфейса** - содержит ли он все необходимые методы для работы с браузером
2. **Соответствие реализации** - совпадают ли методы в интерфейсе с методами в классе BrowserPHP
3. **Недостающие методы** - есть ли методы в BrowserPHP, которых нет в интерфейсе
4. **Актуальность интерфейса** - соответствует ли интерфейс текущей функциональности

Без проверки и обновления интерфейса могут возникнуть проблемы при использовании Dependency Injection. Если интерфейс неполный, нельзя будет использовать все возможности класса через интерфейс.

## 🎯 Ожидаемый результат

После выполнения задачи:

1. **Интерфейс BrowserInterface проверен** - все методы в интерфейсе соответствуют методам в BrowserPHP
2. **Недостающие методы добавлены** (если были найдены) - интерфейс дополнен методами, которые есть в BrowserPHP, но отсутствуют в интерфейсе
3. **PHPDoc комментарии добавлены** (если отсутствовали) - все методы имеют описания
4. **Типы параметров проверены** - все параметры имеют правильные типы
5. **Интерфейс готов к использованию** - можно использовать BrowserInterface в type hints

Если интерфейс уже полный и соответствует реализации, задача считается выполненной без изменений.

## 📝 Что нужно сделать

1. Открыть существующий интерфейс: `lib/MBMigration/Browser/BrowserInterface.php`
2. Открыть реализацию: `lib/MBMigration/Browser/BrowserPHP.php`
3. Сравнить методы:
   - Найти все публичные методы в BrowserPHP
   - Проверить, есть ли они в BrowserInterface
   - Определить недостающие методы
4. Проверить сигнатуры методов:
   - Совпадают ли типы параметров
   - Совпадают ли возвращаемые типы
5. Проверить PHPDoc комментарии:
   - Есть ли описания для всех методов
   - Достаточно ли они информативны
6. Если найдены недостающие методы или несоответствия:
   - Добавить недостающие методы в интерфейс
   - Обновить сигнатуры методов (если нужно)
   - Добавить PHPDoc комментарии
7. Проверить синтаксис обновленного интерфейса

## 📂 Файлы для проверки

- `lib/MBMigration/Browser/BrowserInterface.php` - существующий интерфейс
- `lib/MBMigration/Browser/BrowserPHP.php` - реализация интерфейса

## ✅ Критерии приемки

- [ ] Интерфейс BrowserInterface проверен
- [ ] Все методы из BrowserPHP присутствуют в интерфейсе (или определено, что они не должны быть)
- [ ] Сигнатуры методов совпадают между интерфейсом и реализацией
- [ ] Все методы имеют PHPDoc комментарии
- [ ] Все параметры имеют типы
- [ ] Все методы имеют возвращаемые типы
- [ ] Недостающие методы добавлены (если были найдены)
- [ ] Файл синтаксически корректен (проверка через `php -l`)
- [ ] Интерфейс готов к использованию в type hints

## 🔍 Как проверить

1. Проверить синтаксис: `php -l lib/MBMigration/Browser/BrowserInterface.php`
2. Сравнить методы в интерфейсе с методами в BrowserPHP
3. Проверить, что сигнатуры методов совпадают
4. Убедиться, что типы параметров совпадают
5. Проверить, что возвращаемые типы совпадают

## ⚠️ Важные замечания

- **Не ломать существующий интерфейс** - если интерфейс уже используется, изменения должны быть обратно совместимы
- **Только публичные методы** - в интерфейс должны входить только публичные методы
- **Точность сигнатур** - сигнатуры методов должны точно совпадать с реализацией
- **PHPDoc важен** - если комментариев нет, их нужно добавить

## 🐛 Возможные проблемы

- Методы в интерфейсе не совпадают с методами в BrowserPHP - нужно обновить интерфейс
- Отсутствуют типы - нужно добавить типы для всех параметров и возвращаемых значений
- Недостающие методы - нужно добавить методы, которые есть в BrowserPHP, но отсутствуют в интерфейсе

## 📚 Связанные задачи

- Предыдущая задача: [Задача 1.9: Создать интерфейс S3UploaderInterface](./task-1.9-create-s3-uploader-interface.md)
- Следующая задача: [Задача 1.11: Проверить реализацию интерфейсов](./task-1.11-check-implementation.md)
