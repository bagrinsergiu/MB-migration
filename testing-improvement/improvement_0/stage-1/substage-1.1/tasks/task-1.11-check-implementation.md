# Задача 1.11: Проверить, что классы реализуют интерфейсы

## 📋 Информация о задаче

**Время выполнения**: 30 минут  
**Приоритет**: ВЫСОКИЙ  
**Зависимости**: Задачи 1.3, 1.5, 1.7, 1.9, 1.10 (все интерфейсы должны быть созданы)

## ❌ Проблема

После создания всех интерфейсов необходимо обновить классы, чтобы они реализовывали соответствующие интерфейсы. Без этого:

1. **Интерфейсы не работают** - нельзя использовать интерфейсы в type hints, если классы их не реализуют
2. **Нет проверки соответствия** - PHP не проверит, что класс действительно реализует все методы интерфейса
3. **Невозможно использовать DI** - Dependency Injection не будет работать без реализации интерфейсов
4. **Нет гарантии совместимости** - нет гарантии, что класс действительно соответствует интерфейсу

Без явного указания `implements` в классах интерфейсы не будут работать. PHP требует явного указания реализации интерфейса для проверки соответствия на этапе компиляции.

## 🎯 Ожидаемый результат

После выполнения задачи все классы должны явно реализовывать соответствующие интерфейсы:

1. **BrizyAPI implements BrizyAPIInterface** - класс BrizyAPI реализует интерфейс BrizyAPIInterface
2. **MBProjectDataCollector implements MBProjectDataCollectorInterface** - класс MBProjectDataCollector реализует соответствующий интерфейс
3. **MySQL implements DatabaseInterface** - класс MySQL реализует интерфейс DatabaseInterface
4. **PostgresSQL implements DatabaseInterface** - класс PostgresSQL реализует интерфейс DatabaseInterface
5. **S3Uploader implements S3UploaderInterface** - класс S3Uploader реализует интерфейс S3UploaderInterface
6. **BrowserPHP implements BrowserInterface** - класс BrowserPHP реализует интерфейс BrowserInterface

Все классы должны компилироваться без ошибок после добавления `implements`.

## 📝 Что нужно сделать

1. Для каждого класса обновить объявление класса:
   - Добавить `implements` с соответствующим интерфейсом
   - Добавить `use` statement для интерфейса (если нужно)
2. Проверить классы:
   - `lib/MBMigration/Layer/Brizy/BrizyAPI.php` - добавить `implements BrizyAPIInterface`
   - `lib/MBMigration/Layer/MB/MBProjectDataCollector.php` - добавить `implements MBProjectDataCollectorInterface`
   - `lib/MBMigration/Layer/DataSource/driver/MySQL.php` - добавить `implements DatabaseInterface`
   - `lib/MBMigration/Layer/DataSource/driver/PostgresSQL.php` - добавить `implements DatabaseInterface`
   - `lib/MBMigration/Core/S3Uploader.php` - добавить `implements S3UploaderInterface`
   - `lib/MBMigration/Browser/BrowserPHP.php` - проверить/добавить `implements BrowserInterface`
3. Для каждого класса:
   - Добавить `use` statement для интерфейса в начале файла
   - Обновить объявление класса с `implements`
4. Проверить синтаксис всех обновленных файлов
5. Убедиться, что классы компилируются без ошибок

## 📂 Файлы для обновления

- `lib/MBMigration/Layer/Brizy/BrizyAPI.php`
- `lib/MBMigration/Layer/MB/MBProjectDataCollector.php`
- `lib/MBMigration/Layer/DataSource/driver/MySQL.php`
- `lib/MBMigration/Layer/DataSource/driver/PostgresSQL.php`
- `lib/MBMigration/Core/S3Uploader.php`
- `lib/MBMigration/Browser/BrowserPHP.php`

## ✅ Критерии приемки

- [ ] BrizyAPI implements BrizyAPIInterface
- [ ] MBProjectDataCollector implements MBProjectDataCollectorInterface
- [ ] MySQL implements DatabaseInterface
- [ ] PostgresSQL implements DatabaseInterface
- [ ] S3Uploader implements S3UploaderInterface
- [ ] BrowserPHP implements BrowserInterface
- [ ] Все use statements добавлены
- [ ] Все классы компилируются без ошибок
- [ ] Нет ошибок о нереализованных методах

## 🔍 Как проверить

1. Проверить синтаксис каждого файла: `php -l <путь_к_файлу>`
2. Убедиться, что в каждом классе есть `implements` с соответствующим интерфейсом
3. Проверить, что все use statements добавлены
4. Убедиться, что нет ошибок компиляции
5. Проверить, что PHP не выдает ошибок о нереализованных методах интерфейса

## ⚠️ Важные замечания

- **Точность критична** - имена интерфейсов должны точно совпадать с именами файлов интерфейсов
- **Use statements обязательны** - нужно добавить `use` для каждого интерфейса
- **Проверка компиляции** - если класс не компилируется, значит не все методы интерфейса реализованы
- **Не ломать существующий код** - изменения должны быть минимальными, только добавление `implements`

## 🐛 Возможные проблемы

- Класс не компилируется после добавления `implements` - значит не все методы интерфейса реализованы, нужно проверить сигнатуры
- Ошибка "Class not found" - нужно проверить use statement и путь к интерфейсу
- Ошибка о несовпадении сигнатур - нужно проверить, что сигнатуры методов в классе совпадают с сигнатурами в интерфейсе

## 📚 Связанные задачи

- Предыдущая задача: [Задача 1.10: Проверить и расширить BrowserInterface](./task-1.10-check-browser-interface.md)
- Следующая задача: [Задача 1.12: Финальная проверка интерфейсов](./task-1.12-final-check.md)
