# Задача 1.7: Создать интерфейс DatabaseInterface

## 📋 Информация о задаче

**Время выполнения**: 1 час  
**Приоритет**: ВЫСОКИЙ  
**Зависимости**: Задача 1.6 (классы Database должны быть изучены)

## ❌ Проблема

Классы `MySQL` и `PostgresSQL` используются напрямую в других классах проекта. Это создает проблемы:

1. **Невозможность тестирования с моками** - нельзя подставить фейковую реализацию БД для тестов
2. **Жесткая связанность** - классы зависят от конкретной реализации БД (MySQL или PostgreSQL)
3. **Сложность замены БД** - если понадобится использовать другую БД, придется менять много кода
4. **Дублирование логики** - код должен работать и с MySQL, и с PostgreSQL, но зависит от конкретного класса
5. **Нарушение принципа инверсии зависимостей** - классы зависят от конкретных реализаций

Без интерфейса невозможно использовать Dependency Injection и создавать тесты с моками для работы с базой данных. Это критически важно, так как в тестах не должно быть реальных подключений к базам данных.

## 🎯 Ожидаемый результат

После выполнения задачи должен быть создан файл `lib/MBMigration/Contracts/DatabaseInterface.php`, который:

1. **Содержит все общие методы** из классов MySQL и PostgresSQL с точными сигнатурами
2. **Имеет правильный namespace** - `namespace MBMigration\Contracts;`
3. **Содержит PHPDoc комментарии** для каждого метода с описанием:
   - Назначения метода
   - Параметров (типы, описание)
   - Возвращаемого значения
   - Исключений, которые могут быть выброшены
4. **Использует строгую типизацию** - все параметры и возвращаемые значения имеют типы
5. **Синтаксически корректен** - файл компилируется без ошибок
6. **Универсален** - интерфейс подходит для обоих классов (MySQL и PostgresSQL)
7. **Включает основные операции** - query, insert, update, delete, транзакции

Интерфейс должен быть готов к использованию в type hints и для реализации обоими классами (MySQL и PostgresSQL).

## 📝 Что нужно сделать

1. Создать файл `lib/MBMigration/Contracts/DatabaseInterface.php`
2. Добавить правильный namespace: `namespace MBMigration\Contracts;`
3. Для каждого общего метода из классов MySQL и PostgresSQL (из задачи 1.6):
   - Создать метод в интерфейсе с той же сигнатурой
   - Добавить типы параметров
   - Добавить возвращаемый тип
   - Добавить PHPDoc комментарий с описанием
4. Убедиться, что методы подходят для обоих классов
5. Включить методы для основных операций:
   - Выполнение запросов (query)
   - Вставка данных (insert)
   - Обновление данных (update)
   - Удаление данных (delete)
   - Транзакции (beginTransaction, commit, rollback)
6. Проверить синтаксис файла

## 📂 Исходные файлы

- `lib/MBMigration/Layer/DataSource/driver/MySQL.php` - класс MySQL
- `lib/MBMigration/Layer/DataSource/driver/PostgresSQL.php` - класс PostgresSQL
- Результат задачи 1.6 - список общих методов с сигнатурами

## ✅ Критерии приемки

- [ ] Файл `lib/MBMigration/Contracts/DatabaseInterface.php` создан
- [ ] Namespace правильный: `MBMigration\Contracts`
- [ ] Все общие методы из MySQL и PostgresSQL включены в интерфейс
- [ ] Сигнатуры методов точно соответствуют методам в обоих классах
- [ ] Все параметры имеют типы
- [ ] Все методы имеют возвращаемые типы
- [ ] PHPDoc комментарии добавлены для каждого метода
- [ ] Включены методы для основных операций (CRUD, транзакции)
- [ ] Файл синтаксически корректен (проверка через `php -l`)
- [ ] Интерфейс готов к использованию в type hints
- [ ] Интерфейс подходит для реализации обоими классами

## 🔍 Как проверить

1. Проверить синтаксис: `php -l lib/MBMigration/Contracts/DatabaseInterface.php`
2. Сравнить методы в интерфейсе с методами в обоих классах
3. Проверить, что сигнатуры методов совпадают
4. Убедиться, что типы параметров совпадают
5. Проверить, что возвращаемые типы совпадают
6. Убедиться, что интерфейс универсален для обоих классов
7. **Запустить тест** (рекомендуется):
   ```bash
   # В Docker контейнере
   docker exec mg_migration vendor/bin/phpunit tests/MBMigration/Contracts/DatabaseInterfaceTest.php
   ```

## ⚠️ Важные замечания

- **Универсальность критична** - интерфейс должен работать и с MySQL, и с PostgreSQL
- **Только общие методы** - не включать методы, которые есть только в одном классе
- **Точность сигнатур** - сигнатуры методов должны точно совпадать с обоими классами
- **Типы обязательны** - все параметры и возвращаемые значения должны иметь типы
- **PHPDoc важен** - комментарии помогут понять назначение методов работы с БД

## 🐛 Возможные проблемы

- Методы в интерфейсе не совпадают с методами в классах - нужно тщательно проверить сигнатуры в обоих классах
- Отсутствуют типы - нужно добавить типы для всех параметров и возвращаемых значений
- Различия в сигнатурах между классами - нужно найти компромисс или определить, какой вариант правильный

## 📚 Связанные задачи

- Предыдущая задача: [Задача 1.6: Изучить классы Database](./task-1.6-study-database-classes.md)
- Следующая задача: [Задача 1.8: Изучить класс S3Uploader](./task-1.8-study-s3-uploader.md)
