# Анализ класса BrizyAPI

**Дата анализа**: 2025-01-27  
**Задача**: task-1.2-study-brizy-api  
**Файл**: `lib/MBMigration/Layer/Brizy/BrizyAPI.php`

## 📋 Общая информация

- **Класс**: `MBMigration\Layer\Brizy\BrizyAPI`
- **Наследование**: `extends Utils`
- **Назначение**: Класс для работы с Brizy API - создание проектов, страниц, загрузка медиа, управление шрифтами и т.д.
- **Всего публичных методов**: 44

---

## 🔍 Публичные методы

### 1. Конструктор

```php
/**
 * @throws Exception
 */
public function __construct()
```

**Параметры**: Нет  
**Возвращаемый тип**: void  
**Исключения**: `Exception` - если Config не инициализирован  
**Назначение**: Инициализация класса, проверка токена, получение кэша  
**Критичность**: КРИТИЧЕСКИЙ

---

### 2. getProjectMetadata

```php
/**
 * @throws Exception
 */
public function getProjectMetadata($projectId)
```

**Параметры**: 
- `$projectId` (mixed) - ID проекта

**Возвращаемый тип**: `array|null` - метаданные проекта или null  
**Исключения**: `Exception`  
**Назначение**: Получить метаданные проекта  
**Критичность**: ВЫСОКИЙ

---

### 3. getAllProjectFromContainer

```php
public function getAllProjectFromContainer($containerId, $count = 100)
```

**Параметры**: 
- `$containerId` (mixed) - ID контейнера
- `$count` (int) - количество проектов (по умолчанию 100)

**Возвращаемый тип**: `array` - массив проектов  
**Исключения**: `Exception` - если статус ответа > 200  
**Назначение**: Получить все проекты из контейнера  
**Критичность**: ВЫСОКИЙ

---

### 4. getAllProjectFromContainerV1

```php
public function getAllProjectFromContainerV1($containerId, $count = 100)
```

**Параметры**: 
- `$containerId` (mixed) - ID контейнера
- `$count` (int) - количество проектов (по умолчанию 100)

**Возвращаемый тип**: `array` - массив проектов  
**Исключения**: `Exception` - если статус ответа > 200  
**Назначение**: Получить все проекты из контейнера (версия V1 API)  
**Критичность**: СРЕДНИЙ

---

### 5. deleteProject

```php
public function deleteProject($projectID): bool
```

**Параметры**: 
- `$projectID` (mixed) - ID проекта для удаления

**Возвращаемый тип**: `bool` - true если успешно удален  
**Исключения**: Нет  
**Назначение**: Удалить проект  
**Критичность**: СРЕДНИЙ

---

### 6. getProjectHomePage

```php
public function getProjectHomePage($projectId, $homePageId)
```

**Параметры**: 
- `$projectId` (mixed) - ID проекта
- `$homePageId` (mixed) - ID домашней страницы

**Возвращаемый тип**: `bool` - true если успешно установлена  
**Исключения**: Нет  
**Назначение**: Установить домашнюю страницу проекта  
**Критичность**: ВЫСОКИЙ

---

### 7. getWorkspaces

```php
/**
 * @throws Exception
 */
public function getWorkspaces($name = null)
```

**Параметры**: 
- `$name` (string|null) - имя workspace (опционально)

**Возвращаемый тип**: `array|int|false` - массив workspace, ID workspace или false  
**Исключения**: `Exception`  
**Назначение**: Получить список workspace или найти workspace по имени  
**Критичность**: ВЫСОКИЙ

---

### 8. getProject

```php
/**
 * @throws Exception
 */
public function getProject($workspacesID, $filtre = null)
```

**Параметры**: 
- `$workspacesID` (mixed) - ID workspace
- `$filtre` (mixed|null) - фильтр по имени проекта (опционально)

**Возвращаемый тип**: `array|int|false` - массив проектов, ID проекта или false  
**Исключения**: `Exception`  
**Назначение**: Получить проекты из workspace или найти проект по имени  
**Критичность**: ВЫСОКИЙ

---

### 9. getProjectPrivateApi

```php
public function getProjectPrivateApi($projectID)
```

**Параметры**: 
- `$projectID` (mixed) - ID проекта

**Возвращаемый тип**: `array|false` - данные проекта или false  
**Исключения**: Нет  
**Назначение**: Получить данные проекта через приватный API  
**Критичность**: ВЫСОКИЙ

---

### 10. getGraphToken

```php
/**
 * @throws Exception
 */
public function getGraphToken($projectid)
```

**Параметры**: 
- `$projectid` (mixed) - ID проекта

**Возвращаемый тип**: `string` - токен доступа  
**Исключения**: `Exception` - если ошибка получения токена  
**Назначение**: Получить GraphQL токен для проекта  
**Критичность**: ВЫСОКИЙ

---

### 11. getUserToken

```php
/**
 * @throws Exception
 */
public function getUserToken($userId)
```

**Параметры**: 
- `$userId` (mixed) - ID пользователя

**Возвращаемый тип**: `string|false` - токен пользователя или false  
**Исключения**: `Exception`  
**Назначение**: Получить токен пользователя  
**Критичность**: СРЕДНИЙ

---

### 12. setProjectToken

```php
public function setProjectToken($newToken)
```

**Параметры**: 
- `$newToken` (mixed) - новый токен проекта

**Возвращаемый тип**: void  
**Исключения**: Нет  
**Назначение**: Установить токен проекта  
**Критичность**: СРЕДНИЙ

---

### 13. uploadCustomIcon

```php
public function uploadCustomIcon($projectId, $fileName, $attachment)
```

**Параметры**: 
- `$projectId` (mixed) - ID проекта
- `$fileName` (mixed) - имя файла
- `$attachment` (mixed) - содержимое файла (base64)

**Возвращаемый тип**: `array|false` - результат загрузки или false  
**Исключения**: Нет (обрабатывается внутри)  
**Назначение**: Загрузить кастомную иконку  
**Критичность**: НИЗКИЙ

---

### 14. createMedia

```php
/**
 * @throws Exception
 */
public function createMedia($pathOrUrlToFileName, $nameFolder = '')
```

**Параметры**: 
- `$pathOrUrlToFileName` (mixed) - путь к файлу или URL
- `$nameFolder` (string) - имя папки (опционально)

**Возвращаемый тип**: `array|false` - результат загрузки или false  
**Исключения**: `Exception`  
**Назначение**: Создать медиа файл (изображение) в проекте  
**Критичность**: КРИТИЧЕСКИЙ

---

### 15. createGlobalBlock

```php
/**
 * @throws Exception
 * @throws GuzzleException
 */
public function createGlobalBlock($data, $position, $rules)
```

**Параметры**: 
- `$data` (mixed) - данные блока
- `$position` (mixed) - позиция блока
- `$rules` (mixed) - правила блока

**Возвращаемый тип**: `bool` - всегда возвращает false  
**Исключения**: `Exception`, `GuzzleException`  
**Назначение**: Создать глобальный блок  
**Критичность**: СРЕДНИЙ

---

### 16. deletePage

```php
public function deletePage($url)
```

**Параметры**: 
- `$url` (mixed) - URL страницы

**Возвращаемый тип**: void  
**Исключения**: Нет  
**Назначение**: Удалить страницу (не реализовано полностью)  
**Критичность**: НИЗКИЙ

---

### 17. deleteAllGlobalBlocks

```php
public function deleteAllGlobalBlocks()
```

**Параметры**: Нет  
**Возвращаемый тип**: void  
**Исключения**: Нет  
**Назначение**: Удалить все глобальные блоки проекта  
**Критичность**: НИЗКИЙ

---

### 18. fopenFromURL

```php
public function fopenFromURL($url)
```

**Параметры**: 
- `$url` (string) - URL файла

**Возвращаемый тип**: `resource|false` - файловый дескриптор или false  
**Исключения**: Нет  
**Назначение**: Открыть файл по URL  
**Критичность**: НИЗКИЙ

---

### 19. createFonts

```php
/**
 * @throws Exception
 * @throws GuzzleException
 */
public function createFonts($fontsName, $projectID, array $KitFonts, $displayName)
```

**Параметры**: 
- `$fontsName` (mixed) - имя шрифта
- `$projectID` (mixed) - ID проекта
- `$KitFonts` (array) - массив шрифтов с весами
- `$displayName` (mixed) - отображаемое имя

**Возвращаемый тип**: `array` - результат создания шрифта  
**Исключения**: `Exception`, `GuzzleException`  
**Назначение**: Создать шрифты в проекте  
**Критичность**: ВЫСОКИЙ

---

### 20. addFontAndUpdateProject

```php
/**
 * @throws GuzzleException
 * @throws Exception
 */
public function addFontAndUpdateProject(array $data, string $configFonts = 'upload'): string
```

**Параметры**: 
- `$data` (array) - данные шрифта
- `$configFonts` (string) - тип шрифта ('upload', 'google', 'config'), по умолчанию 'upload'

**Возвращаемый тип**: `string` - ID шрифта  
**Исключения**: `GuzzleException`, `Exception`  
**Назначение**: Добавить шрифт и обновить проект  
**Критичность**: ВЫСОКИЙ

---

### 21. checkUpdateFonts

```php
public function checkUpdateFonts(array $projectDataResponse, $brzFontId, $fontNmae = null)
```

**Параметры**: 
- `$projectDataResponse` (array) - ответ с данными проекта
- `$brzFontId` (mixed) - ID шрифта в Brizy
- `$fontNmae` (mixed|null) - имя шрифта (опционально)

**Возвращаемый тип**: void  
**Исключения**: Нет  
**Назначение**: Проверить, был ли шрифт успешно добавлен в проект  
**Критичность**: СРЕДНИЙ

---

### 22. clearAllFontsInProject

```php
/**
 * @throws GuzzleException
 */
public function clearAllFontsInProject()
```

**Параметры**: Нет  
**Возвращаемый тип**: void  
**Исключения**: `GuzzleException`  
**Назначение**: Очистить все шрифты в проекте  
**Критичность**: НИЗКИЙ

---

### 23. setLabelManualMigration

```php
public function setLabelManualMigration(bool $value, $projectID = null)
```

**Параметры**: 
- `$value` (bool) - значение метки
- `$projectID` (mixed|null) - ID проекта (опционально)

**Возвращаемый тип**: void  
**Исключения**: Нет  
**Назначение**: Установить метку ручной миграции  
**Критичность**: СРЕДНИЙ

---

### 24. setCloningLink

```php
public function setCloningLink(bool $value, $projectID = null)
```

**Параметры**: 
- `$value` (bool) - значение
- `$projectID` (mixed|null) - ID проекта (опционально)

**Возвращаемый тип**: void  
**Исключения**: Нет (обрабатывается внутри)  
**Назначение**: Установить ссылку клонирования проекта  
**Критичность**: СРЕДНИЙ

---

### 25. updateProject

```php
public function updateProject(array $projectFullData): array
```

**Параметры**: 
- `$projectFullData` (array) - полные данные проекта

**Возвращаемый тип**: `array` - обновленные данные проекта  
**Исключения**: Нет  
**Назначение**: Обновить данные проекта  
**Критичность**: КРИТИЧЕСКИЙ

---

### 26. getMetadata

```php
public function getMetadata(): array
```

**Параметры**: Нет  
**Возвращаемый тип**: `array` - метаданные проекта  
**Исключения**: Нет  
**Назначение**: Получить метаданные проекта  
**Критичность**: ВЫСОКИЙ

---

### 27. setMetaDate

```php
/**
 * @throws GuzzleException
 */
public function setMetaDate()
```

**Параметры**: Нет  
**Возвращаемый тип**: void  
**Исключения**: `GuzzleException`  
**Назначение**: Установить метаданные проекта  
**Критичность**: ВЫСОКИЙ

---

### 28. getProjectContainer

```php
/**
 * @throws Exception
 */
public function getProjectContainer(int $containerID, $fullDataProject = false)
```

**Параметры**: 
- `$containerID` (int) - ID контейнера
- `$fullDataProject` (bool) - получить полные данные проекта (по умолчанию false)

**Возвращаемый тип**: `mixed` - данные контейнера или проекта  
**Исключения**: `Exception`  
**Назначение**: Получить данные контейнера проекта  
**Критичность**: КРИТИЧЕСКИЙ

---

### 29. createUser

```php
/**
 * @throws Exception
 */
public function createUser(array $value)
```

**Параметры**: 
- `$value` (array) - данные пользователя

**Возвращаемый тип**: `string|false` - токен пользователя или false  
**Исключения**: `Exception`  
**Назначение**: Создать пользователя  
**Критичность**: НИЗКИЙ

---

### 30. createProject

```php
/**
 * @throws Exception
 */
public function createProject($projectName, $workspacesId, $filter = null)
```

**Параметры**: 
- `$projectName` (mixed) - имя проекта
- `$workspacesId` (mixed) - ID workspace
- `$filter` (mixed|null) - фильтр для результата (опционально)

**Возвращаемый тип**: `array|mixed|false` - результат создания проекта  
**Исключения**: `Exception`  
**Назначение**: Создать новый проект  
**Критичность**: КРИТИЧЕСКИЙ

---

### 31. clearCompileds

```php
public function clearCompileds($projectId)
```

**Параметры**: 
- `$projectId` (mixed) - ID проекта

**Возвращаемый тип**: `array|mixed|false` - результат очистки  
**Исключения**: Нет  
**Назначение**: Очистить скомпилированные файлы проекта  
**Критичность**: НИЗКИЙ

---

### 32. createdWorkspaces

```php
/**
 * Создать новый workspace
 * 
 * @param string|null $name Имя workspace. Если не указано, используется Config::$nameMigration
 * @return array
 * @throws Exception
 */
public function createdWorkspaces(?string $name = null): array
```

**Параметры**: 
- `$name` (string|null) - имя workspace (опционально)

**Возвращаемый тип**: `array` - результат создания workspace  
**Исключения**: `Exception`  
**Назначение**: Создать новый workspace  
**Критичность**: ВЫСОКИЙ

---

### 33. getPage

```php
/**
 * @throws Exception
 */
public function getPage($projectID)
```

**Параметры**: 
- `$projectID` (mixed) - ID проекта

**Возвращаемый тип**: `array|int|false` - страницы проекта, ID страницы или false  
**Исключения**: `Exception`  
**Назначение**: Получить страницы проекта  
**Критичность**: ВЫСОКИЙ

---

### 34. getDomain

```php
public function getDomain($projectID)
```

**Параметры**: 
- `$projectID` (mixed) - ID проекта

**Возвращаемый тип**: `string|false` - домен проекта или false  
**Исключения**: Нет  
**Назначение**: Получить домен проекта  
**Критичность**: СРЕДНИЙ

---

### 35. checkProjectManualMigration

```php
public function checkProjectManualMigration($projectID): bool
```

**Параметры**: 
- `$projectID` (mixed) - ID проекта

**Возвращаемый тип**: `bool` - true если проект помечен как ручная миграция  
**Исключения**: Нет  
**Назначение**: Проверить, помечен ли проект как ручная миграция  
**Критичность**: СРЕДНИЙ

---

### 36. getProjectsDataVersion

```php
public function getProjectsDataVersion($projectID)
```

**Параметры**: 
- `$projectID` (mixed) - ID проекта

**Возвращаемый тип**: `int|false` - версия данных проекта или false  
**Исключения**: Нет (обрабатывается внутри)  
**Назначение**: Получить версию данных проекта  
**Критичность**: СРЕДНИЙ

---

### 37. getProjectsData

```php
public function getProjectsData($projectID)
```

**Параметры**: 
- `$projectID` (mixed) - ID проекта

**Возвращаемый тип**: `array|false` - данные проекта или false  
**Исключения**: Нет (обрабатывается внутри)  
**Назначение**: Получить данные проекта  
**Критичность**: ВЫСОКИЙ

---

### 38. createPage

```php
/**
 * @throws Exception
 */
public function createPage($projectID, $pageName, $filter = null)
```

**Параметры**: 
- `$projectID` (mixed) - ID проекта
- `$pageName` (mixed) - имя страницы
- `$filter` (mixed|null) - фильтр для результата (опционально)

**Возвращаемый тип**: `array|mixed|false` - результат создания страницы  
**Исключения**: `Exception`  
**Назначение**: Создать новую страницу в проекте  
**Критичность**: КРИТИЧЕСКИЙ

---

### 39. getAllProjectPages

```php
public function getAllProjectPages(): array
```

**Параметры**: Нет  
**Возвращаемый тип**: `array` - массив страниц проекта  
**Исключения**: Нет  
**Назначение**: Получить все страницы проекта через GraphQL  
**Критичность**: ВЫСОКИЙ

---

### 40. createMenu

```php
/**
 * @throws Exception
 */
public function createMenu($data)
```

**Параметры**: 
- `$data` (array) - данные меню (project, name, data)

**Возвращаемый тип**: `array|false` - результат создания меню или false  
**Исключения**: `Exception`  
**Назначение**: Создать меню в проекте  
**Критичность**: ВЫСОКИЙ

---

### 41. cloneProject

```php
public function cloneProject($projectId, $workspaceId)
```

**Параметры**: 
- `$projectId` (mixed) - ID проекта для клонирования
- `$workspaceId` (mixed) - ID workspace для нового проекта

**Возвращаемый тип**: `array|false` - результат клонирования или false  
**Исключения**: Нет (обрабатывается внутри)  
**Назначение**: Клонировать проект  
**Критичность**: СРЕДНИЙ

---

### 42. upgradeProject

```php
public function upgradeProject($projectId)
```

**Параметры**: 
- `$projectId` (mixed) - ID проекта

**Возвращаемый тип**: `array|false` - результат обновления или false  
**Исключения**: Нет (обрабатывается внутри)  
**Назначение**: Обновить проект  
**Критичность**: НИЗКИЙ

---

### 43. setMediaFolder

```php
public function setMediaFolder(string $nameFolder)
```

**Параметры**: 
- `$nameFolder` (string) - имя папки для медиа

**Возвращаемый тип**: void  
**Исключения**: Нет  
**Назначение**: Установить имя папки для медиа файлов  
**Критичность**: НИЗКИЙ

---

### 44. createDirectory

```php
public function createDirectory($directoryPath): void
```

**Параметры**: 
- `$directoryPath` (mixed) - путь к директории

**Возвращаемый тип**: void  
**Исключения**: Нет  
**Назначение**: Создать директорию, если она не существует  
**Критичность**: НИЗКИЙ

---

## 📊 Классификация методов по критичности

### КРИТИЧЕСКИЕ (используются в основном процессе миграции):
1. `__construct()` - инициализация
2. `createMedia()` - загрузка медиа
3. `updateProject()` - обновление проекта
4. `getProjectContainer()` - получение данных проекта
5. `createProject()` - создание проекта
6. `createPage()` - создание страниц

### ВЫСОКИЕ (важные для миграции):
- `getProjectMetadata()`, `getAllProjectFromContainer()`, `getProjectHomePage()`, `getWorkspaces()`, `getProject()`, `getProjectPrivateApi()`, `getGraphToken()`, `createFonts()`, `addFontAndUpdateProject()`, `getMetadata()`, `setMetaDate()`, `createdWorkspaces()`, `getPage()`, `getProjectsData()`, `getAllProjectPages()`, `createMenu()`

### СРЕДНИЕ (вспомогательные):
- `getAllProjectFromContainerV1()`, `deleteProject()`, `getUserToken()`, `setProjectToken()`, `createGlobalBlock()`, `checkUpdateFonts()`, `setLabelManualMigration()`, `setCloningLink()`, `getDomain()`, `checkProjectManualMigration()`, `getProjectsDataVersion()`, `cloneProject()`

### НИЗКИЕ (редко используемые):
- `uploadCustomIcon()`, `deletePage()`, `deleteAllGlobalBlocks()`, `fopenFromURL()`, `clearAllFontsInProject()`, `createUser()`, `clearCompileds()`, `upgradeProject()`, `setMediaFolder()`, `createDirectory()`

---

## 🔗 Зависимости между методами

- `createMedia()` использует `setMediaFolder()` для установки папки
- `addFontAndUpdateProject()` использует `getProjectContainer()` и `updateProject()`
- `getAllProjectPages()` использует `getGraphToken()` через QueryBuilder
- `updateProject()` использует приватный метод `request()`
- Большинство методов используют приватный метод `httpClient()`

---

## ⚠️ Особенности

1. **Типы параметров**: Многие параметры имеют тип `mixed`, что усложняет создание интерфейса
2. **Обработка исключений**: Некоторые методы обрабатывают исключения внутри, другие пробрасывают
3. **Возвращаемые типы**: Не все методы имеют явные типы возвращаемых значений
4. **Приватные методы**: Класс использует приватные методы `httpClient()` и `request()` для HTTP запросов
5. **Зависимость от Config**: Класс сильно зависит от статических свойств `Config::$mainToken`, `Config::$urlAPI` и т.д.

---

## 📝 Рекомендации для интерфейса

1. Все публичные методы должны быть включены в интерфейс
2. Для параметров типа `mixed` нужно определить более конкретные типы или использовать `mixed`
3. Добавить PHPDoc комментарии для всех методов с описанием параметров и возвращаемых значений
4. Указать все возможные исключения в PHPDoc
5. Группировать методы по функциональности в интерфейсе (опционально)

---

**Следующий шаг**: Создать интерфейс `BrizyAPIInterface` на основе этого анализа (задача 1.3)
