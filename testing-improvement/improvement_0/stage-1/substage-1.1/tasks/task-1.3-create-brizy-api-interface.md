# Задача 1.3: Создать интерфейс BrizyAPIInterface

## 📋 Информация о задаче

**Время выполнения**: 1-2 часа  
**Приоритет**: КРИТИЧЕСКИЙ  
**Зависимости**: Задача 1.2 (класс BrizyAPI должен быть изучен)

## ❌ Проблема

Класс `BrizyAPI` используется напрямую в других классах проекта (например, в `MigrationPlatform`). Это создает следующие проблемы:

1. **Невозможность тестирования с моками** - нельзя подставить фейковую реализацию для тестов
2. **Жесткая связанность** - классы зависят от конкретной реализации, а не от контракта
3. **Сложность замены реализации** - если понадобится другая реализация API, придется менять много кода
4. **Нарушение принципа инверсии зависимостей** - классы зависят от конкретных реализаций, а не от абстракций

Без интерфейса невозможно использовать Dependency Injection и создавать тесты с моками. Это критически важно для тестируемости проекта.

## 🎯 Ожидаемый результат

После выполнения задачи должен быть создан файл `lib/MBMigration/Contracts/BrizyAPIInterface.php`, который:

1. **Содержит все публичные методы** из класса BrizyAPI с точными сигнатурами
2. **Имеет правильный namespace** - `namespace MBMigration\Contracts;`
3. **Содержит PHPDoc комментарии** для каждого метода с описанием:
   - Назначения метода
   - Параметров (типы, описание)
   - Возвращаемого значения
   - Исключений, которые могут быть выброшены
4. **Использует строгую типизацию** - все параметры и возвращаемые значения имеют типы
5. **Синтаксически корректен** - файл компилируется без ошибок
6. **Соответствует сигнатурам** - методы точно соответствуют методам в классе BrizyAPI

Интерфейс должен быть готов к использованию в type hints и для реализации классом BrizyAPI.

## 📝 Что нужно сделать

1. Создать файл `lib/MBMigration/Contracts/BrizyAPIInterface.php`
2. Добавить правильный namespace: `namespace MBMigration\Contracts;`
3. Для каждого публичного метода из BrizyAPI (из задачи 1.2):
   - Создать метод в интерфейсе с той же сигнатурой
   - Добавить типы параметров
   - Добавить возвращаемый тип
   - Добавить PHPDoc комментарий с описанием
4. Убедиться, что все методы включены
5. Проверить синтаксис файла

## 📂 Исходные файлы

- `lib/MBMigration/Layer/Brizy/BrizyAPI.php` - класс для изучения методов
- Результат задачи 1.2 - список методов с сигнатурами

## ✅ Критерии приемки

- [ ] Файл `lib/MBMigration/Contracts/BrizyAPIInterface.php` создан
- [ ] Namespace правильный: `MBMigration\Contracts`
- [ ] Все публичные методы из BrizyAPI включены в интерфейс
- [ ] Сигнатуры методов точно соответствуют методам в классе
- [ ] Все параметры имеют типы
- [ ] Все методы имеют возвращаемые типы
- [ ] PHPDoc комментарии добавлены для каждого метода
- [ ] Файл синтаксически корректен (проверка через `php -l`)
- [ ] Интерфейс готов к использованию в type hints

## 🔍 Как проверить

1. Проверить синтаксис: `php -l lib/MBMigration/Contracts/BrizyAPIInterface.php`
2. Сравнить количество методов в интерфейсе с количеством публичных методов в классе
3. Проверить, что сигнатуры методов совпадают
4. Убедиться, что типы параметров совпадают
5. Проверить, что возвращаемые типы совпадают
6. **Запустить тест** (рекомендуется):
   ```bash
   # В Docker контейнере
   docker exec mg_migration vendor/bin/phpunit tests/MBMigration/Contracts/BrizyAPIInterfaceTest.php
   ```

## ⚠️ Важные замечания

- **Точность критична** - сигнатуры методов должны точно совпадать с классом
- **Типы обязательны** - все параметры и возвращаемые значения должны иметь типы
- **PHPDoc важен** - комментарии помогут будущим разработчикам понять назначение методов
- **Не пропускать методы** - все публичные методы должны быть в интерфейсе
- **Учитывать исключения** - если метод может выбрасывать исключения, это должно быть отражено в PHPDoc

## 🐛 Возможные проблемы

- Методы в интерфейсе не совпадают с методами в классе - нужно тщательно проверить сигнатуры
- Отсутствуют типы - нужно добавить типы для всех параметров и возвращаемых значений
- Неправильный namespace - проверить, что namespace соответствует структуре проекта

## 📚 Связанные задачи

- Предыдущая задача: [Задача 1.2: Изучить класс BrizyAPI](./task-1.2-study-brizy-api.md)
- Следующая задача: [Задача 1.4: Изучить класс MBProjectDataCollector](./task-1.4-study-mb-collector.md)
