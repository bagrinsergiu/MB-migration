# Задача 1.6: Изучить классы Database (MySQL и PostgresSQL)

## 📋 Информация о задаче

**Время выполнения**: 30-60 минут  
**Приоритет**: ВЫСОКИЙ  
**Зависимости**: Задача 1.5 (MBProjectDataCollectorInterface создан)

## ❌ Проблема

В проекте используются два класса для работы с базами данных: `MySQL` и `PostgresSQL`. Оба класса реализуют одну и ту же функциональность (работа с БД), но для разных типов баз данных. Перед созданием интерфейса необходимо:

1. **Найти общие методы** - какие методы есть в обоих классах с одинаковыми сигнатурами
2. **Определить различия** - есть ли методы, которые есть только в одном классе
3. **Понять назначение методов** - для чего используется каждый метод
4. **Определить сигнатуры** - типы параметров, возвращаемые типы, исключения
5. **Выявить паттерны использования** - как эти классы используются в проекте

Без изучения обоих классов невозможно создать универсальный интерфейс, который будет работать и с MySQL, и с PostgreSQL. Интерфейс должен абстрагировать различия между базами данных и предоставлять единый контракт.

## 🎯 Ожидаемый результат

После выполнения задачи должен быть создан документ или список, содержащий:

1. **Список общих публичных методов** - методы, которые есть в обоих классах с одинаковыми сигнатурами
2. **Список уникальных методов** - методы, которые есть только в одном классе (если есть)
3. **Описание назначения каждого метода** - что делает метод, для чего используется
4. **Типы параметров и возвращаемых значений** - точные типы для каждого метода
5. **Информация об исключениях** - какие исключения может выбрасывать каждый метод
6. **Рекомендации по интерфейсу** - какие методы должны быть в интерфейсе, какие можно опустить

Этот документ будет использоваться в следующей задаче для создания универсального интерфейса DatabaseInterface.

## 📝 Что нужно сделать

1. Открыть файлы:
   - `lib/MBMigration/Layer/DataSource/driver/MySQL.php`
   - `lib/MBMigration/Layer/DataSource/driver/PostgresSQL.php`
2. Изучить структуру обоих классов - свойства, методы
3. Найти все публичные методы в каждом классе
4. Сравнить методы обоих классов:
   - Найти методы с одинаковыми названиями
   - Проверить, совпадают ли сигнатуры
   - Определить общие методы
5. Для каждого общего метода записать:
   - Название метода
   - Параметры с типами
   - Возвращаемый тип
   - Исключения (если есть)
   - Назначение метода
6. Определить, какие методы критичны для работы с БД
7. Создать список или документ с этой информацией

## 📂 Файлы для изучения

- `lib/MBMigration/Layer/DataSource/driver/MySQL.php` - класс для работы с MySQL
- `lib/MBMigration/Layer/DataSource/driver/PostgresSQL.php` - класс для работы с PostgreSQL

## ✅ Критерии приемки

- [ ] Оба класса изучены
- [ ] Все публичные методы найдены в каждом классе
- [ ] Общие методы определены и задокументированы
- [ ] Уникальные методы определены (если есть)
- [ ] Для каждого общего метода определены типы параметров
- [ ] Для каждого общего метода определен возвращаемый тип
- [ ] Определены исключения, которые могут быть выброшены
- [ ] Понятно назначение каждого метода
- [ ] Создан документ или список с информацией о методах

## 🔍 Как проверить

1. Убедиться, что список методов полный - проверить через поиск по файлам
2. Сравнить сигнатуры методов в обоих классах
3. Проверить, что общие методы действительно имеют одинаковые сигнатуры
4. Убедиться, что не пропущены важные методы
5. **Запустить тест анализа**:
   ```bash
   # В Docker контейнере
   docker exec mg_migration vendor/bin/phpunit tests/MBMigration/Layer/DataSource/DatabaseClassesAnalysisTest.php
   ```

## ⚠️ Важные замечания

- **Фокус на общих методах** - интерфейс должен содержать только методы, которые есть в обоих классах
- **Учесть различия в SQL** - некоторые методы могут иметь небольшие различия в реализации, но одинаковую сигнатуру
- **Транзакции важны** - методы работы с транзакциями (beginTransaction, commit, rollback) критичны
- **CRUD операции** - методы для создания, чтения, обновления и удаления данных должны быть в интерфейсе

## 📚 Связанные задачи

- Предыдущая задача: [Задача 1.5: Создать интерфейс MBProjectDataCollectorInterface](./task-1.5-create-mb-collector-interface.md)
- Следующая задача: [Задача 1.7: Создать интерфейс DatabaseInterface](./task-1.7-create-database-interface.md)
