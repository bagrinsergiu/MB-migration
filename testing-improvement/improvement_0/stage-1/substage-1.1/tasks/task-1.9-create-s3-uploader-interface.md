# Задача 1.9: Создать интерфейс S3UploaderInterface

## 📋 Информация о задаче

**Время выполнения**: 30 минут - 1 час  
**Приоритет**: СРЕДНИЙ  
**Зависимости**: Задача 1.8 (класс S3Uploader должен быть изучен)

## ❌ Проблема

Класс `S3Uploader` используется для загрузки файлов в AWS S3. Если он используется напрямую в других классах, это создает проблемы:

1. **Невозможность тестирования с моками** - нельзя подставить фейковую реализацию для тестов
2. **Жесткая связанность** - классы зависят от конкретной реализации работы с S3
3. **Сложность замены хранилища** - если понадобится использовать другое хранилище, придется менять код
4. **Нарушение принципа инверсии зависимостей** - классы зависят от конкретной реализации

Без интерфейса невозможно использовать Dependency Injection и создавать тесты с моками для работы с S3. Это важно для тестирования, так как в тестах не должно быть реальных подключений к AWS S3.

## 🎯 Ожидаемый результат

После выполнения задачи должен быть создан файл `lib/MBMigration/Contracts/S3UploaderInterface.php`, который:

1. **Содержит все публичные методы** из класса S3Uploader с точными сигнатурами
2. **Имеет правильный namespace** - `namespace MBMigration\Contracts;`
3. **Содержит PHPDoc комментарии** для каждого метода с описанием:
   - Назначения метода
   - Параметров (типы, описание)
   - Возвращаемого значения
   - Исключений, которые могут быть выброшены
   - Особенностей работы (например, что возвращается, если S3 неактивен)
4. **Использует строгую типизацию** - все параметры и возвращаемые значения имеют типы
5. **Синтаксически корректен** - файл компилируется без ошибок
6. **Соответствует сигнатурам** - методы точно соответствуют методам в классе S3Uploader

Интерфейс должен быть готов к использованию в type hints и для реализации классом S3Uploader.

## 📝 Что нужно сделать

1. Создать файл `lib/MBMigration/Contracts/S3UploaderInterface.php`
2. Добавить правильный namespace: `namespace MBMigration\Contracts;`
3. Для каждого публичного метода из S3Uploader (из задачи 1.8):
   - Создать метод в интерфейсе с той же сигнатурой
   - Добавить типы параметров
   - Добавить возвращаемый тип
   - Добавить PHPDoc комментарий с описанием
   - Указать особенности работы (например, возврат пустой строки при неактивном S3)
4. Убедиться, что все методы включены
5. Проверить синтаксис файла

## 📂 Исходные файлы

- `lib/MBMigration/Core/S3Uploader.php` - класс для изучения методов
- Результат задачи 1.8 - список методов с сигнатурами

## ✅ Критерии приемки

- [ ] Файл `lib/MBMigration/Contracts/S3UploaderInterface.php` создан
- [ ] Namespace правильный: `MBMigration\Contracts`
- [ ] Все публичные методы из S3Uploader включены в интерфейс
- [ ] Сигнатуры методов точно соответствуют методам в классе
- [ ] Все параметры имеют типы
- [ ] Все методы имеют возвращаемые типы
- [ ] PHPDoc комментарии добавлены для каждого метода
- [ ] Указаны особенности работы методов (неактивное состояние S3)
- [ ] Файл синтаксически корректен (проверка через `php -l`)
- [ ] Интерфейс готов к использованию в type hints

## 🔍 Как проверить

1. Проверить синтаксис: `php -l lib/MBMigration/Contracts/S3UploaderInterface.php`
2. Сравнить количество методов в интерфейсе с количеством публичных методов в классе
3. Проверить, что сигнатуры методов совпадают
4. Убедиться, что типы параметров совпадают
5. Проверить, что возвращаемые типы совпадают
6. **Запустить тест** (рекомендуется):
   ```bash
   # В Docker контейнере
   docker exec mg_migration vendor/bin/phpunit tests/MBMigration/Contracts/S3UploaderInterfaceTest.php
   ```

## ⚠️ Важные замечания

- **Точность критична** - сигнатуры методов должны точно совпадать с классом
- **Типы обязательны** - все параметры и возвращаемые значения должны иметь типы
- **PHPDoc важен** - комментарии должны отражать особенности работы, включая неактивное состояние
- **Не пропускать методы** - все публичные методы должны быть в интерфейсе
- **Учесть неактивное состояние** - в PHPDoc должно быть указано, что происходит, если S3 неактивен

## 🐛 Возможные проблемы

- Методы в интерфейсе не совпадают с методами в классе - нужно тщательно проверить сигнатуры
- Отсутствуют типы - нужно добавить типы для всех параметров и возвращаемых значений
- Не указаны особенности работы - нужно добавить информацию о неактивном состоянии в PHPDoc

## 📚 Связанные задачи

- Предыдущая задача: [Задача 1.8: Изучить класс S3Uploader](./task-1.8-study-s3-uploader.md)
- Следующая задача: [Задача 1.10: Проверить и расширить BrowserInterface](./task-1.10-check-browser-interface.md)
