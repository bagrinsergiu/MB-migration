# Задача 1.5: Создать интерфейс MBProjectDataCollectorInterface

## 📋 Информация о задаче

**Время выполнения**: 1-2 часа  
**Приоритет**: КРИТИЧЕСКИЙ  
**Зависимости**: Задача 1.4 (класс MBProjectDataCollector должен быть изучен)

## ❌ Проблема

Класс `MBProjectDataCollector` используется напрямую в других классах проекта (например, в `MigrationPlatform`). Это создает те же проблемы, что и с BrizyAPI:

1. **Невозможность тестирования с моками** - нельзя подставить фейковую реализацию для получения тестовых данных
2. **Жесткая связанность** - классы зависят от конкретной реализации работы с базой данных MB
3. **Сложность замены источника данных** - если понадобится получать данные из другого источника, придется менять много кода
4. **Нарушение принципа инверсии зависимостей** - классы зависят от конкретной реализации

Без интерфейса невозможно использовать Dependency Injection и создавать тесты с моками для получения данных из Ministry Brands. Это критически важно, так как в тестах не должно быть реальных подключений к базе данных MB.

## 🎯 Ожидаемый результат

После выполнения задачи должен быть создан файл `lib/MBMigration/Contracts/MBProjectDataCollectorInterface.php`, который:

1. **Содержит все публичные методы** из класса MBProjectDataCollector с точными сигнатурами
2. **Содержит статические методы** (если они есть и могут быть в интерфейсе)
3. **Имеет правильный namespace** - `namespace MBMigration\Contracts;`
4. **Содержит PHPDoc комментарии** для каждого метода с описанием:
   - Назначения метода
   - Параметров (типы, описание)
   - Возвращаемого значения
   - Исключений, которые могут быть выброшены
5. **Использует строгую типизацию** - все параметры и возвращаемые значения имеют типы
6. **Синтаксически корректен** - файл компилируется без ошибок
7. **Соответствует сигнатурам** - методы точно соответствуют методам в классе MBProjectDataCollector

Интерфейс должен быть готов к использованию в type hints и для реализации классом MBProjectDataCollector.

## 📝 Что нужно сделать

1. Создать файл `lib/MBMigration/Contracts/MBProjectDataCollectorInterface.php`
2. Добавить правильный namespace: `namespace MBMigration\Contracts;`
3. Для каждого публичного метода из MBProjectDataCollector (из задачи 1.4):
   - Создать метод в интерфейсе с той же сигнатурой
   - Добавить типы параметров
   - Добавить возвращаемый тип
   - Добавить PHPDoc комментарий с описанием
4. Учесть статические методы (если они есть и могут быть в интерфейсе)
5. Убедиться, что все методы включены
6. Проверить синтаксис файла

## 📂 Исходные файлы

- `lib/MBMigration/Layer/MB/MBProjectDataCollector.php` - класс для изучения методов
- Результат задачи 1.4 - список методов с сигнатурами

## ✅ Критерии приемки

- [ ] Файл `lib/MBMigration/Contracts/MBProjectDataCollectorInterface.php` создан
- [ ] Namespace правильный: `MBMigration\Contracts`
- [ ] Все публичные методы из MBProjectDataCollector включены в интерфейс
- [ ] Статические методы учтены (если возможно)
- [ ] Сигнатуры методов точно соответствуют методам в классе
- [ ] Все параметры имеют типы
- [ ] Все методы имеют возвращаемые типы
- [ ] PHPDoc комментарии добавлены для каждого метода
- [ ] Файл синтаксически корректен (проверка через `php -l`)
- [ ] Интерфейс готов к использованию в type hints

## 🔍 Как проверить

1. Проверить синтаксис: `php -l lib/MBMigration/Contracts/MBProjectDataCollectorInterface.php`
2. Сравнить количество методов в интерфейсе с количеством публичных методов в классе
3. Проверить, что сигнатуры методов совпадают
4. Убедиться, что типы параметров совпадают
5. Проверить, что возвращаемые типы совпадают
6. **Запустить тест** (рекомендуется):
   ```bash
   # В Docker контейнере
   docker exec mg_migration vendor/bin/phpunit tests/MBMigration/Contracts/MBProjectDataCollectorInterfaceTest.php
   ```

## ⚠️ Важные замечания

- **Точность критична** - сигнатуры методов должны точно совпадать с классом
- **Типы обязательны** - все параметры и возвращаемые значения должны иметь типы
- **PHPDoc важен** - комментарии помогут понять назначение методов, особенно для методов работы с БД
- **Не пропускать методы** - все публичные методы должны быть в интерфейсе
- **Статические методы** - в PHP интерфейсах статические методы могут быть определены, но нужно проверить совместимость

## 🐛 Возможные проблемы

- Методы в интерфейсе не совпадают с методами в классе - нужно тщательно проверить сигнатуры
- Отсутствуют типы - нужно добавить типы для всех параметров и возвращаемых значений
- Статические методы - нужно проверить, можно ли их включить в интерфейс

## 📚 Связанные задачи

- Предыдущая задача: [Задача 1.4: Изучить класс MBProjectDataCollector](./task-1.4-study-mb-collector.md)
- Следующая задача: [Задача 1.6: Изучить классы Database](./task-1.6-study-database-classes.md)
