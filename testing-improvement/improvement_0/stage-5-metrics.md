# Этап 5: Метрики и мониторинг

## 📋 Обзор этапа

**Длительность**: 1 неделя  
**Приоритет**: НИЗКИЙ (можно отложить)  
**Зависимости**: Этап 3 (тесты должны быть написаны)

## 🎯 Цель этапа

Настроить отслеживание метрик качества кода: покрытие тестами, статический анализ, производительность тестов.

## ❌ Проблема

Сейчас нет:
- Автоматического отслеживания покрытия кода
- Метрик качества кода
- Отчетов о производительности тестов
- Трендов изменения качества

## ✅ Решение

Настроить:
- Автоматическую генерацию отчетов о покрытии
- Интеграцию с сервисами метрик (Codecov, Coveralls)
- Отслеживание трендов
- Дашборды качества кода

---

## 📝 Задачи этапа

### Задача 5.1: Настроить генерацию отчетов о покрытии

**Время**: 1-2 часа

**Что сделать**:
1. Убедиться, что xdebug установлен
2. Настроить генерацию HTML отчетов
3. Настроить генерацию Clover XML (для CI)

**Команды**:
```bash
# HTML отчет (локально)
composer test:coverage

# Clover XML (для CI)
vendor/bin/phpunit --coverage-clover=coverage.xml
```

**Обновить composer.json**:
```json
{
  "scripts": {
    "test:coverage": "vendor/bin/phpunit --coverage-html coverage",
    "test:coverage:ci": "vendor/bin/phpunit --coverage-clover=coverage.xml"
  }
}
```

**Чеклист**:
- [ ] HTML отчеты генерируются
- [ ] Clover XML генерируется
- [ ] Отчеты доступны

---

### Задача 5.2: Интегрировать с Codecov

**Время**: 1-2 часа

**Что сделать**:
1. Зарегистрироваться на codecov.io
2. Добавить токен в CI secrets
3. Обновить CI workflow для загрузки покрытия

**Обновить `.github/workflows/tests.yml`**:
```yaml
- name: Upload coverage to Codecov
  uses: codecov/codecov-action@v3
  with:
    file: ./coverage.xml
    flags: unit,integration
    name: codecov
    fail_ci_if_error: false
  env:
    CODECOV_TOKEN: ${{ secrets.CODECOV_TOKEN }}
```

**Чеклист**:
- [ ] Codecov настроен
- [ ] Токен добавлен в secrets
- [ ] Покрытие загружается
- [ ] Отчеты доступны на codecov.io

---

### Задача 5.3: Настроить PHPStan на более высокий уровень

**Время**: 2-3 часа

**Что сделать**:
1. Увеличить уровень PHPStan (сейчас может быть низкий)
2. Исправить найденные проблемы
3. Добавить в CI как обязательную проверку

**Текущий уровень**: Проверить в `phpstan.neon` или `composer.json`

**Целевой уровень**: 5-6 (рекомендуется начать с 5)

**Команды**:
```bash
# Запуск PHPStan
vendor/bin/phpstan analyse --level=5 lib/ dashboard/

# Генерация baseline (если много ошибок)
vendor/bin/phpstan analyse --level=5 --generate-baseline lib/ dashboard/
```

**Чеклист**:
- [ ] PHPStan уровень 5+
- [ ] Критичные ошибки исправлены
- [ ] Baseline создан (если нужно)
- [ ] PHPStan в CI

---

### Задача 5.4: Настроить Psalm (опционально)

**Время**: 2-3 часа  
**Приоритет**: НИЗКИЙ

**Что сделать**:
1. Установить Psalm: `composer require --dev vimeo/psalm`
2. Инициализировать: `vendor/bin/psalm --init`
3. Настроить уровень
4. Добавить в CI

**Чеклист**:
- [ ] Psalm установлен
- [ ] Настроен
- [ ] Добавлен в CI

---

### Задача 5.5: Настроить отслеживание производительности тестов

**Время**: 1-2 часа

**Что сделать**:
1. Добавить измерение времени выполнения тестов
2. Отслеживать медленные тесты
3. Генерировать отчеты

**PHPUnit уже показывает время**, но можно улучшить:

**Создать скрипт**:
```bash
#!/bin/bash
# scripts/test-performance.sh

echo "Running tests with performance tracking..."

vendor/bin/phpunit --log-junit test-results.xml

# Анализ медленных тестов
grep "time=" test-results.xml | sort -t'"' -k4 -n -r | head -10
```

**Чеклист**:
- [ ] Время выполнения отслеживается
- [ ] Медленные тесты идентифицированы
- [ ] Отчеты генерируются

---

### Задача 5.6: Создать дашборд качества кода

**Время**: 2-3 часа

**Что сделать**:
1. Собрать все метрики в одном месте
2. Создать README с метриками
3. Обновлять автоматически через CI

**Создать `docs/QUALITY_METRICS.md`**:
```markdown
# Метрики качества кода

## Покрытие тестами

- **Unit тесты**: 85% (цель: 80%+)
- **Интеграционные тесты**: 65% (цель: 60%+)
- **Общее покрытие**: 75% (цель: 70%+)

[Подробный отчет](./coverage/index.html)

## Статический анализ

- **PHPStan**: Уровень 5, 0 ошибок
- **Psalm**: Уровень 3, 0 ошибок

## Производительность тестов

- **Unit тесты**: 12 секунд (цель: < 30 сек)
- **Интеграционные тесты**: 45 секунд
- **E2E тесты**: 3 минуты

## Тренды

[Графики на Codecov](https://codecov.io/gh/user/MB-migration)
```

**Обновлять автоматически через CI** (опционально)

**Чеклист**:
- [ ] Дашборд создан
- [ ] Метрики актуальны
- [ ] Доступен команде

---

### Задача 5.7: Настроить алерты при падении метрик

**Время**: 1-2 часа

**Что сделать**:
1. Настроить проверку покрытия в CI
2. Блокировать merge при падении ниже порога
3. Отправлять уведомления

**Обновить CI**:
```yaml
- name: Check coverage threshold
  run: |
    COVERAGE=$(vendor/bin/phpunit --coverage-text --coverage-filter=lib/ | grep "Lines:" | awk '{print $2}' | sed 's/%//')
    THRESHOLD=70
    if (( $(echo "$COVERAGE < $THRESHOLD" | bc -l) )); then
      echo "Coverage $COVERAGE% is below threshold $THRESHOLD%"
      exit 1
    fi
```

**Чеклист**:
- [ ] Проверка порога настроена
- [ ] Блокирует merge при падении
- [ ] Уведомления работают

---

### Задача 5.8: Настроить историю метрик

**Время**: 1-2 часа

**Что сделать**:
1. Сохранять историю покрытия
2. Отслеживать тренды
3. Визуализировать изменения

**Использовать Codecov** (автоматически сохраняет историю)

**Или создать скрипт**:
```bash
#!/bin/bash
# scripts/save-metrics.sh

DATE=$(date +%Y-%m-%d)
COVERAGE=$(vendor/bin/phpunit --coverage-text | grep "Lines:" | awk '{print $2}')

echo "$DATE,$COVERAGE" >> metrics-history.csv
```

**Чеклист**:
- [ ] История сохраняется
- [ ] Тренды видны
- [ ] Можно анализировать изменения

---

### Задача 5.9: Документировать метрики

**Время**: 1 час  
**Файл**: `docs/METRICS.md`

**Что описать**:
1. Какие метрики отслеживаются
2. Как интерпретировать метрики
3. Цели и пороги
4. Как улучшить метрики

**Чеклист**:
- [ ] Документация создана
- [ ] Метрики описаны
- [ ] Цели указаны

---

## 📊 Критерии завершения этапа

- [ ] Отчеты о покрытии генерируются автоматически
- [ ] Codecov интегрирован (или другой сервис)
- [ ] PHPStan уровень 5+
- [ ] Производительность тестов отслеживается
- [ ] Дашборд качества создан
- [ ] Алерты настроены
- [ ] Документация написана

---

## 🎯 Целевые метрики

### Покрытие тестами:
- **Unit тесты**: 80%+ (желательно 85%+)
- **Интеграционные тесты**: 60%+ (желательно 70%+)
- **Общее покрытие**: 70%+ (желательно 75%+)

### Статический анализ:
- **PHPStan**: Уровень 5+, 0 критичных ошибок
- **Psalm**: Уровень 3+ (опционально)

### Производительность:
- **Unit тесты**: < 30 секунд
- **Интеграционные тесты**: < 2 минут
- **E2E тесты**: < 5 минут

---

## 🧪 Проверка работы

### Тест 1: Генерация отчета покрытия
```bash
composer test:coverage
```

**Ожидаемый результат**: 
- HTML отчет создан в `coverage/`
- Можно открыть и просмотреть

### Тест 2: Проверка покрытия в CI
```bash
# Сделать коммит
```

**Ожидаемый результат**: 
- Покрытие загружается в Codecov
- Отчет доступен

### Тест 3: PHPStan проверка
```bash
vendor/bin/phpstan analyse --level=5 lib/
```

**Ожидаемый результат**: 
- Анализ проходит
- Критичные ошибки отсутствуют

---

## 📚 Примеры кода

См. [examples/stage-5/](../examples/stage-5/) для:
- Конфигурации PHPStan
- Скрипты для метрик
- Примеры дашбордов

---

## ⚠️ Важные замечания

1. **Не гнаться за 100% покрытием** - важно покрыть критичные пути
2. **Метрики - инструмент, а не цель** - использовать для улучшения, а не для галочки
3. **Регулярно проверять** - метрики должны обновляться автоматически
4. **Анализировать тренды** - важно не только текущее значение, но и динамика

---

## 🐛 Возможные проблемы

### Проблема: Покрытие падает
**Решение**: 
- Найти непокрытые участки
- Написать тесты для критичных путей
- Использовать отчеты покрытия

### Проблема: PHPStan находит много ошибок
**Решение**: 
- Начать с низкого уровня
- Постепенно повышать
- Использовать baseline для старых ошибок

### Проблема: Тесты медленные
**Решение**: 
- Оптимизировать тесты
- Использовать моки вместо реальных сервисов
- Запускать параллельно

---

## 📅 Примерный план по дням

**День 1**:
- Задачи 5.1-5.2 (отчеты покрытия и Codecov)

**День 2**:
- Задача 5.3 (PHPStan)

**День 3**:
- Задачи 5.4-5.5 (Psalm и производительность)

**День 4**:
- Задачи 5.6-5.7 (дашборд и алерты)

**День 5**:
- Задачи 5.8-5.9 (история и документация)
- Финальная проверка

---

## 🎉 Завершение всех этапов

После завершения Этапа 5:

✅ **Проект полностью тестируем**:
- Код рефакторен для тестирования
- Инфраструктура создана
- Тесты написаны (70%+ покрытие)
- CI/CD настроен
- Метрики отслеживаются

✅ **При каждом коммите**:
- Автоматически запускаются тесты
- Проверяется качество кода
- Генерируются отчеты

✅ **При добавлении новой фичи**:
- Пишутся тесты сразу
- Проверяется, что ничего не сломалось
- Уверенность в стабильности

---

**Поздравляем! Все этапы завершены! 🎉**
